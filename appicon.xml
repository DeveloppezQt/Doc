<?xml version="1.0" encoding="UTF-8"?>
<db:article xmlns:db="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink" version="5.2" xml:lang="en">
<db:info>
<db:title>Setting the Application Icon</db:title>
<db:productname>QtDoc</db:productname>
<db:edition>Qt 5.6.3 Reference Documentation</db:edition>
<db:titleabbrev>Qt Reference Documentation</db:titleabbrev>
<db:abstract>
<db:para>How to set your application's icon.</db:para>
</db:abstract>
<db:pubdate>2025-06-29</db:pubdate>
<db:date>2025-06-29</db:date>
<db:authorgroup>
<db:author>
<db:orgname class="corporation">The Qt Company Ltd.</db:orgname>
</db:author>
</db:authorgroup>
</db:info>
<db:para>The application icon, typically displayed in the top-left corner of an application's top-level windows, is set by calling the <db:link xlink:href="qwindow.xml#setIcon">QWindow::setIcon</db:link>() method.</db:para>
<db:para>In order to change the icon of the executable application file itself, as it is presented on the desktop (i.e., prior to application execution), it is necessary to employ another, platform-dependent technique.</db:para>
<db:section xml:id="setting-the-application-icon-on-windows">
<db:title>Setting the Application Icon on Windows</db:title>
<db:para>First, create an ICO format bitmap file that contains the icon image. This can be done with e.g. Microsoft Visual C++: Select <db:emphasis role="bold">File|New</db:emphasis>, then select the <db:emphasis role="bold">File</db:emphasis> tab in the dialog that appears, and choose <db:emphasis role="bold">Icon</db:emphasis>. (Note that you do not need to load your application into Visual C++; here we are only using the icon editor.)</db:para>
<db:para>Store the ICO file in your application's source code directory, for example, with the name <db:code>myappico.ico</db:code>.</db:para>
<db:para>Then, assuming you are using qmake to generate your makefiles, you only need to add a single line to your <db:code>.pro</db:code> project file:</db:para>
<db:programlisting language="cpp">RC_ICONS = myappico.ico
</db:programlisting>
<db:para>Finally, regenerate your makefile and your application. The <db:code>.exe</db:code> file will now be represented by your icon in Explorer.</db:para>
<db:para>However, if you already have an <db:code>.rc</db:code> file, for example, with the name <db:code>myapp.rc</db:code>, which you want to reuse, the following two steps will be required. First, put a single line of text to the <db:code>myapp.rc</db:code> file:</db:para>
<db:programlisting language="cpp">IDI_ICON1               ICON    DISCARDABLE     &quot;myappico.ico&quot;
</db:programlisting>
<db:para>Then, add this line to your <db:code>myapp.pro</db:code> file:</db:para>
<db:programlisting language="cpp">RC_FILE = myapp.rc
</db:programlisting>
<db:para>If you do not use <db:code>qmake</db:code>, the necessary steps are: first, create an <db:code>.rc</db:code> file and run the <db:code>rc</db:code> or <db:code>windres</db:code> program on the <db:code>.rc</db:code> file, then link your application with the resulting <db:code>.res</db:code> file.</db:para>
</db:section>
<db:section xml:id="setting-the-application-icon-on-macos">
<db:title>Setting the Application Icon on macOS</db:title>
<db:para>The application icon, typically displayed in the application dock area, is set by calling QWindow::setWindowIcon() on a window. It is possible that the program could appear in the application dock area before the function call, in which case a default icon will appear during the bouncing animation.</db:para>
<db:para>To ensure that the correct icon appears, both when the application is being launched, and in the Finder, it is necessary to employ a platform-dependent technique.</db:para>
<db:para>Although many programs can create icon files (<db:code>.icns</db:code>), the recommended approach is to use the <db:emphasis>iconutil</db:emphasis> program supplied by Apple. <db:emphasis>iconutil</db:emphasis> is a command-line tool that converts iconset folders to deployment-ready, high-resolution icns files. Using this tool also compresses the resulting icns file, so there is no need for you to perform additional compression.</db:para>
<db:para>If you are using qmake to generate your makefiles, you only need to add a single line to your <db:code>.pro</db:code> project file. For example, if the name of your icon file is <db:code>myapp.icns</db:code>, and your project file is <db:code>myapp.pro</db:code>, add this line to <db:code>myapp.pro</db:code>:</db:para>
<db:programlisting language="cpp">ICON = myapp.icns
</db:programlisting>
<db:para>This will ensure that <db:code>qmake</db:code> puts your icons in the proper place and creates an <db:code>Info.plist</db:code> entry for the icon.</db:para>
<db:para>If you do not use <db:code>qmake</db:code>, you must do the following manually:</db:para>
<db:orderedlist numeration="arabic">
<db:listitem>
<db:para>Create an <db:code>Info.plist</db:code> file for your application (using the <db:code>PropertyListEditor</db:code>, found in <db:code>Developer/Applications</db:code>).</db:para>
</db:listitem>
<db:listitem>
<db:para>Associate your <db:code>.icns</db:code> record with the <db:code>CFBundleIconFile</db:code> record in the <db:code>Info.plist</db:code> file (again, using the <db:code>PropertyListEditor</db:code>).</db:para>
</db:listitem>
<db:listitem>
<db:para>Copy the <db:code>Info.plist</db:code> file into your application bundle's <db:code>Contents</db:code> directory.</db:para>
</db:listitem>
<db:listitem>
<db:para>Copy the <db:code>.icns</db:code> file into your application bundle's <db:code>Contents/Resources</db:code> directory.</db:para>
</db:listitem>
</db:orderedlist>
</db:section>
<db:section xml:id="setting-the-application-icon-on-common-linux-desktops">
<db:title>Setting the Application Icon on Common Linux Desktops</db:title>
<db:para>In this section we briefly describe the issues involved in providing icons for applications for two common Linux desktop environments: <db:link xlink:href="http://www.kde.org/">KDE</db:link> and <db:link xlink:href="http://www.gnome.org/">GNOME</db:link>. The core technology used to describe application icons is the same for both desktops, and may also apply to others, but there are details which are specific to each. The main source of information on the standards used by these Linux desktops is <db:link xlink:href="http://www.freedesktop.org/">freedesktop.org</db:link>. For information on other Linux desktops please refer to the documentation for the desktops you are interested in.</db:para>
<db:para>Often, users do not use executable files directly, but instead launch applications by clicking icons on the desktop. These icons are representations of &quot;desktop entry files&quot; that contain a description of the application that includes information about its icon. Both desktop environments are able to retrieve the information in these files, and they use it to generate shortcuts to applications on the desktop, in the start menu, and on the panel.</db:para>
<db:para>More information about desktop entry files can be found in the <db:link xlink:href="http://www.freedesktop.org/Standards/desktop-entry-spec">Desktop Entry Specification</db:link>.</db:para>
<db:para>Although desktop entry files can usefully encapsulate the application's details, we still need to store the icons in the conventional location for each desktop environment. A number of locations for icons are given in the <db:link xlink:href="http://www.freedesktop.org/Standards/icon-theme-spec">Icon Theme Specification</db:link>.</db:para>
<db:para>Although the path used to locate icons depends on the desktop in use, and on its configuration, the directory structure beneath each of these should follow the same pattern: subdirectories are arranged by theme, icon size, and application type. Generally, application icons are added to the hicolor theme, so a square application icon 32 pixels in size would be stored in the <db:code>hicolor/32x32/apps</db:code> directory beneath the icon path.</db:para>
<db:section xml:id="k-desktop-environment-kde">
<db:title>K Desktop Environment (KDE)</db:title>
<db:para>Application icons can be installed for use by all users, or on a per-user basis. A user currently logged into their KDE 4 desktop can discover these locations by using kde4-config, for example, by typing the following in a terminal window:</db:para>
<db:programlisting language="cpp">kde4-config --path icon
</db:programlisting>
<db:para>Applications using Qt 5 and KDE Frameworks 5 will find their icons in the list returned by this command:</db:para>
<db:programlisting language="cpp">qtpaths --locate-dirs GenericDataLocation icons
</db:programlisting>
<db:para>Typically, the list of colon-separated paths printed to stdout includes the user-specific icon path and the system-wide path. Beneath these directories, it should be possible to locate and install icons according to the conventions described in the <db:link xlink:href="http://www.freedesktop.org/Standards/icon-theme-spec">Icon Theme Specification</db:link>.</db:para>
<db:para>If you are developing exclusively for KDE, you may wish to take advantage of the <db:link xlink:href="http://techbase.kde.org/Development/CMake/Addons_for_KDE">KDE build system</db:link> to configure your application. This ensures that your icons are installed in the appropriate locations for KDE.</db:para>
<db:para>The KDE developer website is at <db:link xlink:href="http://techbase.kde.org/">http://techbase.kde.org/</db:link>.</db:para>
</db:section>
<db:section xml:id="gnome">
<db:title>GNOME</db:title>
<db:para>Application icons are stored within a standard system-wide directory containing architecture-independent files. This location can be determined by using <db:code>gnome-config</db:code>, for example by typing the following in a terminal window:</db:para>
<db:programlisting language="cpp">gnome-config --datadir
</db:programlisting>
<db:para>The path printed on stdout refers to a location that should contain a directory called <db:code>pixmaps</db:code>; the directory structure within the <db:code>pixmaps</db:code> directory is described in the <db:link xlink:href="http://www.freedesktop.org/Standards/icon-theme-spec">Icon Theme Specification</db:link>.</db:para>
<db:para>If you are developing exclusively for GNOME, you may wish to use the standard set of <db:link xlink:href="http://developer.gnome.org/tools/build.html">GNU Build Tools</db:link>, also described in the relevant section of the <db:link xlink:href="http://developer.gnome.org/doc/GGAD/ggad.html">GTK+/Gnome Application Development book</db:link>. This ensures that your icons are installed in the appropriate locations for GNOME.</db:para>
<db:para>The GNOME developer website is at <db:link xlink:href="http://developer.gnome.org/">http://developer.gnome.org/</db:link>.</db:para>
</db:section>
</db:section>
</db:article>
