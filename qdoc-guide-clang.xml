<?xml version="1.0" encoding="UTF-8"?>
<db:article xmlns:db="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink" version="5.2" xml:lang="en">
<db:info>
<db:title>Installing Clang for QDoc</db:title>
<db:productname>QDoc</db:productname>
<db:edition>Qt 5.12.12 Reference Documentation</db:edition>
<db:titleabbrev>QDoc Manual</db:titleabbrev>
<db:extendedlink xlink:type="extended"><db:link xlink:to="qdoc-guide.xml" xlink:type="arc" xlink:arcrole="prev" xlink:title="Getting Started with QDoc"/></db:extendedlink>
<db:extendedlink xlink:type="extended"><db:link xlink:to="qdoc-guide-conf.xml" xlink:type="arc" xlink:arcrole="next" xlink:title="Creating QDoc Configuration Files"/></db:extendedlink>
<db:abstract>
<db:para>QDoc Manual.</db:para></db:abstract>
<db:pubdate>2024-12-27</db:pubdate>
<db:date>2024-12-27</db:date>
<db:authorgroup>
<db:author>
<db:orgname class="corporation">The Qt Company Ltd.</db:orgname>
</db:author>
</db:authorgroup>
</db:info>
<db:para>QDoc uses Clang when parsing C++ files as well as function signatures in <db:link xlink:href="13-qdoc-commands-topics.xml#fn-command">\fn</db:link> commands. Clang is part of <db:link xlink:href="https://llvm.org/">the LLVM Compiler Infrastructure Project</db:link>. Therefore, if you are going to build QDoc from source, you need to install <db:link xlink:href="http://clang.llvm.org">LLVM 6.0</db:link> or greater first. You can find the required pre-built binaries <db:link xlink:href="http://releases.llvm.org/download.html">here</db:link>.</db:para>
<db:para>To build QDoc on Debian-based Linux distributions, it is sufficient to install the <db:code>libclang-dev</db:code> package and its dependencies. For running QDoc, the <db:code>libclang</db:code> package is required.</db:para>
<db:section xml:id="set-clang-location-automatically">
<db:title>Set Clang location automatically</db:title>
<db:para>The Qt build system uses the tool <db:code>llvm-config</db:code> to discover the location of LLVM on your system, and relies on the path to llvm-config being in the <db:code>PATH</db:code> environment variable to do so. <db:code>llvm-config</db:code> is commonly installed with LLVM and Clang on Linux systems, and also on macOS if LLVM is installed using Brew.</db:para>
<db:para>On Windows, the installation binaries provided by LLVM do not include <db:code>llvm-config</db:code>. You may still have it on your system, if you have built LLVM yourself from source. If that is the case, and you want to let the build system find Clang automatically, add the path to <db:code>llvm-config</db:code> to your <db:code>PATH</db:code> environment variable. For example:</db:para>
<db:programlisting language="cpp" role="bad">C:\&amp;gt; set PATH=%PATH%;C:\[build directory]\bin\Release&quot;
</db:programlisting>
<db:para>Make sure to set the path to your LLVM build directory. The <db:code>llvm-config</db:code> executable is located in <db:code>bin\Release</db:code> or <db:code>bin\Debug</db:code>, reflecting the build version.</db:para>
</db:section>
<db:section xml:id="specify-clang-location-manually">
<db:title>Specify Clang location manually</db:title>
<db:para>If <db:code>llvm-config</db:code> is not in your <db:code>PATH</db:code> environment variable, or not installed on your system, you can still build QDoc, by manually setting the environment variable <db:code>LLVM_INSTALL_DIR</db:code> to point to the directory where the Clang libraries are installed. This directory should be the top level directory. For example, on a Linux or macOS system with LLVM installed to <db:code>/usr/llvm</db:code>:</db:para>
<db:programlisting language="cpp" role="bad">$ export LLVM_INSTALL_DIR=/usr/llvm
</db:programlisting>
<db:para>Debian-based Linux distributions usually offer multiple versions of the <db:code>libclang</db:code> packages, such as <db:code>libclang-&lt;VERSION&gt;</db:code>. In this case, <db:code>LLVM_INSTALL_DIR</db:code> should contain <db:code>/usr/lib/clang/&lt;VERSION&gt;</db:code> .</db:para>
<db:para>On a Windows system with LLVM installed to <db:code>C:\Program Files\LLVM</db:code>:</db:para>
<db:programlisting language="cpp" role="bad">C:\&amp;gt; set LLVM_INSTALL_DIR=C:\Program Files\LLVM
</db:programlisting>
</db:section>
</db:article>
