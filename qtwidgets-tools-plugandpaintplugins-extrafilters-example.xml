<?xml version="1.0" encoding="UTF-8"?>
<db:article xmlns:db="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink" version="5.2" xml:lang="en_US">
<db:info>
<db:title>Plug &amp; Paint Extra Filters Example</db:title>
<db:productname>QtWidgets</db:productname>
<db:edition>Qt 5.2.1 Reference Documentation</db:edition>
<db:titleabbrev>Qt Widgets Reference Documentation</db:titleabbrev>
<db:abstract>
<db:para>Qt Widgets Reference Documentation.</db:para></db:abstract>
<db:pubdate>2025-06-29</db:pubdate>
<db:date>2025-06-29</db:date>
<db:authorgroup>
<db:author>
<db:orgname class="corporation">The Qt Company Ltd.</db:orgname>
</db:author>
</db:authorgroup>
</db:info>
<db:para>The Extra Filters example is a plugin for the <db:link xlink:href="">Plug &amp; Paint</db:link> example. It provides a set of filters in addition to those provided by the <db:link xlink:href="">Basic Tools</db:link> plugin.</db:para>
<db:para>Since the approach is identical to <db:link xlink:href="">Basic Tools</db:link>, we won't review the code here. The only part of interest is the <db:code>.pro</db:code> file, since Extra Filters is a dynamic plugin (<db:link xlink:href="">Basic Tools</db:link> is linked statically into the Plug &amp; Paint executable).</db:para>
<db:para>Here's the project file for building the Extra Filters plugin:</db:para>
<db:programlisting language="cpp">TEMPLATE      = lib
CONFIG       += plugin
QT           += widgets
INCLUDEPATH  += ../..
HEADERS       = extrafiltersplugin.h
SOURCES       = extrafiltersplugin.cpp
TARGET        = $$qtLibraryTarget(pnp_extrafilters)
DESTDIR       = ../../plugandpaint/plugins

</db:programlisting>
<db:para>The <db:code>.pro</db:code> file differs from typical <db:code>.pro</db:code> files in many respects. First, it starts with a <db:code>TEMPLATE</db:code> entry specifying <db:code>lib</db:code>. (The default template is <db:code>app</db:code>.) It also adds <db:code>plugin</db:code> to the <db:code>CONFIG</db:code> variable. This is necessary on some platforms to avoid generating symbolic links with version numbers in the file name, which is appropriate for most dynamic libraries but not for plugins.</db:para>
<db:para>The <db:code>INCLUDEPATH</db:code> variable sets the search paths for global headers (i.e., header files included using <db:code>#include &lt;...&gt;</db:code>). We add Qt's <db:code>examples/tools</db:code> directory (strictly speaking, <db:code>examples/tools/plugandpaintplugins/basictools/../..</db:code>) to the list, so that we can include <db:code>&lt;plugandpaint/interfaces.h&gt;</db:code>.</db:para>
<db:para>The <db:code>TARGET</db:code> variable specifies which name we want to give the target library. We use <db:code>pnp_</db:code> as the prefix to show that the plugin is designed to work with Plug &amp; Paint. On Unix, <db:code>lib</db:code> is also prepended to that name. On all platforms, a platform-specific suffix is appended (e.g., <db:code>.dll</db:code> on Windows, <db:code>.so</db:code> on Linux).</db:para>
<db:para>The <db:code>DESTDIR</db:code> variable specifies where we want to install the plugin. We put it in Plug &amp; Paint's <db:code>plugins</db:code> subdirectory, since that's where the application looks for dynamic plugins.</db:para>
<db:para>The <db:code>CONFIG()</db:code> code at the end is necessary for this example because the example is part of the Qt distribution and Qt can be configured to be built simultaneously in debug and in release modes. You don't need to for your own plugins.</db:para>
<db:section>
<db:title>List of Files</db:title>
<db:para>Files:</db:para>
<db:section>
<db:title>List of Files</db:title>
<db:itemizedlist>
<db:listitem>
<db:para><db:link xlink:href="tools/plugandpaintplugins/extrafilters/extrafilters.pro">tools/plugandpaintplugins/extrafilters/extrafilters.pro</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="tools/plugandpaintplugins/extrafilters/extrafiltersplugin.cpp">tools/plugandpaintplugins/extrafilters/extrafiltersplugin.cpp</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="tools/plugandpaintplugins/extrafilters/extrafiltersplugin.h">tools/plugandpaintplugins/extrafilters/extrafiltersplugin.h</db:link></db:para></db:listitem>
</db:itemizedlist>
</db:section>
</db:section></db:article>
