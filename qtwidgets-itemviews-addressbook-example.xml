<?xml version="1.0" encoding="UTF-8"?>
<db:article xmlns:db="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink" version="5.2" xml:lang="en">
<db:info>
<db:title>Address Book Example</db:title>
<db:productname>QtWidgets</db:productname>
<db:edition>Qt 5.13.2 Reference Documentation</db:edition>
<db:titleabbrev>Qt Widgets Reference Documentation</db:titleabbrev>
<db:abstract>
<db:para>The address book example shows how to use proxy models to display different views onto data from a single model.</db:para>
</db:abstract>
<db:pubdate>2024-12-27</db:pubdate>
<db:date>2024-12-27</db:date>
<db:authorgroup>
<db:author>
<db:orgname class="corporation">The Qt Company Ltd.</db:orgname>
</db:author>
</db:authorgroup>
</db:info>
<db:mediaobject>
<db:alt>Screenshot of the Address Book example</db:alt>
<db:imageobject>
<db:imagedata fileref="images/addressbook-example.png"/>
</db:imageobject>
</db:mediaobject>
<db:para>This example provides an address book that allows contacts to be grouped alphabetically into 9 groups: ABC, DEF, GHI, ... , VW, ..., XYZ. This is achieved by using multiple views on the same model, each of which is filtered using an instance of the QSortFilterProxyModel class.</db:para>
<db:section xml:id="overview">
<db:title>Overview</db:title>
<db:para>The address book contains 5 classes: <db:code>MainWindow</db:code>, <db:code>AddressWidget</db:code>, <db:code>TableModel</db:code>, <db:code>NewAddressTab</db:code> and <db:code>AddDialog</db:code>. The <db:code>MainWindow</db:code> class uses <db:code>AddressWidget</db:code> as its central widget and provides <db:guilabel>File</db:guilabel> and <db:guilabel>Tools</db:guilabel> menus.</db:para>
<db:mediaobject>
<db:alt>Diagram for Address Book Example</db:alt>
<db:imageobject>
<db:imagedata fileref="images/addressbook-classes.png"/>
</db:imageobject>
</db:mediaobject>
<db:para>The <db:code>AddressWidget</db:code> class is a QTabWidget subclass that is used to manipulate the 10 tabs displayed in the example: the 9 alphabet group tabs and an instance of <db:code>NewAddressTab</db:code>. The <db:code>NewAddressTab</db:code> class is a subclass of QWidget that is only used whenever the address book is empty, prompting the user to add some contacts. <db:code>AddressWidget</db:code> also interacts with an instance of <db:code>TableModel</db:code> to add, edit and remove entries to the address book.</db:para>
<db:para><db:code>TableModel</db:code> is a subclass of QAbstractTableModel that provides the standard model/view API to access data. It holds a list of added contacts. However, this data is not all visible in a single tab. Instead, QTableView is used to provide 9 different views of the same data, according to the alphabet groups.</db:para>
<db:para>QSortFilterProxyModel is the class responsible for filtering the contacts for each group of contacts. Each proxy model uses a QRegExp to filter out contacts that do not belong in the corresponding alphabetical group. The <db:code>AddDialog</db:code> class is used to obtain information from the user for the address book. This QDialog subclass is instantiated by <db:code>NewAddressTab</db:code> to add contacts, and by <db:code>AddressWidget</db:code> to add and edit contacts.</db:para>
<db:para>We begin by looking at the <db:code>TableModel</db:code> implementation.</db:para>
</db:section>
<db:section xml:id="tablemodel-class-definition">
<db:title>TableModel Class Definition</db:title>
<db:para>The <db:code>TableModel</db:code> class provides standard API to access data in its list of contacts by subclassing QAbstractTableModel. The basic functions that must be implemented in order to do so are: <db:code>rowCount()</db:code>, <db:code>columnCount()</db:code>, <db:code>data()</db:code>, <db:code>headerData()</db:code>. For TableModel to be editable, it has to provide implementations <db:code>insertRows()</db:code>, <db:code>removeRows()</db:code>, <db:code>setData()</db:code> and <db:code>flags()</db:code> functions.</db:para>
<db:programlisting language="cpp">struct Contact
{
    QString name;
    QString address;

    bool operator==(const Contact &amp;amp;other) const
    {
        return name == other.name &amp;amp;&amp;amp; address == other.address;
    }
};

inline QDataStream &amp;amp;operator&amp;lt;&amp;lt;(QDataStream &amp;amp;stream, const Contact &amp;amp;contact)
{
    return stream &amp;lt;&amp;lt; contact.name &amp;lt;&amp;lt; contact.address;
}

inline QDataStream &amp;amp;operator&amp;gt;&amp;gt;(QDataStream &amp;amp;stream, Contact &amp;amp;contact)
{
    return stream &amp;gt;&amp;gt; contact.name &amp;gt;&amp;gt; contact.address;
}

class TableModel : public QAbstractTableModel
{
    Q_OBJECT

public:
    TableModel(QObject *parent = nullptr);
    TableModel(const QVector&amp;lt;Contact&amp;gt; &amp;amp;contacts, QObject *parent = nullptr);

    int rowCount(const QModelIndex &amp;amp;parent) const override;
    int columnCount(const QModelIndex &amp;amp;parent) const override;
    QVariant data(const QModelIndex &amp;amp;index, int role) const override;
    QVariant headerData(int section, Qt::Orientation orientation, int role) const override;
    Qt::ItemFlags flags(const QModelIndex &amp;amp;index) const override;
    bool setData(const QModelIndex &amp;amp;index, const QVariant &amp;amp;value, int role = Qt::EditRole) override;
    bool insertRows(int position, int rows, const QModelIndex &amp;amp;index = QModelIndex()) override;
    bool removeRows(int position, int rows, const QModelIndex &amp;amp;index = QModelIndex()) override;
    const QVector&amp;lt;Contact&amp;gt; &amp;amp;getContacts() const;

private:
    QVector&amp;lt;Contact&amp;gt; contacts;
};
</db:programlisting>
<db:para>Two constructors are used, a default constructor which uses <db:code>TableModel</db:code>'s own <db:code>QVector&lt;Contact&gt;</db:code> and one that takes <db:code>QVector&lt;Contact&gt;</db:code> as an argument, for convenience.</db:para>
</db:section>
<db:section xml:id="tablemodel-class-implementation">
<db:title>TableModel Class Implementation</db:title>
<db:para>We implement the two constructors as defined in the header file. The second constructor initializes the list of contacts in the model, with the parameter value.</db:para>
<db:programlisting language="cpp">TableModel::TableModel(QObject *parent)
    : QAbstractTableModel(parent)
{
}

TableModel::TableModel(const QVector&amp;lt;Contact&amp;gt; &amp;amp;contacts, QObject *parent)
    : QAbstractTableModel(parent),
      contacts(contacts)
{
}
</db:programlisting>
<db:para>The <db:code>rowCount()</db:code> and <db:code>columnCount()</db:code> functions return the dimensions of the model. Whereas, <db:code>rowCount()</db:code>'s value will vary depending on the number of contacts added to the address book, <db:code>columnCount()</db:code>'s value is always 2 because we only need space for the <db:emphasis role="bold">Name</db:emphasis> and <db:emphasis role="bold">Address</db:emphasis> columns.</db:para>
<db:programlisting language="cpp">int TableModel::rowCount(const QModelIndex &amp;amp;parent) const
{
    return parent.isValid() ? 0 : contacts.size();
}

int TableModel::columnCount(const QModelIndex &amp;amp;parent) const
{
    return parent.isValid() ? 0 : 2;
}
</db:programlisting>
<db:para>The <db:code>data()</db:code> function returns either a <db:emphasis role="bold">Name</db:emphasis> or <db:emphasis role="bold">Address</db:emphasis>, based on the contents of the model index supplied. The row number stored in the model index is used to reference an item in the list of contacts. Selection is handled by the QItemSelectionModel, which will be explained with <db:code>AddressWidget</db:code>.</db:para>
<db:programlisting language="cpp">QVariant TableModel::data(const QModelIndex &amp;amp;index, int role) const
{
    if (!index.isValid())
        return QVariant();

    if (index.row() &amp;gt;= contacts.size() || index.row() &amp;lt; 0)
        return QVariant();

    if (role == Qt::DisplayRole) {
        const auto &amp;amp;contact = contacts.at(index.row());

        switch (index.column()) {
            case 0:
                return contact.name;
            case 1:
                return contact.address;
            default:
                break;
        }
    }
    return QVariant();
}
</db:programlisting>
<db:para>The <db:code>headerData()</db:code> function displays the table's header, <db:emphasis role="bold">Name</db:emphasis> and <db:emphasis role="bold">Address</db:emphasis>. If you require numbered entries for your address book, you can use a vertical header which we have hidden in this example (see the <db:code>AddressWidget</db:code> implementation).</db:para>
<db:programlisting language="cpp">QVariant TableModel::headerData(int section, Qt::Orientation orientation, int role) const
{
    if (role != Qt::DisplayRole)
        return QVariant();

    if (orientation == Qt::Horizontal) {
        switch (section) {
            case 0:
                return tr(&quot;Name&quot;);
            case 1:
                return tr(&quot;Address&quot;);
            default:
                break;
        }
    }
    return QVariant();
}
</db:programlisting>
<db:para>The <db:code>insertRows()</db:code> function is called before new data is added, otherwise the data will not be displayed. The <db:code>beginInsertRows()</db:code> and <db:code>endInsertRows()</db:code> functions are called to ensure all connected views are aware of the changes.</db:para>
<db:programlisting language="cpp">bool TableModel::insertRows(int position, int rows, const QModelIndex &amp;amp;index)
{
    Q_UNUSED(index);
    beginInsertRows(QModelIndex(), position, position + rows - 1);

    for (int row = 0; row &amp;lt; rows; ++row)
        contacts.insert(position, { QString(), QString() });

    endInsertRows();
    return true;
}
</db:programlisting>
<db:para>The <db:code>removeRows()</db:code> function is called to remove data. Again, <db:link xlink:href="">beginRemoveRows()</db:link> and <db:link xlink:href="">endRemoveRows()</db:link> are called to ensure all connected views are aware of the changes.</db:para>
<db:programlisting language="cpp">bool TableModel::removeRows(int position, int rows, const QModelIndex &amp;amp;index)
{
    Q_UNUSED(index);
    beginRemoveRows(QModelIndex(), position, position + rows - 1);

    for (int row = 0; row &amp;lt; rows; ++row)
        contacts.removeAt(position);

    endRemoveRows();
    return true;
}
</db:programlisting>
<db:para>The <db:code>setData()</db:code> function is the function that inserts data into the table, item by item and not row by row. This means that to fill a row in the address book, <db:code>setData()</db:code> must be called twice, as each row has 2 columns. It is important to emit the <db:link xlink:href="">dataChanged()</db:link> signal as it tells all connected views to update their displays.</db:para>
<db:programlisting language="cpp">bool TableModel::setData(const QModelIndex &amp;amp;index, const QVariant &amp;amp;value, int role)
{
    if (index.isValid() &amp;amp;&amp;amp; role == Qt::EditRole) {
        const int row = index.row();
        auto contact = contacts.value(row);

        switch (index.column()) {
            case 0:
                contact.name = value.toString();
                break;
            case 1:
                contact.address = value.toString();
                break;
            default:
                return false;
        }
        contacts.replace(row, contact);
        emit dataChanged(index, index, {Qt::DisplayRole, Qt::EditRole});

        return true;
    }

    return false;
}
</db:programlisting>
<db:para>The <db:code>flags()</db:code> function returns the item flags for the given index.</db:para>
<db:programlisting language="cpp">Qt::ItemFlags TableModel::flags(const QModelIndex &amp;amp;index) const
{
    if (!index.isValid())
        return Qt::ItemIsEnabled;

    return QAbstractTableModel::flags(index) | Qt::ItemIsEditable;
}
</db:programlisting>
<db:para>We set the Qt::ItemIsEditable flag because we want to allow the <db:code>TableModel</db:code> to be edited. Although for this example we don't use the editing features of the QTableView object, we enable them here so that we can reuse the model in other programs.</db:para>
<db:para>The last function in <db:code>TableModel</db:code>, <db:code>getContacts()</db:code> returns the <db:link xlink:href="qvector.xml">QVector</db:link>&lt;Contact&gt; object that holds all the contacts in the address book. We use this function later to obtain the list of contacts to check for existing entries, write the contacts to a file and read them back. Further explanation is given with <db:code>AddressWidget</db:code>.</db:para>
<db:programlisting language="cpp">const QVector&amp;lt;Contact&amp;gt; &amp;amp;TableModel::getContacts() const
{
    return contacts;
}
</db:programlisting>
</db:section>
<db:section xml:id="addresswidget-class-definition">
<db:title>AddressWidget Class Definition</db:title>
<db:para>The <db:code>AddressWidget</db:code> class is technically the main class involved in this example as it provides functions to add, edit and remove contacts, to save the contacts to a file and to load them from a file.</db:para>
<db:programlisting language="cpp">class AddressWidget : public QTabWidget
{
    Q_OBJECT

public:
    AddressWidget(QWidget *parent = nullptr);
    void readFromFile(const QString &amp;amp;fileName);
    void writeToFile(const QString &amp;amp;fileName);

public slots:
    void showAddEntryDialog();
    void addEntry(const QString &amp;amp;name, const QString &amp;amp;address);
    void editEntry();
    void removeEntry();

signals:
    void selectionChanged (const QItemSelection &amp;amp;selected);

private:
    void setupTabs();

    TableModel *table;
    NewAddressTab *newAddressTab;
};
</db:programlisting>
<db:para><db:code>AddressWidget</db:code> extends QTabWidget in order to hold 10 tabs (<db:code>NewAddressTab</db:code> and the 9 alphabet group tabs) and also manipulates <db:code>table</db:code>, the <db:code>TableModel</db:code> object, <db:code>proxyModel</db:code>, the QSortFilterProxyModel object that we use to filter the entries, and <db:code>tableView</db:code>, the QTableView object.</db:para>
</db:section>
<db:section xml:id="addresswidget-class-implementation">
<db:title>AddressWidget Class Implementation</db:title>
<db:para>The <db:code>AddressWidget</db:code> constructor accepts a parent widget and instantiates <db:code>NewAddressTab</db:code>, <db:code>TableModel</db:code> and QSortFilterProxyModel. The <db:code>NewAddressTab</db:code> object, which is used to indicate that the address book is empty, is added and the rest of the 9 tabs are set up with <db:code>setupTabs()</db:code>.</db:para>
<db:programlisting language="cpp">AddressWidget::AddressWidget(QWidget *parent)
    : QTabWidget(parent),
      table(new TableModel(this)),
      newAddressTab(new NewAddressTab(this))
{
    connect(newAddressTab, &amp;amp;NewAddressTab::sendDetails,
        this, &amp;amp;AddressWidget::addEntry);

    addTab(newAddressTab, tr(&quot;Address Book&quot;));

    setupTabs();
}
</db:programlisting>
<db:para>The <db:code>setupTabs()</db:code> function is used to set up the 9 alphabet group tabs, table views and proxy models in <db:code>AddressWidget</db:code>. Each proxy model in turn is set to filter contact names according to the relevant alphabet group using a <db:link xlink:href="">case-insensitive</db:link> QRegExp object. The table views are also sorted in ascending order using the corresponding proxy model's <db:link xlink:href="">sort()</db:link> function.</db:para>
<db:para>Each table view's selectionMode is set to QAbstractItemView::SingleSelection and selectionBehavior is set to QAbstractItemView::SelectRows, allowing the user to select all the items in one row at the same time. Each QTableView object is automatically given a QItemSelectionModel that keeps track of the selected indexes.</db:para>
<db:programlisting language="cpp">void AddressWidget::setupTabs()
{
    const auto groups = { &quot;ABC&quot;, &quot;DEF&quot;, &quot;GHI&quot;, &quot;JKL&quot;, &quot;MNO&quot;, &quot;PQR&quot;, &quot;STU&quot;, &quot;VW&quot;, &quot;XYZ&quot; };

    for (const QString &amp;amp;str : groups) {
        const auto regExp = QRegularExpression(QString(&quot;^[%1].*&quot;).arg(str),
                                               QRegularExpression::CaseInsensitiveOption);

        auto proxyModel = new QSortFilterProxyModel(this);
        proxyModel-&amp;gt;setSourceModel(table);
        proxyModel-&amp;gt;setFilterRegularExpression(regExp);
        proxyModel-&amp;gt;setFilterKeyColumn(0);

        QTableView *tableView = new QTableView;
        tableView-&amp;gt;setModel(proxyModel);
        tableView-&amp;gt;setSelectionBehavior(QAbstractItemView::SelectRows);
        tableView-&amp;gt;horizontalHeader()-&amp;gt;setStretchLastSection(true);
        tableView-&amp;gt;verticalHeader()-&amp;gt;hide();
        tableView-&amp;gt;setEditTriggers(QAbstractItemView::NoEditTriggers);
        tableView-&amp;gt;setSelectionMode(QAbstractItemView::SingleSelection);
        tableView-&amp;gt;setSortingEnabled(true);

        connect(tableView-&amp;gt;selectionModel(), &amp;amp;QItemSelectionModel::selectionChanged,
                this, &amp;amp;AddressWidget::selectionChanged);

        connect(this, &amp;amp;QTabWidget::currentChanged, this, [this, tableView](int tabIndex) {
            if (widget(tabIndex) == tableView)
                emit selectionChanged(tableView-&amp;gt;selectionModel()-&amp;gt;selection());
        });

        addTab(tableView, str);
    }
}
</db:programlisting>
<db:para>The QItemSelectionModel class provides a selectionChanged signal that is connected to <db:code>AddressWidget</db:code>'s <db:code>selectionChanged()</db:code> signal. We also connect QTabWidget::currentChanged() signal to the lambda expression which emits <db:code>AddressWidget</db:code>'s <db:code>selectionChanged()</db:code> as well. These connections are necessary to enable the <db:guilabel>Edit Entry...</db:guilabel> and <db:guilabel>Remove Entry</db:guilabel> actions in <db:code>MainWindow</db:code>'s Tools menu. It is further explained in <db:code>MainWindow</db:code>'s implementation.</db:para>
<db:para>Each table view in the address book is added as a tab to the QTabWidget with the relevant label, obtained from the <db:link xlink:href="qstringlist.xml">QStringList</db:link> of groups.</db:para>
<db:mediaobject>
<db:alt>Signals and Slots Connections</db:alt>
<db:imageobject>
<db:imagedata fileref="images/addressbook-signals.png"/>
</db:imageobject>
</db:mediaobject>
<db:para>We provide two <db:code>addEntry()</db:code> functions: One which is intended to be used to accept user input, and the other which performs the actual task of adding new entries to the address book. We divide the responsibility of adding entries into two parts to allow <db:code>newAddressTab</db:code> to insert data without having to popup a dialog.</db:para>
<db:para>The first <db:code>addEntry()</db:code> function is a slot connected to the <db:code>MainWindow</db:code>'s <db:guilabel>Add Entry...</db:guilabel> action. This function creates an <db:code>AddDialog</db:code> object and then calls the second <db:code>addEntry()</db:code> function to actually add the contact to <db:code>table</db:code>.</db:para>
<db:programlisting language="cpp">void AddressWidget::showAddEntryDialog()
{
    AddDialog aDialog;

    if (aDialog.exec())
        addEntry(aDialog.name(), aDialog.address());
}
</db:programlisting>
<db:para>Basic validation is done in the second <db:code>addEntry()</db:code> function to prevent duplicate entries in the address book. As mentioned with <db:code>TableModel</db:code>, this is part of the reason why we require the getter method <db:code>getContacts()</db:code>.</db:para>
<db:programlisting language="cpp">void AddressWidget::addEntry(const QString &amp;amp;name, const QString &amp;amp;address)
{
    if (!table-&amp;gt;getContacts().contains({ name, address })) {
        table-&amp;gt;insertRows(0, 1, QModelIndex());

        QModelIndex index = table-&amp;gt;index(0, 0, QModelIndex());
        table-&amp;gt;setData(index, name, Qt::EditRole);
        index = table-&amp;gt;index(0, 1, QModelIndex());
        table-&amp;gt;setData(index, address, Qt::EditRole);
        removeTab(indexOf(newAddressTab));
    } else {
        QMessageBox::information(this, tr(&quot;Duplicate Name&quot;),
            tr(&quot;The name \&quot;%1\&quot; already exists.&quot;).arg(name));
    }
}
</db:programlisting>
<db:para>If the model does not already contain an entry with the same name, we call <db:code>setData()</db:code> to insert the name and address into the first and second columns. Otherwise, we display a QMessageBox to inform the user.</db:para>
<db:note>
<db:para>The <db:code>newAddressTab</db:code> is removed once a contact is added as the address book is no longer empty.</db:para>
</db:note>
<db:para>Editing an entry is a way to update the contact's address only, as the example does not allow the user to change the name of an existing contact.</db:para>
<db:para>Firstly, we obtain the active tab's QTableView object using QTabWidget::currentWidget(). Then we extract the <db:code>selectionModel</db:code> from the <db:code>tableView</db:code> to obtain the selected indexes.</db:para>
<db:programlisting language="cpp">void AddressWidget::editEntry()
{
    QTableView *temp = static_cast&amp;lt;QTableView*&amp;gt;(currentWidget());
    QSortFilterProxyModel *proxy = static_cast&amp;lt;QSortFilterProxyModel*&amp;gt;(temp-&amp;gt;model());
    QItemSelectionModel *selectionModel = temp-&amp;gt;selectionModel();

    const QModelIndexList indexes = selectionModel-&amp;gt;selectedRows();
    QString name;
    QString address;
    int row = -1;

    for (const QModelIndex &amp;amp;index : indexes) {
        row = proxy-&amp;gt;mapToSource(index).row();
        QModelIndex nameIndex = table-&amp;gt;index(row, 0, QModelIndex());
        QVariant varName = table-&amp;gt;data(nameIndex, Qt::DisplayRole);
        name = varName.toString();

        QModelIndex addressIndex = table-&amp;gt;index(row, 1, QModelIndex());
        QVariant varAddr = table-&amp;gt;data(addressIndex, Qt::DisplayRole);
        address = varAddr.toString();
    }
</db:programlisting>
<db:para>Next we extract data from the row the user intends to edit. This data is displayed in an instance of <db:code>AddDialog</db:code> with a different window title. The <db:code>table</db:code> is only updated if changes have been made to data in <db:code>aDialog</db:code>.</db:para>
<db:programlisting language="cpp">    AddDialog aDialog;
    aDialog.setWindowTitle(tr(&quot;Edit a Contact&quot;));
    aDialog.editAddress(name, address);

    if (aDialog.exec()) {
        const QString newAddress = aDialog.address();
        if (newAddress != address) {
            const QModelIndex index = table-&amp;gt;index(row, 1, QModelIndex());
            table-&amp;gt;setData(index, newAddress, Qt::EditRole);
        }
    }
}
</db:programlisting>
<db:mediaobject>
<db:alt>Screenshot of Dialog to Edit a Contact</db:alt>
<db:imageobject>
<db:imagedata fileref="images/addressbook-editdialog.png"/>
</db:imageobject>
</db:mediaobject>
<db:para>Entries are removed using the <db:code>removeEntry()</db:code> function. The selected row is removed by accessing it through the QItemSelectionModel object, <db:code>selectionModel</db:code>. The <db:code>newAddressTab</db:code> is re-added to the <db:code>AddressWidget</db:code> only if the user removes all the contacts in the address book.</db:para>
<db:programlisting language="cpp">void AddressWidget::removeEntry()
{
    QTableView *temp = static_cast&amp;lt;QTableView*&amp;gt;(currentWidget());
    QSortFilterProxyModel *proxy = static_cast&amp;lt;QSortFilterProxyModel*&amp;gt;(temp-&amp;gt;model());
    QItemSelectionModel *selectionModel = temp-&amp;gt;selectionModel();

    const QModelIndexList indexes = selectionModel-&amp;gt;selectedRows();

    for (QModelIndex index : indexes) {
        int row = proxy-&amp;gt;mapToSource(index).row();
        table-&amp;gt;removeRows(row, 1, QModelIndex());
    }

    if (table-&amp;gt;rowCount(QModelIndex()) == 0)
        insertTab(0, newAddressTab, tr(&quot;Address Book&quot;));
}
</db:programlisting>
<db:para>The <db:code>writeToFile()</db:code> function is used to save a file containing all the contacts in the address book. The file is saved in a custom <db:code>.dat</db:code> format. The contents of the list of contacts are written to <db:code>file</db:code> using QDataStream. If the file cannot be opened, a QMessageBox is displayed with the related error message.</db:para>
<db:programlisting language="cpp">void AddressWidget::writeToFile(const QString &amp;amp;fileName)
{
    QFile file(fileName);

    if (!file.open(QIODevice::WriteOnly)) {
        QMessageBox::information(this, tr(&quot;Unable to open file&quot;), file.errorString());
        return;
    }

    QDataStream out(&amp;amp;file);
    out &amp;lt;&amp;lt; table-&amp;gt;getContacts();
}
</db:programlisting>
<db:para>The <db:code>readFromFile()</db:code> function loads a file containing all the contacts in the address book, previously saved using <db:code>writeToFile()</db:code>. QDataStream is used to read the contents of a <db:code>.dat</db:code> file into a list of contacts and each of these is added using <db:code>addEntry()</db:code>.</db:para>
<db:programlisting language="cpp">void AddressWidget::readFromFile(const QString &amp;amp;fileName)
{
    QFile file(fileName);

    if (!file.open(QIODevice::ReadOnly)) {
        QMessageBox::information(this, tr(&quot;Unable to open file&quot;),
            file.errorString());
        return;
    }

    QVector&amp;lt;Contact&amp;gt; contacts;
    QDataStream in(&amp;amp;file);
    in &amp;gt;&amp;gt; contacts;

    if (contacts.isEmpty()) {
        QMessageBox::information(this, tr(&quot;No contacts in file&quot;),
                                 tr(&quot;The file you are attempting to open contains no contacts.&quot;));
    } else {
        for (const auto &amp;amp;contact: qAsConst(contacts))
            addEntry(contact.name, contact.address);
    }
}
</db:programlisting>
</db:section>
<db:section xml:id="newaddresstab-class-definition">
<db:title>NewAddressTab Class Definition</db:title>
<db:para>The <db:code>NewAddressTab</db:code> class provides an informative tab telling the user that the address book is empty. It appears and disappears according to the contents of the address book, as mentioned in <db:code>AddressWidget</db:code>'s implementation.</db:para>
<db:mediaobject>
<db:alt>Screenshot of NewAddressTab</db:alt>
<db:imageobject>
<db:imagedata fileref="images/addressbook-newaddresstab.png"/>
</db:imageobject>
</db:mediaobject>
<db:para>The <db:code>NewAddressTab</db:code> class extends QWidget and contains a QLabel and QPushButton.</db:para>
<db:programlisting language="cpp">class NewAddressTab : public QWidget
{
    Q_OBJECT

public:
    NewAddressTab(QWidget *parent = nullptr);

public slots:
    void addEntry();

signals:
    void sendDetails(const QString &amp;amp;name, const QString &amp;amp;address);
};
</db:programlisting>
</db:section>
<db:section xml:id="newaddresstab-class-implementation">
<db:title>NewAddressTab Class Implementation</db:title>
<db:para>The constructor instantiates the <db:code>addButton</db:code>, <db:code>descriptionLabel</db:code> and connects the <db:code>addButton</db:code>'s signal to the <db:code>addEntry()</db:code> slot.</db:para>
<db:programlisting language="cpp">NewAddressTab::NewAddressTab(QWidget *parent)
    : QWidget(parent)
{
    auto descriptionLabel = new QLabel(tr(&quot;There are currently no contacts in your address book. &quot;
                                          &quot;\nClick Add to add new contacts.&quot;));

    auto addButton = new QPushButton(tr(&quot;Add&quot;));

    connect(addButton, &amp;amp;QAbstractButton::clicked, this, &amp;amp;NewAddressTab::addEntry);

    auto mainLayout = new QVBoxLayout;
    mainLayout-&amp;gt;addWidget(descriptionLabel);
    mainLayout-&amp;gt;addWidget(addButton, 0, Qt::AlignCenter);

    setLayout(mainLayout);
}
</db:programlisting>
<db:para>The <db:code>addEntry()</db:code> function is similar to <db:code>AddressWidget</db:code>'s <db:code>addEntry()</db:code> in the sense that both functions instantiate an <db:code>AddDialog</db:code> object. Data from the dialog is extracted and sent to <db:code>AddressWidget</db:code>'s <db:code>addEntry()</db:code> slot by emitting the <db:code>sendDetails()</db:code> signal.</db:para>
<db:programlisting language="cpp">void NewAddressTab::addEntry()
{
    AddDialog aDialog;

    if (aDialog.exec())
        emit sendDetails(aDialog.name(), aDialog.address());
}
</db:programlisting>
<db:mediaobject>
<db:imageobject>
<db:imagedata fileref="images/signals-n-slots-aw-nat.png"/>
</db:imageobject>
</db:mediaobject>
</db:section>
<db:section xml:id="adddialog-class-definition">
<db:title>AddDialog Class Definition</db:title>
<db:para>The <db:code>AddDialog</db:code> class extends QDialog and provides the user with a QLineEdit and a QTextEdit to input data into the address book.</db:para>
<db:programlisting language="cpp">class AddDialog : public QDialog
{
    Q_OBJECT

public:
    AddDialog(QWidget *parent = nullptr);

    QString name() const;
    QString address() const;
    void editAddress(const QString &amp;amp;name, const QString &amp;amp;address);

private:
    QLineEdit *nameText;
    QTextEdit *addressText;
};
</db:programlisting>
<db:mediaobject>
<db:imageobject>
<db:imagedata fileref="images/addressbook-adddialog.png"/>
</db:imageobject>
</db:mediaobject>
</db:section>
<db:section xml:id="adddialog-class-implementation">
<db:title>AddDialog Class Implementation</db:title>
<db:para>The <db:code>AddDialog</db:code>'s constructor sets up the user interface, creating the necessary widgets and placing them into layouts.</db:para>
<db:programlisting language="cpp">AddDialog::AddDialog(QWidget *parent)
    : QDialog(parent),
      nameText(new QLineEdit),
      addressText(new QTextEdit)
{
    auto nameLabel = new QLabel(tr(&quot;Name&quot;));
    auto addressLabel = new QLabel(tr(&quot;Address&quot;));
    auto okButton = new QPushButton(tr(&quot;OK&quot;));
    auto cancelButton = new QPushButton(tr(&quot;Cancel&quot;));

    auto gLayout = new QGridLayout;
    gLayout-&amp;gt;setColumnStretch(1, 2);
    gLayout-&amp;gt;addWidget(nameLabel, 0, 0);
    gLayout-&amp;gt;addWidget(nameText, 0, 1);

    gLayout-&amp;gt;addWidget(addressLabel, 1, 0, Qt::AlignLeft|Qt::AlignTop);
    gLayout-&amp;gt;addWidget(addressText, 1, 1, Qt::AlignLeft);

    auto buttonLayout = new QHBoxLayout;
    buttonLayout-&amp;gt;addWidget(okButton);
    buttonLayout-&amp;gt;addWidget(cancelButton);

    gLayout-&amp;gt;addLayout(buttonLayout, 2, 1, Qt::AlignRight);

    auto mainLayout = new QVBoxLayout;
    mainLayout-&amp;gt;addLayout(gLayout);
    setLayout(mainLayout);

    connect(okButton, &amp;amp;QAbstractButton::clicked, this, &amp;amp;QDialog::accept);
    connect(cancelButton, &amp;amp;QAbstractButton::clicked, this, &amp;amp;QDialog::reject);

    setWindowTitle(tr(&quot;Add a Contact&quot;));
}

QString AddDialog::name() const
{
    return nameText-&amp;gt;text();
}

QString AddDialog::address() const
{
    return addressText-&amp;gt;toPlainText();
}

void AddDialog::editAddress(const QString &amp;amp;name, const QString &amp;amp;address)
{
    nameText-&amp;gt;setReadOnly(true);
    nameText-&amp;gt;setText(name);
    addressText-&amp;gt;setPlainText(address);
}
</db:programlisting>
<db:para>To give the dialog the desired behavior, we connect the <db:guilabel>OK</db:guilabel> and <db:guilabel>Cancel</db:guilabel> buttons to the dialog's <db:link xlink:href="">accept()</db:link> and <db:link xlink:href="">reject()</db:link> slots. Since the dialog only acts as a container for name and address information, we do not need to implement any other functions for it.</db:para>
</db:section>
<db:section xml:id="mainwindow-class-definition">
<db:title>MainWindow Class Definition</db:title>
<db:para>The <db:code>MainWindow</db:code> class extends QMainWindow and implements the menus and actions necessary to manipulate the address book.</db:para>
<db:informaltable style="generic">
<db:tr valign="top">
<db:td>
<db:para><db:inlinemediaobject>
<db:imageobject>
<db:imagedata fileref="images/addressbook-filemenu.png"/>
</db:imageobject>
</db:inlinemediaobject></db:para>
</db:td>
<db:td>
<db:para><db:inlinemediaobject>
<db:imageobject>
<db:imagedata fileref="images/addressbook-toolsmenu.png"/>
</db:imageobject>
</db:inlinemediaobject></db:para>
</db:td>
</db:tr>
</db:informaltable>
<db:programlisting language="cpp">class MainWindow : public QMainWindow
{
    Q_OBJECT

public:
    MainWindow();

private slots:
    void updateActions(const QItemSelection &amp;amp;selection);
    void openFile();
    void saveFile();

private:
    void createMenus();

    AddressWidget *addressWidget;
    QAction *editAct;
    QAction *removeAct;
};
</db:programlisting>
<db:para>The <db:code>MainWindow</db:code> class uses an <db:code>AddressWidget</db:code> as its central widget and provides the File menu with <db:guilabel>Open</db:guilabel>, <db:guilabel>Close</db:guilabel> and <db:guilabel>Exit</db:guilabel> actions, as well as the <db:guilabel>Tools</db:guilabel> menu with <db:guilabel>Add Entry...</db:guilabel>, <db:guilabel>Edit Entry...</db:guilabel> and <db:guilabel>Remove Entry</db:guilabel> actions.</db:para>
</db:section>
<db:section xml:id="mainwindow-class-implementation">
<db:title>MainWindow Class Implementation</db:title>
<db:para>The constructor for <db:code>MainWindow</db:code> instantiates AddressWidget, sets it as its central widget and calls the <db:code>createMenus()</db:code> function.</db:para>
<db:programlisting language="cpp">MainWindow::MainWindow()
    : QMainWindow(),
      addressWidget(new AddressWidget)
{
    setCentralWidget(addressWidget);
    createMenus();
    setWindowTitle(tr(&quot;Address Book&quot;));
}
</db:programlisting>
<db:para>The <db:code>createMenus()</db:code> function sets up the <db:guilabel>File</db:guilabel> and <db:guilabel>Tools</db:guilabel> menus, connecting the actions to their respective slots. Both the <db:guilabel>Edit Entry...</db:guilabel> and <db:guilabel>Remove Entry</db:guilabel> actions are disabled by default as such actions cannot be carried out on an empty address book. They are only enabled when one or more contacts are added.</db:para>
<db:programlisting language="cpp">void MainWindow::createMenus()
{
    QMenu *fileMenu = menuBar()-&amp;gt;addMenu(tr(&quot;&amp;amp;File&quot;));

    QAction *openAct = new QAction(tr(&quot;&amp;amp;Open...&quot;), this);
    fileMenu-&amp;gt;addAction(openAct);
    connect(openAct, &amp;amp;QAction::triggered, this, &amp;amp;MainWindow::openFile);
    ...

    editAct = new QAction(tr(&quot;&amp;amp;Edit Entry...&quot;), this);
    editAct-&amp;gt;setEnabled(false);
    toolMenu-&amp;gt;addAction(editAct);
    connect(editAct, &amp;amp;QAction::triggered, addressWidget, &amp;amp;AddressWidget::editEntry);

    toolMenu-&amp;gt;addSeparator();

    removeAct = new QAction(tr(&quot;&amp;amp;Remove Entry&quot;), this);
    removeAct-&amp;gt;setEnabled(false);
    toolMenu-&amp;gt;addAction(removeAct);
    connect(removeAct, &amp;amp;QAction::triggered, addressWidget, &amp;amp;AddressWidget::removeEntry);

    connect(addressWidget, &amp;amp;AddressWidget::selectionChanged,
        this, &amp;amp;MainWindow::updateActions);
}
</db:programlisting>
<db:para>Apart from connecting all the actions' signals to their respective slots, we also connect <db:code>AddressWidget</db:code>'s <db:code>selectionChanged()</db:code> signal to its <db:code>updateActions()</db:code> slot.</db:para>
<db:para>The <db:code>openFile()</db:code> function allows the user to choose a file with the <db:link xlink:href="">open file dialog</db:link>. The chosen file has to be a custom <db:code>.dat</db:code> file that contains address book contacts. This function is a slot connected to <db:code>openAct</db:code> in the <db:guilabel>File</db:guilabel> menu.</db:para>
<db:programlisting language="cpp">void MainWindow::openFile()
{
    QString fileName = QFileDialog::getOpenFileName(this);
    if (!fileName.isEmpty())
        addressWidget-&amp;gt;readFromFile(fileName);
}
</db:programlisting>
<db:para>The <db:code>saveFile()</db:code> function allows the user to save a file with the <db:link xlink:href="">save file dialog</db:link>. This function is a slot connected to <db:code>saveAct</db:code> in the <db:guilabel>File</db:guilabel> menu.</db:para>
<db:programlisting language="cpp">void MainWindow::saveFile()
{
    QString fileName = QFileDialog::getSaveFileName(this);
    if (!fileName.isEmpty())
        addressWidget-&amp;gt;writeToFile(fileName);
}
</db:programlisting>
<db:para>The <db:code>updateActions()</db:code> function enables and disables <db:guilabel>Edit Entry...</db:guilabel> and <db:guilabel>Remove Entry</db:guilabel> depending on the contents of the address book. If the address book is empty, these actions are disabled; otherwise, they are enabled. This function is a slot is connected to the <db:code>AddressWidget</db:code>'s <db:code>selectionChanged()</db:code> signal.</db:para>
<db:programlisting language="cpp">void MainWindow::updateActions(const QItemSelection &amp;amp;selection)
{
    QModelIndexList indexes = selection.indexes();

    if (!indexes.isEmpty()) {
        removeAct-&amp;gt;setEnabled(true);
        editAct-&amp;gt;setEnabled(true);
    } else {
        removeAct-&amp;gt;setEnabled(false);
        editAct-&amp;gt;setEnabled(false);
    }
}
</db:programlisting>
</db:section>
<db:section xml:id="main-function">
<db:title>main() Function</db:title>
<db:para>The main function for the address book instantiates QApplication and opens a <db:code>MainWindow</db:code> before running the event loop.</db:para>
<db:programlisting language="cpp">int main(int argc, char *argv[])
{
    QApplication app(argc, argv);
    MainWindow mw;
    mw.show();
    return app.exec();
}
</db:programlisting>
</db:section>
<db:section>
<db:title>Example project</db:title>
<db:para><db:link xlink:href="https://code.qt.io/cgit/qt/qtbase.git/tree/examples/widgets/itemviews/addressbook?h=5.13">Example project @ code.qt.io</db:link></db:para>
</db:section>
</db:article>
