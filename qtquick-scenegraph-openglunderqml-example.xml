<?xml version="1.0" encoding="UTF-8"?>
<db:article xmlns:db="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink" version="5.2" xml:lang="en_US">
<db:info>
<db:title>Scene Graph - OpenGL Under QML</db:title>
<db:productname>QtQuick</db:productname>
<db:edition>Qt 5.2.1 Reference Documentation</db:edition>
<db:titleabbrev>Qt Quick Reference Documentation</db:titleabbrev>
<db:abstract>
<db:para>Shows how to render OpenGL under a Qt Quick scene.</db:para>
</db:abstract>
<db:pubdate>2025-06-29</db:pubdate>
<db:date>2025-06-29</db:date>
<db:authorgroup>
<db:author>
<db:orgname class="corporation">The Qt Company Ltd.</db:orgname>
</db:author>
</db:authorgroup>
</db:info>
<db:mediaobject>
<db:imageobject>
<db:imagedata fileref="images/openglunderqml-example.jpg"/>
</db:imageobject>
</db:mediaobject>
<db:para>The OpenGL under QML example shows how an application can make use of the <db:link xlink:href="">QQuickWindow::beforeRendering()</db:link> signal to draw custom OpenGL content under a Qt Quick scene. This signal is emitted at the start of every frame, before the scene graph starts its rendering, thus any OpenGL draw calls that are made as a response to this signal, will stack under the Qt Quick items.</db:para>
<db:para>As an alternative, applications that wish to render OpenGL content on top of the Qt Quick scene, can do so by connecting to the <db:link xlink:href="">QQuickWindow::afterRendering()</db:link> signal.</db:para>
<db:para>In this example, we will also see how it is possible to have values that are exposed to QML which affect the OpenGL rendering. We animate the threshold value using a <db:link xlink:href="qml-qtquick-numberanimation.xml">NumberAnimation</db:link> in the QML file and this value is used by the OpenGL shader program that draws the squircles.</db:para>
<db:programlisting language="cpp">class Squircle : public QQuickItem
{
    Q_OBJECT

    Q_PROPERTY(qreal t READ t WRITE setT NOTIFY tChanged)

public:
    Squircle();

    qreal t() const { return m_t; }
    void setT(qreal t);

signals:
    void tChanged();

public slots:
    void paint();
    void cleanup();
    void sync();

private slots:
    void handleWindowChanged(QQuickWindow *win);

private:
    QOpenGLShaderProgram *m_program;

    qreal m_t;
    qreal m_thread_t;
};
</db:programlisting>
<db:para>First of all, we need a <db:link xlink:href="qobject.xml">QObject</db:link> with a slot to connect the signals to. We subclass QQuickItem in order to use the <db:link xlink:href="">QQuickItem::window()</db:link> which holds the window instance we want to connect to.</db:para>
<db:para>We use two values of <db:code>t</db:code>. The variable <db:code>m_t</db:code> is the property value as it exists in the GUI thread. The <db:code>m_thread_t</db:code> value is a copy of <db:code>m_t</db:code> for use in the rendering thread. We need an explicit copy because the scene graph can render in one thread while updating properties on the GUI thread in preparation for the next frame. If we had used only one value, the animation could have updated the value to that of the next frame before we got a chance to render it.</db:para>
<db:note>
<db:para>In this example, a wrong value for <db:code>t</db:code> will have minimal consequences, but we emphasize that rendering and GUI thread objects and values must stay separate to avoid race conditions, undesired behavior and in the worst case, crashes.</db:para>
</db:note>
<db:para>Lets move on to the implementation.</db:para>
<db:programlisting language="cpp">Squircle::Squircle()
    : m_program(0)
    , m_t(0)
    , m_thread_t(0)
{
    connect(this, SIGNAL(windowChanged(QQuickWindow*)), this, SLOT(handleWindowChanged(QQuickWindow*)));
}
</db:programlisting>
<db:para>The constructor of the <db:code>Squircle</db:code> class simply initializes the values. The shader program will be initialized during rendering later.</db:para>
<db:programlisting language="cpp">void Squircle::setT(qreal t)
{
    if (t == m_t)
        return;
    m_t = t;
    emit tChanged();
    if (window())
        window()-&gt;update();
}
</db:programlisting>
<db:para>The property setter checks that the value has indeed changed before updating its internal variable. It then calls <db:link xlink:href="">QQuickWindow::update()</db:link> which will trigger another frame to be rendered. Note that the setter might be called during initialization, before the object has been entered into the scene and before it has a window.</db:para>
<db:programlisting language="cpp">void Squircle::handleWindowChanged(QQuickWindow *win)
{
    if (win) {
        connect(win, SIGNAL(beforeRendering()), this, SLOT(paint()), Qt::DirectConnection);
        connect(win, SIGNAL(beforeSynchronizing()), this, SLOT(sync()), Qt::DirectConnection);
</db:programlisting>
<db:para>For our paint function to be called, we need to connect to the window's signals. When Squircle object is populated into the scene, the windowChanged signal is emitted. In our handler, we connect <db:link xlink:href="">QQuickWindow::beforeRendering()</db:link> to <db:code>paint()</db:code> to do the rendering, and <db:link xlink:href="">QQuickWindow::beforeSynchronizing()</db:link> to <db:code>sync()</db:code> to copy the state of the <db:code>t</db:code> property for the upcoming frame.</db:para>
<db:note>
<db:para>Since the Squircle object has affinity to the GUI thread and the signals are emitted from the rendering thread, it is crucial that the connections are made with <db:link xlink:href="qt.xml#ConnectionType-enum">Qt::DirectConnection</db:link>. Failing to do so, will result in that the slots are invoked on the wrong thread with no OpenGL context present.</db:para>
</db:note>
<db:programlisting language="cpp">        win-&gt;setClearBeforeRendering(false);
    }
}

</db:programlisting>
<db:para>The default behavior of the scene graph is to clear the framebuffer before rendering. Since we render before the scene graph, we need to turn this clearing off. This means that we need to clear ourselves in the <db:code>paint()</db:code> function.</db:para>
<db:programlisting language="cpp">void Squircle::paint()
{
    if (!m_program) {
        m_program = new QOpenGLShaderProgram();
        m_program-&gt;addShaderFromSourceCode(QOpenGLShader::Vertex,
                                           &quot;attribute highp vec4 vertices;&quot;
                                           &quot;varying highp vec2 coords;&quot;
                                           &quot;void main() {&quot;
                                           &quot;    gl_Position = vertices;&quot;
                                           &quot;    coords = vertices.xy;&quot;
                                           &quot;}&quot;);
        m_program-&gt;addShaderFromSourceCode(QOpenGLShader::Fragment,
                                           &quot;uniform lowp float t;&quot;
                                           &quot;varying highp vec2 coords;&quot;
                                           &quot;void main() {&quot;
                                           &quot;    lowp float i = 1. - (pow(abs(coords.x), 4.) + pow(abs(coords.y), 4.));&quot;
                                           &quot;    i = smoothstep(t - 0.8, t + 0.8, i);&quot;
                                           &quot;    i = floor(i * 20.) / 20.;&quot;
                                           &quot;    gl_FragColor = vec4(coords * .5 + .5, i, i);&quot;
                                           &quot;}&quot;);

        m_program-&gt;bindAttributeLocation(&quot;vertices&quot;, 0);
        m_program-&gt;link();

        connect(window()-&gt;openglContext(), SIGNAL(aboutToBeDestroyed()),
                this, SLOT(cleanup()), Qt::DirectConnection);
    }
</db:programlisting>
<db:para>The first thing we do in the <db:code>paint()</db:code> function is to initialize the shader program. By initializing the shader program here, we make sure that the OpenGL context is bound and that we are on the correct thread.</db:para>
<db:para>We also connect to the QOpenGLContext::aboutToBeDestroyed() signal, so that we can clean up the shader program when the context is destroyed. Again, this is a <db:link xlink:href="qt.xml#ConnectionType-enum">Qt::DirectConnection</db:link> as all rendering related operations must happen on the rendering thread.</db:para>
<db:programlisting language="cpp">    m_program-&gt;bind();

    m_program-&gt;enableAttributeArray(0);

    float values[] = {
        -1, -1,
        1, -1,
        -1, 1,
        1, 1
    };
    m_program-&gt;setAttributeArray(0, GL_FLOAT, values, 2);
    m_program-&gt;setUniformValue(&quot;t&quot;, (float) m_thread_t);

    qreal ratio = window()-&gt;devicePixelRatio();
    int w = int(ratio * window()-&gt;width());
    int h = int(ratio * window()-&gt;height());
    glViewport(0, 0, w, h);

    glDisable(GL_DEPTH_TEST);

    glClearColor(0, 0, 0, 1);
    glClear(GL_COLOR_BUFFER_BIT);

    glEnable(GL_BLEND);
    glBlendFunc(GL_SRC_ALPHA, GL_ONE);

    glDrawArrays(GL_TRIANGLE_STRIP, 0, 4);

    m_program-&gt;disableAttributeArray(0);
    m_program-&gt;release();
}
</db:programlisting>
<db:para>We use the shader program to draw the squircle. At the end of the <db:code>paint</db:code> function we release the program and disable the attributes we used so that the OpenGL context is in a &quot;clean&quot; state for the scene graph to pick it up.</db:para>
<db:programlisting language="cpp">void Squircle::cleanup()
{
    if (m_program) {
        delete m_program;
        m_program = 0;
    }
}
</db:programlisting>
<db:para>In the <db:code>cleanup()</db:code> function we delete the program.</db:para>
<db:programlisting language="cpp">void Squircle::sync()
{
    m_thread_t = m_t;
}
</db:programlisting>
<db:para>We use the <db:code>sync()</db:code> function to copy the state of the object in the GUI thread into the rendering thread.</db:para>
<db:para>The signal is emitted on the rendering thread while the GUI thread is blocked, so it is safe to simply copy the value without any additional protection.</db:para>
<db:programlisting language="cpp">int main(int argc, char **argv)
{
    QGuiApplication app(argc, argv);

    qmlRegisterType&lt;Squircle&gt;(&quot;OpenGLUnderQML&quot;, 1, 0, &quot;Squircle&quot;);

    QQuickView view;
    view.setResizeMode(QQuickView::SizeRootObjectToView);
    view.setSource(QUrl(&quot;qrc:///scenegraph/openglunderqml/main.qml&quot;));
    view.show();

    return app.exec();
}
</db:programlisting>
<db:para>The application's <db:code>main()</db:code> function instantiates a QQuickView and launches the <db:code>main.qml</db:code> file. The only thing worth noting is that we export the <db:code>Squircle</db:code> class to QML using the <db:link xlink:href="">qmlRegisterType()</db:link> macro.</db:para>
<db:programlisting language="qml">import QtQuick 2.0
import OpenGLUnderQML 1.0

Item {

    width: 320
    height: 480

    Squircle {
        SequentialAnimation on t {
            NumberAnimation { to: 1; duration: 2500; easing.type: Easing.InQuad }
            NumberAnimation { to: 0; duration: 2500; easing.type: Easing.OutQuad }
            loops: Animation.Infinite
            running: true
        }
    }
</db:programlisting>
<db:para>We import the Squircle QML type with the name we registered in the <db:code>main()</db:code> function. We then instantiate it and create a running <db:link xlink:href="qml-qtquick-numberanimation.xml">NumberAnimation</db:link> on its <db:code>t</db:code> property.</db:para>
<db:programlisting language="qml">    Rectangle {
        color: Qt.rgba(1, 1, 1, 0.7)
        radius: 10
        border.width: 1
        border.color: &quot;white&quot;
        anchors.fill: label
        anchors.margins: -10
    }

    Text {
        id: label
        color: &quot;black&quot;
        wrapMode: Text.WordWrap
        text: &quot;The background here is a squircle rendered with raw OpenGL using the 'beforeRender()' signal in QQuickWindow. This text label and its border is rendered using QML&quot;
        anchors.right: parent.right
        anchors.left: parent.left
        anchors.bottom: parent.bottom
        anchors.margins: 20
    }
}
</db:programlisting>
<db:para>Then we overlay a short descriptive text, so that it is clearly visible that we are in fact rendering OpenGL under our Qt Quick scene.</db:para>
<db:section>
<db:title>List of Files</db:title>
<db:para>Files:</db:para>
<db:section>
<db:title>List of Files</db:title>
<db:itemizedlist>
<db:listitem>
<db:para><db:link xlink:href="scenegraph/openglunderqml/main.cpp">scenegraph/openglunderqml/main.cpp</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="scenegraph/openglunderqml/main.qml">scenegraph/openglunderqml/main.qml</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="scenegraph/openglunderqml/openglunderqml.pro">scenegraph/openglunderqml/openglunderqml.pro</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="scenegraph/openglunderqml/openglunderqml.qrc">scenegraph/openglunderqml/openglunderqml.qrc</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="scenegraph/openglunderqml/squircle.cpp">scenegraph/openglunderqml/squircle.cpp</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="scenegraph/openglunderqml/squircle.h">scenegraph/openglunderqml/squircle.h</db:link></db:para></db:listitem>
</db:itemizedlist>
</db:section>
</db:section></db:article>
