<?xml version="1.0" encoding="UTF-8"?>
<db:article xmlns:db="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink" version="5.2" xml:lang="en">
<db:info>
<db:title>Ping Pong States Example</db:title>
<db:productname>QtWidgets</db:productname>
<db:edition>Qt 5.7.1 Reference Documentation</db:edition>
<db:titleabbrev>Qt Widgets Reference Documentation</db:titleabbrev>
<db:abstract>
<db:para>The Ping Pong States example shows how to use parallel states together with custom events and transitions in <db:link xlink:href="statemachine-api.xml">The State Machine Framework</db:link>.</db:para>
</db:abstract>
<db:pubdate>2025-06-29</db:pubdate>
<db:date>2025-06-29</db:date>
<db:authorgroup>
<db:author>
<db:orgname class="corporation">The Qt Company Ltd.</db:orgname>
</db:author>
</db:authorgroup>
</db:info>
<db:para>This example implements a statechart where two states communicate by posting events to the state machine. The state chart looks as follows:</db:para>
<db:mediaobject>
<db:imageobject>
<db:imagedata fileref="images/pingpong-example.png"/>
</db:imageobject>
</db:mediaobject>
<db:para>The pinger and ponger states are parallel states, i.e. they are entered simultaneously and will take transitions independently of eachother.</db:para>
<db:para>The pinger state will post the first ping event upon entry; the ponger state will respond by posting a pong event; this will cause the pinger state to post a new ping event; and so on.</db:para>
<db:programlisting language="cpp">class PingEvent : public QEvent
{
public:
    PingEvent() : QEvent(QEvent::Type(QEvent::User+2))
        {}
};

class PongEvent : public QEvent
{
public:
    PongEvent() : QEvent(QEvent::Type(QEvent::User+3))
        {}
};
</db:programlisting>
<db:para>Two custom events are defined, PingEvent and PongEvent.</db:para>
<db:programlisting language="cpp">class Pinger : public QState
{
public:
    Pinger(QState *parent)
        : QState(parent) {}

protected:
    virtual void onEntry(QEvent *) Q_DECL_OVERRIDE
    {
        machine()-&gt;postEvent(new PingEvent());
        fprintf(stdout, &quot;ping?\n&quot;);
    }
};
</db:programlisting>
<db:para>The Pinger class defines a state that posts a PingEvent to the state machine when the state is entered.</db:para>
<db:programlisting language="cpp">class PingTransition : public QAbstractTransition
{
public:
    PingTransition() {}

protected:
    virtual bool eventTest(QEvent *e) Q_DECL_OVERRIDE {
        return (e-&gt;type() == QEvent::User+2);
    }
    virtual void onTransition(QEvent *) Q_DECL_OVERRIDE
    {
        machine()-&gt;postDelayedEvent(new PongEvent(), 500);
        fprintf(stdout, &quot;pong!\n&quot;);
    }
};
</db:programlisting>
<db:para>The PingTransition class defines a transition that is triggered by events of type PingEvent, and that posts a PongEvent (with a delay of 500 milliseconds) to the state machine when the transition is triggered.</db:para>
<db:programlisting language="cpp">class PongTransition : public QAbstractTransition
{
public:
    PongTransition() {}

protected:
    virtual bool eventTest(QEvent *e) Q_DECL_OVERRIDE {
        return (e-&gt;type() == QEvent::User+3);
    }
    virtual void onTransition(QEvent *) Q_DECL_OVERRIDE
    {
        machine()-&gt;postDelayedEvent(new PingEvent(), 500);
        fprintf(stdout, &quot;ping?\n&quot;);
    }
};
</db:programlisting>
<db:para>The PongTransition class defines a transition that is triggered by events of type PongEvent, and that posts a PingEvent (with a delay of 500 milliseconds) to the state machine when the transition is triggered.</db:para>
<db:programlisting language="cpp">int main(int argc, char **argv)
{
    QCoreApplication app(argc, argv);

    QStateMachine machine;
    QState *group = new QState(QState::ParallelStates);
    group-&gt;setObjectName(&quot;group&quot;);
</db:programlisting>
<db:para>The main() function begins by creating a state machine and a parallel state group.</db:para>
<db:programlisting language="cpp">    Pinger *pinger = new Pinger(group);
    pinger-&gt;setObjectName(&quot;pinger&quot;);
    pinger-&gt;addTransition(new PongTransition());

    QState *ponger = new QState(group);
    ponger-&gt;setObjectName(&quot;ponger&quot;);
    ponger-&gt;addTransition(new PingTransition());
</db:programlisting>
<db:para>Next, the pinger and ponger states are created, with the parallel state group as their parent state. Note that the transitions are <db:emphasis>targetless</db:emphasis>. When such a transition is triggered, the source state won't be exited and re-entered; only the transition's onTransition() function will be called, and the state machine's configuration will remain the same, which is precisely what we want in this case.</db:para>
<db:programlisting language="cpp">    machine.addState(group);
    machine.setInitialState(group);
    machine.start();

    return app.exec();
}
</db:programlisting>
<db:para>Finally, the group is added to the state machine, the machine is started, and the application event loop is entered.</db:para>
<db:section>
<db:title>List of Files</db:title>
<db:para>Files:</db:para>
<db:section>
<db:title>List of Files</db:title>
<db:itemizedlist>
<db:listitem>
<db:para><db:link xlink:href="statemachine/pingpong/main.cpp">statemachine/pingpong/main.cpp</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="statemachine/pingpong/pingpong.pro">statemachine/pingpong/pingpong.pro</db:link></db:para></db:listitem>
</db:itemizedlist>
</db:section>
</db:section></db:article>
