<?xml version="1.0" encoding="UTF-8"?>
<db:article xmlns:db="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink" version="5.2" xml:lang="en">
<db:info>
<db:title>Nested Donuts Example</db:title>
<db:productname>QtCharts</db:productname>
<db:edition>Qt 5.7.1 Reference Documentation</db:edition>
<db:titleabbrev>Qt Charts Reference Documentation</db:titleabbrev>
<db:abstract>
<db:para>This example shows how to create a nested donuts chart using the QPieSeries API.</db:para>
</db:abstract>
<db:pubdate>2025-06-29</db:pubdate>
<db:date>2025-06-29</db:date>
<db:authorgroup>
<db:author>
<db:orgname class="corporation">The Qt Company Ltd.</db:orgname>
</db:author>
</db:authorgroup>
</db:info>
<db:mediaobject>
<db:imageobject>
<db:imagedata fileref="images/examples_nesteddonuts.png"/>
</db:imageobject>
</db:mediaobject>
<db:para>Let's start by creating a QChartView instance and enabling the Antialiasing on it. A QChart object is then obtained from the QChartView instance. The legend is disabled and the title of the chart is set. Last line enables the animations of the chart.</db:para>
<db:programlisting language="cpp">QChartView *chartView = new QChartView;
chartView-&gt;setRenderHint(QPainter::Antialiasing);
QChart *chart = chartView-&gt;chart();
chart-&gt;legend()-&gt;setVisible(false);
chart-&gt;setTitle(&quot;Nested donuts demo&quot;);
chart-&gt;setAnimationOptions(QChart::AllAnimations);
</db:programlisting>
<db:para>Three variables are defined that will be used to define the donut chart. Min and max size define the relative size of the whole donut. minSize is the relative inner size of the smallest donut. maxSize is the relative outer size of the biggest donut.</db:para>
<db:programlisting language="cpp">qreal minSize = 0.1;
qreal maxSize = 0.9;
int donutCount = 5;
</db:programlisting>
<db:para>The following block of code defines the individual donuts and their slices. First a new QPieSeries object is created. The number of slices in each donut is randomized. The internal for loop creates the slices with a random value and label same as the value. Next the label of the slice is set to be visible and its color is set to white. To make the example more interesting the hovered signal of the slice is connected to widget's slot, of which the inner workings are explained later. Finally the slice is added to the donut. The donut's size is adjusted to achieve the nesting of the donuts. Then the donut is added to the widget's list of donuts and to the chart.</db:para>
<db:programlisting language="cpp">for (int i = 0; i &lt; donutCount; i++) {
    QPieSeries *donut = new QPieSeries;
    int sliceCount =  3 + qrand() % 3;
    for (int j = 0; j &lt; sliceCount; j++) {
        qreal value = 100 + qrand() % 100;
        QPieSlice *slice = new QPieSlice(QString(&quot;%1&quot;).arg(value), value);
        slice-&gt;setLabelVisible(true);
        slice-&gt;setLabelColor(Qt::white);
        slice-&gt;setLabelPosition(QPieSlice::LabelInsideTangential);
        connect(slice, SIGNAL(hovered(bool)), this, SLOT(explodeSlice(bool)));
        donut-&gt;append(slice);
        donut-&gt;setHoleSize(minSize + i * (maxSize - minSize) / donutCount);
        donut-&gt;setPieSize(minSize + (i + 1) * (maxSize - minSize) / donutCount);
    }
    m_donuts.append(donut);
    chartView-&gt;chart()-&gt;addSeries(donut);
}
</db:programlisting>
<db:para>Finally the widget is placed in a layout used by the application.</db:para>
<db:programlisting language="cpp">QGridLayout *mainLayout = new QGridLayout;
mainLayout-&gt;addWidget(chartView, 1, 1);
setLayout(mainLayout);
</db:programlisting>
<db:para>To make the example more interesting the donuts are rotated randomly every 1.25 sec.</db:para>
<db:programlisting language="cpp">updateTimer = new QTimer(this);
connect(updateTimer, SIGNAL(timeout()), this, SLOT(updateRotation()));
updateTimer-&gt;start(1250);
</db:programlisting>
<db:para>The widget's updatedRotation slot is defined below. It goes through all of the donuts and modifies their current rotation by a random value.</db:para>
<db:programlisting language="cpp">void Widget::updateRotation()
{
    for (int i = 0; i &lt; m_donuts.count(); i++) {
        QPieSeries *donut = m_donuts.at(i);
        qreal phaseShift =  -50 + qrand() % 100;
        donut-&gt;setPieStartAngle(donut-&gt;pieStartAngle() + phaseShift);
        donut-&gt;setPieEndAngle(donut-&gt;pieEndAngle() + phaseShift);
    }
}
</db:programlisting>
<db:para>The earlier mentioned explodeSlice slot code is provided below. If the slice is set to exploded, then stop the timer that controls the donuts rotation. Then the slice's start and end angles are obtained from the slice. To highlight the selected slice all the other donuts that lie outward from the one that contains the selected slice have their start and end angles modified so that they wouldn't &quot;block&quot; the way for the hightlighted slice. If the slice is no longer selected return to the original state.</db:para>
<db:programlisting language="cpp">void Widget::explodeSlice(bool exploded)
{
    QPieSlice *slice = qobject_cast&lt;QPieSlice *&gt;(sender());
    if (exploded) {
        updateTimer-&gt;stop();
        qreal sliceStartAngle = slice-&gt;startAngle();
        qreal sliceEndAngle = slice-&gt;startAngle() + slice-&gt;angleSpan();

        QPieSeries *donut = slice-&gt;series();
        qreal seriesIndex = m_donuts.indexOf(donut);
        for (int i = seriesIndex + 1; i &lt; m_donuts.count(); i++) {
            m_donuts.at(i)-&gt;setPieStartAngle(sliceEndAngle);
            m_donuts.at(i)-&gt;setPieEndAngle(360 + sliceStartAngle);
        }
    } else {
        for (int i = 0; i &lt; m_donuts.count(); i++) {
            m_donuts.at(i)-&gt;setPieStartAngle(0);
            m_donuts.at(i)-&gt;setPieEndAngle(360);
        }
        updateTimer-&gt;start();
    }
    slice-&gt;setExploded(exploded);
}
</db:programlisting>
<db:section>
<db:title>List of Files</db:title>
<db:para>Files:</db:para>
<db:section>
<db:title>List of Files</db:title>
<db:itemizedlist>
<db:listitem>
<db:para><db:link xlink:href="nesteddonuts/main.cpp">nesteddonuts/main.cpp</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="nesteddonuts/nesteddonuts.pro">nesteddonuts/nesteddonuts.pro</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="nesteddonuts/widget.cpp">nesteddonuts/widget.cpp</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="nesteddonuts/widget.h">nesteddonuts/widget.h</db:link></db:para></db:listitem>
</db:itemizedlist>
</db:section>
</db:section></db:article>
