<?xml version="1.0" encoding="UTF-8"?>
<db:article xmlns:db="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink" version="5.2" xml:lang="en_US">
<db:info>
<db:title>Tablet Example</db:title>
<db:productname>QtWidgets</db:productname>
<db:edition>Qt 5.3.2 Reference Documentation</db:edition>
<db:titleabbrev>Qt Widgets Reference Documentation</db:titleabbrev>
<db:abstract>
<db:para>This example shows how to use a Wacom tablet in Qt applications.</db:para>
</db:abstract>
<db:pubdate>2025-06-29</db:pubdate>
<db:date>2025-06-29</db:date>
<db:authorgroup>
<db:author>
<db:orgname class="corporation">The Qt Company Ltd.</db:orgname>
</db:author>
</db:authorgroup>
</db:info>
<db:mediaobject>
<db:imageobject>
<db:imagedata fileref="images/tabletexample.png"/>
</db:imageobject>
</db:mediaobject>
<db:para>When you use a tablet with Qt applications, <db:link xlink:href="qtabletevent.xml">QTabletEvent</db:link>s are generated. You need to reimplement the <db:link xlink:href="qwidget.xml#tabletEvent">tabletEvent</db:link>() event handler if you want to handle tablet events. Events are generated when the device used for drawing enters and leaves the proximity of the tablet (i.e., when it is close but not pressed down on it), when a device is pushed down and released from it, and when a device is moved on the tablet.</db:para>
<db:para>The information available in <db:link xlink:href="qtabletevent.xml">QTabletEvent</db:link> depends on the device used. The tablet in this example has two different devices for drawing: a stylus and an airbrush. For both devices the event contains the position of the device, pressure on the tablet, vertical tilt, and horizontal tilt (i.e, the angle between the device and the perpendicular of the tablet). The airbrush has a finger wheel; the position of this is also available in the tablet event.</db:para>
<db:para>In this example we implement a drawing program. You can use the stylus to draw on the tablet as you use a pencil on paper. When you draw with the airbrush you get a spray of paint; the finger wheel is used to change the density of the spray. The pressure and tilt can change the alpha and saturation values of the <db:link xlink:href="qcolor.xml">QColor</db:link> and the width of the <db:link xlink:href="qpen.xml">QPen</db:link> used for drawing.</db:para>
<db:para>The example consists of the following:</db:para>
<db:itemizedlist>
<db:listitem>
<db:para>The <db:code>MainWindow</db:code> class inherits <db:link xlink:href="qmainwindow.xml">QMainWindow</db:link> and creates the examples menus and connect their slots and signals.</db:para>
</db:listitem>
<db:listitem>
<db:para>The <db:code>TabletCanvas</db:code> class inherits <db:link xlink:href="qwidget.xml">QWidget</db:link> and receives tablet events. It uses the events to paint on a offscreen pixmap, which it draws onto itself.</db:para>
</db:listitem>
<db:listitem>
<db:para>The <db:code>TabletApplication</db:code> class inherits <db:link xlink:href="qapplication.xml">QApplication</db:link>. This class handles tablet events that are not sent to <db:code>tabletEvent()</db:code>. We will look at this later.</db:para>
</db:listitem>
<db:listitem>
<db:para>The <db:code>main()</db:code> function creates a <db:code>MainWindow</db:code> and shows it as a top level window.</db:para>
</db:listitem>
</db:itemizedlist>
<db:section xml:id="mainwindow-class-definition">
<db:title>MainWindow Class Definition</db:title>
<db:para>The <db:code>MainWindow</db:code> creates a <db:code>TabletCanvas</db:code> and sets it as its center widget.</db:para>
<db:programlisting language="cpp">class MainWindow : public QMainWindow
{
    Q_OBJECT

public:
    MainWindow(TabletCanvas *canvas);

private slots:
    void brushColorAct();
    void alphaActionTriggered(QAction *action);
    void lineWidthActionTriggered(QAction *action);
    void saturationActionTriggered(QAction *action);
    void saveAct();
    void loadAct();
    void aboutAct();

private:
    void createActions();
    void createMenus();

    TabletCanvas *myCanvas;

    QAction *brushColorAction;
    QActionGroup *brushActionGroup;

    QActionGroup *alphaChannelGroup;
    QAction *alphaChannelPressureAction;
    QAction *alphaChannelTiltAction;
    QAction *noAlphaChannelAction;

    QActionGroup *colorSaturationGroup;
    QAction *colorSaturationVTiltAction;
    QAction *colorSaturationHTiltAction;
    QAction *colorSaturationPressureAction;
    QAction *noColorSaturationAction;

    QActionGroup *lineWidthGroup;
    QAction *lineWidthPressureAction;
    QAction *lineWidthTiltAction;
    QAction *lineWidthFixedAction;

    QAction *exitAction;
    QAction *saveAction;
    QAction *loadAction;

    QAction *aboutAction;
    QAction *aboutQtAction;

    QMenu *fileMenu;
    QMenu *brushMenu;
    QMenu *tabletMenu;
    QMenu *helpMenu;
    QMenu *colorSaturationMenu;
    QMenu *lineWidthMenu;
    QMenu *alphaChannelMenu;
};
</db:programlisting>
<db:para>The QActions let the user select if the tablets pressure and tilt should change the pen width, color alpha component and color saturation. <db:code>createActions()</db:code> creates all actions, and <db:code>createMenus()</db:code> sets up the menus with the actions. We have one <db:link xlink:href="qactiongroup.xml">QActionGroup</db:link> for the actions that alter the alpha channel, color saturation and line width respectively. The action groups are connected to the <db:code>alphaActionTriggered()</db:code>, <db:code>colorSaturationActiontriggered()</db:code>, and <db:code>lineWidthActionTriggered()</db:code> slots, which calls functions in <db:code>myCanvas</db:code>.</db:para>
</db:section>
<db:section xml:id="mainwindow-class-implementation">
<db:title>MainWindow Class Implementation</db:title>
<db:para>We start width a look at the constructor <db:code>MainWindow()</db:code>:</db:para>
<db:programlisting language="cpp">MainWindow::MainWindow(TabletCanvas *canvas)
{
    myCanvas = canvas;
    createActions();
    createMenus();

    myCanvas-&gt;setColor(Qt::red);
    myCanvas-&gt;setLineWidthType(TabletCanvas::LineWidthPressure);
    myCanvas-&gt;setAlphaChannelType(TabletCanvas::NoAlpha);
    myCanvas-&gt;setColorSaturationType(TabletCanvas::NoSaturation);

    setWindowTitle(tr(&quot;Tablet Example&quot;));
    setCentralWidget(myCanvas);
}
</db:programlisting>
<db:para>In the constructor we create the canvas, actions, and menus. We set the canvas as the center widget. We also initialize the canvas to match the state of our menus and start drawing with a red color.</db:para>
<db:para>Here is the implementation of <db:code>brushColorAct()</db:code>:</db:para>
<db:programlisting language="cpp">void MainWindow::brushColorAct()
{
    QColor color = QColorDialog::getColor(myCanvas-&gt;color());

    if (color.isValid())
        myCanvas-&gt;setColor(color);
}
</db:programlisting>
<db:para>We let the user pick a color with a <db:link xlink:href="qcolordialog.xml">QColorDialog</db:link>. If it is valid, we set a new drawing color with <db:code>setColor()</db:code>.</db:para>
<db:para>Here is the implementation of <db:code>alphaActionTriggered()</db:code>:</db:para>
<db:programlisting language="cpp">void MainWindow::alphaActionTriggered(QAction *action)
{
    if (action == alphaChannelPressureAction) {
        myCanvas-&gt;setAlphaChannelType(TabletCanvas::AlphaPressure);
    } else if (action == alphaChannelTiltAction) {
        myCanvas-&gt;setAlphaChannelType(TabletCanvas::AlphaTilt);
    } else {
        myCanvas-&gt;setAlphaChannelType(TabletCanvas::NoAlpha);
    }
}
</db:programlisting>
<db:para>The <db:code>TabletCanvas</db:code> class supports two ways by which the alpha channel of the drawing color can be changed: tablet pressure and tilt. We have one action for each and an action if the alpha channel should not be changed.</db:para>
<db:para>Here is the implementation of <db:code>lineWidthActionTriggered()</db:code>:</db:para>
<db:programlisting language="cpp">void MainWindow::lineWidthActionTriggered(QAction *action)
{
    if (action == lineWidthPressureAction) {
        myCanvas-&gt;setLineWidthType(TabletCanvas::LineWidthPressure);
    } else if (action == lineWidthTiltAction) {
        myCanvas-&gt;setLineWidthType(TabletCanvas::LineWidthTilt);
    } else {
        myCanvas-&gt;setLineWidthType(TabletCanvas::NoLineWidth);
    }
}
</db:programlisting>
<db:para>We check which action is selected in <db:code>lineWidthGroup</db:code>, and set how the canvas should change the drawing line width.</db:para>
<db:para>Here is the implementation of <db:code>saturationActionTriggered()</db:code>:</db:para>
<db:programlisting language="cpp">void MainWindow::saturationActionTriggered(QAction *action)
{
    if (action == colorSaturationVTiltAction) {
        myCanvas-&gt;setColorSaturationType(TabletCanvas::SaturationVTilt);
    } else if (action == colorSaturationHTiltAction) {
        myCanvas-&gt;setColorSaturationType(TabletCanvas::SaturationHTilt);
    } else if (action == colorSaturationPressureAction) {
        myCanvas-&gt;setColorSaturationType(TabletCanvas::SaturationPressure);
    } else {
        myCanvas-&gt;setColorSaturationType(TabletCanvas::NoSaturation);
    }
}
</db:programlisting>
<db:para>We check which action is selected in <db:code>colorSaturationGroup</db:code>, and set how the canvas should change the color saturation of the drawing color.</db:para>
<db:para>Here is the implementation of <db:code>saveAct()</db:code>:</db:para>
<db:programlisting language="cpp">void MainWindow::saveAct()
{
    QString path = QDir::currentPath() + &quot;/untitled.png&quot;;
    QString fileName = QFileDialog::getSaveFileName(this, tr(&quot;Save Picture&quot;),
                             path);

    if (!myCanvas-&gt;saveImage(fileName))
        QMessageBox::information(this, &quot;Error Saving Picture&quot;,
                                 &quot;Could not save the image&quot;);
}
</db:programlisting>
<db:para>We use the <db:link xlink:href="qfiledialog.xml">QFileDialog</db:link> to let the user select a file to save the drawing in. It is the <db:code>TabletCanvas</db:code> that save the drawing, so we call its <db:code>saveImage()</db:code> function.</db:para>
<db:para>Here is the implementation of <db:code>loadAct()</db:code>:</db:para>
<db:programlisting language="cpp">void MainWindow::loadAct()
{
    QString fileName = QFileDialog::getOpenFileName(this, tr(&quot;Open Picture&quot;),
                                                    QDir::currentPath());

    if (!myCanvas-&gt;loadImage(fileName))
        QMessageBox::information(this, &quot;Error Opening Picture&quot;,
                                 &quot;Could not open picture&quot;);
}
</db:programlisting>
<db:para>We let the user select the image file to be opened with a <db:link xlink:href="qfiledialog.xml">QFileDialog</db:link>; we then ask the canvas to load the image with <db:code>loadImage()</db:code>.</db:para>
<db:para>Here is the implementation of <db:code>aboutAct()</db:code>:</db:para>
<db:programlisting language="cpp">void MainWindow::aboutAct()
{
    QMessageBox::about(this, tr(&quot;About Tablet Example&quot;),
                       tr(&quot;This example shows use of a Wacom tablet in Qt&quot;));
}
</db:programlisting>
<db:para>We show a message box with a short description of the example.</db:para>
<db:para><db:code>createActions()</db:code> creates all actions and action groups of the example. We look at the creation of one action group and its actions. See the <db:link xlink:href="qtwidgets-mainwindows-application-example.xml">application example</db:link> if you want a high-level introduction to QActions.</db:para>
<db:para>Here is the implementation of <db:code>createActions</db:code>:</db:para>
<db:programlisting language="cpp">void MainWindow::createActions()
{
    ...
    alphaChannelPressureAction = new QAction(tr(&quot;&amp;Pressure&quot;), this);
    alphaChannelPressureAction-&gt;setCheckable(true);

    alphaChannelTiltAction = new QAction(tr(&quot;&amp;Tilt&quot;), this);
    alphaChannelTiltAction-&gt;setCheckable(true);

    noAlphaChannelAction = new QAction(tr(&quot;No Alpha Channel&quot;), this);
    noAlphaChannelAction-&gt;setCheckable(true);
    noAlphaChannelAction-&gt;setChecked(true);

    alphaChannelGroup = new QActionGroup(this);
    alphaChannelGroup-&gt;addAction(alphaChannelPressureAction);
    alphaChannelGroup-&gt;addAction(alphaChannelTiltAction);
    alphaChannelGroup-&gt;addAction(noAlphaChannelAction);
    connect(alphaChannelGroup, SIGNAL(triggered(QAction*)),
            this, SLOT(alphaActionTriggered(QAction*)));

</db:programlisting>
<db:para>We want the user to be able to choose if the drawing color's alpha component should be changed by the tablet pressure or tilt. We have one action for each choice and an action if the alpha channel is not to be changed, i.e, the color is opaque. We make the actions checkable; the <db:code>alphaChannelGroup</db:code> will then ensure that only one of the actions are checked at any time. The <db:code>triggered()</db:code> signal is emitted when an action is checked.</db:para>
<db:programlisting language="cpp">    ...
}
</db:programlisting>
<db:para>Here is the implementation of <db:code>createMenus()</db:code>:</db:para>
<db:programlisting language="cpp">void MainWindow::createMenus()
{
    fileMenu = menuBar()-&gt;addMenu(tr(&quot;&amp;File&quot;));
    fileMenu-&gt;addAction(loadAction);
    fileMenu-&gt;addAction(saveAction);
    fileMenu-&gt;addSeparator();
    fileMenu-&gt;addAction(exitAction);

    brushMenu = menuBar()-&gt;addMenu(tr(&quot;&amp;Brush&quot;));
    brushMenu-&gt;addAction(brushColorAction);

    tabletMenu = menuBar()-&gt;addMenu(tr(&quot;&amp;Tablet&quot;));

    lineWidthMenu = tabletMenu-&gt;addMenu(tr(&quot;&amp;Line Width&quot;));
    lineWidthMenu-&gt;addAction(lineWidthPressureAction);
    lineWidthMenu-&gt;addAction(lineWidthTiltAction);
    lineWidthMenu-&gt;addAction(lineWidthFixedAction);

    alphaChannelMenu = tabletMenu-&gt;addMenu(tr(&quot;&amp;Alpha Channel&quot;));
    alphaChannelMenu-&gt;addAction(alphaChannelPressureAction);
    alphaChannelMenu-&gt;addAction(alphaChannelTiltAction);
    alphaChannelMenu-&gt;addAction(noAlphaChannelAction);

    colorSaturationMenu = tabletMenu-&gt;addMenu(tr(&quot;&amp;Color Saturation&quot;));
    colorSaturationMenu-&gt;addAction(colorSaturationVTiltAction);
    colorSaturationMenu-&gt;addAction(colorSaturationHTiltAction);
    colorSaturationMenu-&gt;addAction(noColorSaturationAction);

    helpMenu = menuBar()-&gt;addMenu(&quot;&amp;Help&quot;);
    helpMenu-&gt;addAction(aboutAction);
    helpMenu-&gt;addAction(aboutQtAction);
}
</db:programlisting>
<db:para>We create the menus of the example and add the actions to them.</db:para>
</db:section>
<db:section xml:id="tabletcanvas-class-definition">
<db:title>TabletCanvas Class Definition</db:title>
<db:para>The <db:code>TabletCanvas</db:code> class provides a surface on which the user can draw with a tablet.</db:para>
<db:programlisting language="cpp">class TabletCanvas : public QWidget
{
    Q_OBJECT

public:
    enum AlphaChannelType { AlphaPressure, AlphaTilt, NoAlpha };
    enum ColorSaturationType { SaturationVTilt, SaturationHTilt,
                               SaturationPressure, NoSaturation };
    enum LineWidthType { LineWidthPressure, LineWidthTilt, NoLineWidth };

    TabletCanvas();

    bool saveImage(const QString &amp;file);
    bool loadImage(const QString &amp;file);
    void setAlphaChannelType(AlphaChannelType type)
        { alphaChannelType = type; }
    void setColorSaturationType(ColorSaturationType type)
        { colorSaturationType = type; }
    void setLineWidthType(LineWidthType type)
        { lineWidthType = type; }
    void setColor(const QColor &amp;color)
        { myColor = color; }
    QColor color() const
        { return myColor; }
    void setTabletDevice(QTabletEvent::TabletDevice device)
        { myTabletDevice = device; }
    int maximum(int a, int b)
        { return a &gt; b ? a : b; }

protected:
    void tabletEvent(QTabletEvent *event);
    void paintEvent(QPaintEvent *event);
    void resizeEvent(QResizeEvent *event);

private:
    void initPixmap();
    void paintPixmap(QPainter &amp;painter, QTabletEvent *event);
    Qt::BrushStyle brushPattern(qreal value);
    void updateBrush(QTabletEvent *event);

    AlphaChannelType alphaChannelType;
    ColorSaturationType colorSaturationType;
    LineWidthType lineWidthType;
    QTabletEvent::PointerType pointerType;
    QTabletEvent::TabletDevice myTabletDevice;
    QColor myColor;

    QPixmap pixmap;
    QBrush myBrush;
    QPen myPen;
    bool deviceDown;
    QPoint polyLine[3];
};
</db:programlisting>
<db:para>The canvas can change the alpha channel, color saturation, and line width of the drawing. We have one enum for each of these; their values decide if it is the tablet pressure or tilt that will alter them. We keep a private variable for each, the <db:code>alphaChannelType</db:code>, <db:code>colorSturationType</db:code>, and <db:code>penWidthType</db:code>, which we provide access functions for.</db:para>
<db:para>We draw on a <db:link xlink:href="qpixmap.xml">QPixmap</db:link> with <db:code>myPen</db:code> and <db:code>myBrush</db:code> using <db:code>myColor</db:code>. The <db:code>saveImage()</db:code> and <db:code>loadImage()</db:code> saves and loads the <db:link xlink:href="qpixmap.xml">QPixmap</db:link> to disk. The pixmap is drawn on the widget in <db:code>paintEvent()</db:code>. The <db:code>pointerType</db:code> and <db:code>deviceType</db:code> keeps the type of pointer, which is either a pen or an eraser, and device currently used on the tablet, which is either a stylus or an airbrush.</db:para>
<db:para>The interpretation of events from the tablet is done in <db:code>tabletEvent()</db:code>; <db:code>paintPixmap()</db:code>, <db:code>updateBrush()</db:code>, and <db:code>brushPattern()</db:code> are helper functions used by <db:code>tabletEvent()</db:code>.</db:para>
</db:section>
<db:section xml:id="tabletcanvas-class-implementation">
<db:title>TabletCanvas Class Implementation</db:title>
<db:para>We start with a look at the constructor:</db:para>
<db:programlisting language="cpp">TabletCanvas::TabletCanvas()
{
    resize(500, 500);
    myBrush = QBrush();
    myPen = QPen();
    initPixmap();
    setAutoFillBackground(true);
    deviceDown = false;
    myColor = Qt::red;
    myTabletDevice = QTabletEvent::Stylus;
    alphaChannelType = NoAlpha;
    colorSaturationType = NoSaturation;
    lineWidthType = LineWidthPressure;
}

void TabletCanvas::initPixmap()
{
    QPixmap newPixmap = QPixmap(width(), height());
    newPixmap.fill(Qt::white);
    QPainter painter(&amp;newPixmap);
    if (!pixmap.isNull())
        painter.drawPixmap(0, 0, pixmap);
    painter.end();
    pixmap = newPixmap;
}
</db:programlisting>
<db:para>In the constructor we initialize our class variables. We need to draw the background of our pixmap, as the default is gray.</db:para>
<db:para>Here is the implementation of <db:code>saveImage()</db:code>:</db:para>
<db:programlisting language="cpp">bool TabletCanvas::saveImage(const QString &amp;file)
{
    return pixmap.save(file);
}
</db:programlisting>
<db:para><db:link xlink:href="qpixmap.xml">QPixmap</db:link> implements functionality to save itself to disk, so we simply call <db:link xlink:href="qpixmap.xml#save">save</db:link>().</db:para>
<db:para>Here is the implementation of <db:code>loadImage()</db:code>:</db:para>
<db:programlisting language="cpp">bool TabletCanvas::loadImage(const QString &amp;file)
{
    bool success = pixmap.load(file);

    if (success) {
        update();
        return true;
    }
    return false;
}
</db:programlisting>
<db:para>We simply call <db:link xlink:href="qpixmap.xml#load">load</db:link>(), which loads the image in <db:code role="parameter">file</db:code>.</db:para>
<db:para>Here is the implementation of <db:code>tabletEvent()</db:code>:</db:para>
<db:programlisting language="cpp">void TabletCanvas::tabletEvent(QTabletEvent *event)
{

    switch (event-&gt;type()) {
        case QEvent::TabletPress:
            if (!deviceDown) {
                deviceDown = true;
                polyLine[0] = polyLine[1] = polyLine[2] = event-&gt;pos();
            }
            break;
        case QEvent::TabletRelease:
            if (deviceDown)
                deviceDown = false;
            break;
        case QEvent::TabletMove:
            polyLine[2] = polyLine[1];
            polyLine[1] = polyLine[0];
            polyLine[0] = event-&gt;pos();

            if (deviceDown) {
                updateBrush(event);
                QPainter painter(&amp;pixmap);
                paintPixmap(painter, event);
            }
            break;
        default:
            break;
    }
    update();
}
</db:programlisting>
<db:para>We get three kind of events to this function: TabletPress, TabletRelease, and TabletMove, which is generated when a device is pressed down on, leaves, or moves on the tablet. We set the <db:code>deviceDown</db:code> to true when a device is pressed down on the tablet; we then know when we should draw when we receive move events. We have implemented the <db:code>updateBrush()</db:code> and <db:code>paintPixmap()</db:code> helper functions to update <db:code>myBrush</db:code> and <db:code>myPen</db:code> after the state of <db:code>alphaChannelType</db:code>, <db:code>colorSaturationType</db:code>, and <db:code>lineWidthType</db:code>.</db:para>
<db:para>Here is the implementation of <db:code>paintEvent()</db:code>:</db:para>
<db:programlisting language="cpp">void TabletCanvas::paintEvent(QPaintEvent *)
{
    QPainter painter(this);
    painter.drawPixmap(0, 0, pixmap);
}
</db:programlisting>
<db:para>We simply draw the pixmap to the top left of the widget.</db:para>
<db:para>Here is the implementation of <db:code>paintPixmap()</db:code>:</db:para>
<db:programlisting language="cpp">void TabletCanvas::paintPixmap(QPainter &amp;painter, QTabletEvent *event)
{
    QPoint brushAdjust(10, 10);

    switch (myTabletDevice) {
        case QTabletEvent::Airbrush:
            myBrush.setColor(myColor);
            myBrush.setStyle(brushPattern(event-&gt;pressure()));
            painter.setPen(Qt::NoPen);
            painter.setBrush(myBrush);

            for (int i = 0; i &lt; 3; ++i) {
                painter.drawEllipse(QRect(polyLine[i] - brushAdjust,
                                    polyLine[i] + brushAdjust));
            }
            break;
        case QTabletEvent::Puck:
        case QTabletEvent::FourDMouse:
        case QTabletEvent::RotationStylus:
            {
                const QString error(tr(&quot;This input device is not supported by the example.&quot;));
#ifndef QT_NO_STATUSTIP
                QStatusTipEvent status(error);
                QApplication::sendEvent(this, &amp;status);
#else
                qWarning() &lt;&lt; error;
#endif
            }
            break;
        default:
            {
                const QString error(tr(&quot;Unknown tablet device - treating as stylus&quot;));
#ifndef QT_NO_STATUSTIP
                QStatusTipEvent status(error);
                QApplication::sendEvent(this, &amp;status);
#else
                qWarning() &lt;&lt; error;
#endif
            }
            // FALL-THROUGH
        case QTabletEvent::Stylus:
            painter.setBrush(myBrush);
            painter.setPen(myPen);
            painter.drawLine(polyLine[1], event-&gt;pos());
            break;
    }
}
</db:programlisting>
<db:para>In this function we draw on the pixmap based on the movement of the device. If the device used on the tablet is a stylus we want to draw a line between the positions of the stylus recorded in <db:code>polyLine</db:code>. We also assume that this is a reasonable handling of any unknown device, but update the statusbar with a warning so that the user can see that for his tablet he might have to implement special handling. If it is an airbrush we want to draw a circle of points with a point density based on the tangential pressure, which is the position of the finger wheel on the airbrush. We use the <db:link xlink:href="qt.xml#BrushStyle-enum">Qt::BrushStyle</db:link> to draw the points as it has styles that draw points with different density; we select the style based on the tangential pressure in <db:code>brushPattern()</db:code>.</db:para>
<db:programlisting language="cpp">Qt::BrushStyle TabletCanvas::brushPattern(qreal value)
{
    int pattern = int((value) * 100.0) % 7;

    switch (pattern) {
        case 0:
            return Qt::SolidPattern;
        case 1:
            return Qt::Dense1Pattern;
        case 2:
            return Qt::Dense2Pattern;
        case 3:
            return Qt::Dense3Pattern;
        case 4:
            return Qt::Dense4Pattern;
        case 5:
            return Qt::Dense5Pattern;
        case 6:
            return Qt::Dense6Pattern;
        default:
            return Qt::Dense7Pattern;
    }
}
</db:programlisting>
<db:para>We return a brush style with a point density that increases with the tangential pressure.</db:para>
<db:para>In <db:code>updateBrush()</db:code> we set the pen and brush used for drawing to match <db:code>alphaChannelType</db:code>, <db:code>lineWidthType</db:code>, <db:code>colorSaturationType</db:code>, and <db:code>myColor</db:code>. We will examine the code to set up <db:code>myBrush</db:code> and <db:code>myPen</db:code> for each of these variables:</db:para>
<db:programlisting language="cpp">void TabletCanvas::updateBrush(QTabletEvent *event)
{
    int hue, saturation, value, alpha;
    myColor.getHsv(&amp;hue, &amp;saturation, &amp;value, &amp;alpha);

    int vValue = int(((event-&gt;yTilt() + 60.0) / 120.0) * 255);
    int hValue = int(((event-&gt;xTilt() + 60.0) / 120.0) * 255);
</db:programlisting>
<db:para>We fetch the current drawingcolor's hue, saturation, value, and alpha values. <db:code>hValue</db:code> and <db:code>vValue</db:code> are set to the horizontal and vertical tilt as a number from 0 to 255. The original values are in degrees from -60 to 60, i.e., 0 equals -60, 127 equals 0, and 255 equals 60 degrees. The angle measured is between the device and the perpendicular of the tablet (see <db:link xlink:href="qtabletevent.xml">QTabletEvent</db:link> for an illustration).</db:para>
<db:programlisting language="cpp">    switch (alphaChannelType) {
        case AlphaPressure:
            myColor.setAlpha(int(event-&gt;pressure() * 255.0));
            break;
        case AlphaTilt:
            myColor.setAlpha(maximum(abs(vValue - 127), abs(hValue - 127)));
            break;
        default:
            myColor.setAlpha(255);
    }

</db:programlisting>
<db:para>The alpha channel of <db:link xlink:href="qcolor.xml">QColor</db:link> is given as a number between 0 and 255 where 0 is transparent and 255 is opaque. <db:link xlink:href="qtabletevent.xml#pressure">pressure</db:link>() returns the pressure as a qreal between 0.0 and 1.0. By subtracting 127 from the tilt values and taking the absolute value we get the smallest alpha values (i.e., the color is most transparent) when the pen is perpendicular to the tablet. We select the largest of the vertical and horizontal tilt value.</db:para>
<db:programlisting language="cpp">    switch (colorSaturationType) {
        case SaturationVTilt:
            myColor.setHsv(hue, vValue, value, alpha);
            break;
        case SaturationHTilt:
            myColor.setHsv(hue, hValue, value, alpha);
            break;
        case SaturationPressure:
            myColor.setHsv(hue, int(event-&gt;pressure() * 255.0), value, alpha);
            break;
        default:
            ;
    }

</db:programlisting>
<db:para>The colorsaturation is given as a number between 0 and 255. It is set with <db:link xlink:href="qcolor.xml#setHsv">setHsv</db:link>(). We can set the tilt values directly, but must multiply the pressure to a number between 0 and 255.</db:para>
<db:programlisting language="cpp">    switch (lineWidthType) {
        case LineWidthPressure:
            myPen.setWidthF(event-&gt;pressure() * 10 + 1);
            break;
        case LineWidthTilt:
            myPen.setWidthF(maximum(abs(vValue - 127), abs(hValue - 127)) / 12);
            break;
        default:
            myPen.setWidthF(1);
    }

</db:programlisting>
<db:para>The width of the pen increases with the pressure. When the pen width is controlled with the tilt we let the width increse with the angle between the device and the perpendicular of the tablet.</db:para>
<db:programlisting language="cpp">    if (event-&gt;pointerType() == QTabletEvent::Eraser) {
        myBrush.setColor(Qt::white);
        myPen.setColor(Qt::white);
        myPen.setWidthF(event-&gt;pressure() * 10 + 1);
    } else {
        myBrush.setColor(myColor);
        myPen.setColor(myColor);
    }
}
</db:programlisting>
<db:para>We finally check whether the pointer is the stylus or the eraser. If it is the eraser, we set the color to the background color of the pixmap an let the pressure decide the pen width, else we set the colors we have set up previously in the function.</db:para>
</db:section>
<db:section xml:id="tabletapplication-class-definition">
<db:title>TabletApplication Class Definition</db:title>
<db:para>We inherit <db:link xlink:href="qapplication.xml">QApplication</db:link> in this class because we want to reimplement the <db:link xlink:href="qapplication.xml#event">event</db:link>() function.</db:para>
<db:programlisting language="cpp">class TabletApplication : public QApplication
{
    Q_OBJECT

public:
    TabletApplication(int &amp;argv, char **args)
    : QApplication(argv, args) {}

    bool event(QEvent *event);
    void setCanvas(TabletCanvas *canvas)
        { myCanvas = canvas; }

private:
    TabletCanvas *myCanvas;
};
</db:programlisting>
<db:para>We keep a <db:code>TabletCanvas</db:code> we send the device type of the events we handle in the <db:code>event()</db:code> function to. The TabletEnterProximity and TabletLeaveProximity events are not sendt to the <db:link xlink:href="qapplication.xml">QApplication</db:link> object, while other tablet events are sendt to the <db:link xlink:href="qwidget.xml">QWidget</db:link>'s <db:code>event()</db:code>, which sends them on to <db:link xlink:href="qwidget.xml#tabletEvent">tabletEvent</db:link>(). Since we want to handle these events we have implemented <db:code>TabletApplication</db:code>.</db:para>
</db:section>
<db:section xml:id="tabletapplication-class-implementation">
<db:title>TabletApplication Class Implementation</db:title>
<db:para>Here is the implementation of <db:code>event()</db:code>:</db:para>
<db:programlisting language="cpp">bool TabletApplication::event(QEvent *event)
{
    if (event-&gt;type() == QEvent::TabletEnterProximity ||
        event-&gt;type() == QEvent::TabletLeaveProximity) {
        myCanvas-&gt;setTabletDevice(
            static_cast&lt;QTabletEvent *&gt;(event)-&gt;device());
        return true;
    }
    return QApplication::event(event);
}
</db:programlisting>
<db:para>We use this function to handle the TabletEnterProximity and TabletLeaveProximity events, which is generated when a device enters and leaves the proximity of the tablet. The intended use of these events is to do work that is dependent on what kind of device is used on the tablet. This way, you don't have to do this work when other events are generated, which is more frequently than the leave and enter proximity events. We call <db:code>setTabletDevice()</db:code> in <db:code>TabletCanvas</db:code>.</db:para>
</db:section>
<db:section xml:id="the-main-function">
<db:title>The <db:code>main()</db:code> function</db:title>
<db:para>Here is the examples <db:code>main()</db:code> function:</db:para>
<db:programlisting language="cpp">int main(int argv, char *args[])
{
    TabletApplication app(argv, args);
    TabletCanvas *canvas = new TabletCanvas;
    app.setCanvas(canvas);

    MainWindow mainWindow(canvas);
    mainWindow.resize(500, 500);
    mainWindow.show();
    return app.exec();
}
</db:programlisting>
<db:para>In the <db:code>main()</db:code> function we create a <db:code>MainWinow</db:code> and display it as a top level window. We use the <db:code>TabletApplication</db:code> class. We need to set the canvas after the application is created. We cannot use classes that implement event handling before an <db:link xlink:href="qapplication.xml">QApplication</db:link> object is instantiated.</db:para>
</db:section>
<db:section>
<db:title>List of Files</db:title>
<db:para>Files:</db:para>
<db:section>
<db:title>List of Files</db:title>
<db:itemizedlist>
<db:listitem>
<db:para><db:link xlink:href="widgets/tablet/main.cpp">widgets/tablet/main.cpp</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="widgets/tablet/mainwindow.cpp">widgets/tablet/mainwindow.cpp</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="widgets/tablet/mainwindow.h">widgets/tablet/mainwindow.h</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="widgets/tablet/tablet.pro">widgets/tablet/tablet.pro</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="widgets/tablet/tabletapplication.cpp">widgets/tablet/tabletapplication.cpp</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="widgets/tablet/tabletapplication.h">widgets/tablet/tabletapplication.h</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="widgets/tablet/tabletcanvas.cpp">widgets/tablet/tabletcanvas.cpp</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="widgets/tablet/tabletcanvas.h">widgets/tablet/tabletcanvas.h</db:link></db:para></db:listitem>
</db:itemizedlist>
</db:section>
</db:section></db:article>
