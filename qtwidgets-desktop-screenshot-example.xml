<?xml version="1.0" encoding="UTF-8"?>
<db:article xmlns:db="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink" version="5.2" xml:lang="en">
<db:info>
<db:title>Screenshot Example</db:title>
<db:productname>QtWidgets</db:productname>
<db:edition>Qt 5.4.2 Reference Documentation</db:edition>
<db:titleabbrev>Qt Widgets Reference Documentation</db:titleabbrev>
<db:abstract>
<db:para>The Screenshot example shows how to take a screenshot of the desktop.</db:para>
</db:abstract>
<db:pubdate>2025-06-29</db:pubdate>
<db:date>2025-06-29</db:date>
<db:authorgroup>
<db:author>
<db:orgname class="corporation">The Qt Company Ltd.</db:orgname>
</db:author>
</db:authorgroup>
</db:info>
<db:mediaobject>
<db:imageobject>
<db:imagedata fileref="images/screenshot-example.png"/>
</db:imageobject>
</db:mediaobject>
<db:para>With the application the users can take a screenshot of their desktop. They are provided with a couple of options:</db:para>
<db:itemizedlist>
<db:listitem>
<db:para>Delaying the screenshot, giving them time to rearrange their desktop.</db:para>
</db:listitem>
<db:listitem>
<db:para>Hiding the application's window while the screenshot is taken.</db:para>
</db:listitem>
</db:itemizedlist>
<db:para>In addition the application allows the users to save their screenshot if they want to.</db:para>
<db:section xml:id="screenshot-class-definition">
<db:title>Screenshot Class Definition</db:title>
<db:programlisting language="cpp">class Screenshot : public QWidget
{
    Q_OBJECT

public:
    Screenshot();

protected:
    void resizeEvent(QResizeEvent *event) Q_DECL_OVERRIDE;

private slots:
    void newScreenshot();
    void saveScreenshot();
    void shootScreen();
    void updateCheckBox();

private:
    void createOptionsGroupBox();
    void createButtonsLayout();
    QPushButton *createButton(const QString &amp;text, QWidget *receiver, const char *member);
    void updateScreenshotLabel();

    QPixmap originalPixmap;

    QLabel *screenshotLabel;
    QGroupBox *optionsGroupBox;
    QSpinBox *delaySpinBox;
    QLabel *delaySpinBoxLabel;
    QCheckBox *hideThisWindowCheckBox;
    QPushButton *newScreenshotButton;
    QPushButton *saveScreenshotButton;
    QPushButton *quitScreenshotButton;

    QVBoxLayout *mainLayout;
    QGridLayout *optionsGroupBoxLayout;
    QHBoxLayout *buttonsLayout;
};
</db:programlisting>
<db:para>The <db:code>Screenshot</db:code> class inherits QWidget and is the application's main widget. It displays the application options and a preview of the screenshot.</db:para>
<db:para>We reimplement the QWidget::resizeEvent() function to make sure that the preview of the screenshot scales properly when the user resizes the application widget. We also need several private slots to facilitate the options:</db:para>
<db:itemizedlist>
<db:listitem>
<db:para>The <db:code>newScreenshot()</db:code> slot prepares a new screenshot.</db:para>
</db:listitem>
<db:listitem>
<db:para>The <db:code>saveScreenshot()</db:code> slot saves the last screenshot.</db:para>
</db:listitem>
<db:listitem>
<db:para>The <db:code>shootScreen()</db:code> slot takes the screenshot.</db:para>
</db:listitem>
<db:listitem>
<db:para>The <db:code>updateCheckBox()</db:code> slot enables or disables the <db:guilabel>Hide This Window</db:guilabel> option.</db:para>
</db:listitem>
</db:itemizedlist>
<db:para>We also declare some private functions: We use the <db:code>createOptionsGroupBox()</db:code>, <db:code>createButtonsLayout()</db:code> and <db:code>createButton()</db:code> functions when we construct the widget. And we call the private <db:code>updateScreenshotLabel()</db:code> function whenever a new screenshot is taken or when a resize event changes the size of the screenshot preview label.</db:para>
<db:para>In addition we need to store the screenshot's original pixmap. The reason is that when we display the preview of the screenshot, we need to scale its pixmap, storing the original we make sure that no data are lost in that process.</db:para>
</db:section>
<db:section xml:id="screenshot-class-implementation">
<db:title>Screenshot Class Implementation</db:title>
<db:programlisting language="cpp">Screenshot::Screenshot()
{
    screenshotLabel = new QLabel;
    screenshotLabel-&gt;setSizePolicy(QSizePolicy::Expanding, QSizePolicy::Expanding);
    screenshotLabel-&gt;setAlignment(Qt::AlignCenter);
    screenshotLabel-&gt;setMinimumSize(240, 160);

    createOptionsGroupBox();
    createButtonsLayout();

    mainLayout = new QVBoxLayout;
    mainLayout-&gt;addWidget(screenshotLabel);
    mainLayout-&gt;addWidget(optionsGroupBox);
    mainLayout-&gt;addLayout(buttonsLayout);
    setLayout(mainLayout);

    shootScreen();
    delaySpinBox-&gt;setValue(5);

    setWindowTitle(tr(&quot;Screenshot&quot;));
    resize(300, 200);
}
</db:programlisting>
<db:para>In the constructor we first create the QLabel displaying the screenshot preview.</db:para>
<db:para>We set the QLabel's size policy to be QSizePolicy::Expanding both horizontally and vertically. This means that the QLabel's size hint is a sensible size, but the widget can be shrunk and still be useful. Also, the widget can make use of extra space, so it should get as much space as possible. Then we make sure the QLabel is aligned in the center of the <db:code>Screenshot</db:code> widget, and set its minimum size.</db:para>
<db:para>We create the applications's buttons and the group box containing the application's options, and put it all into a main layout. Finally we take the initial screenshot, and set the initial delay and the window title, before we resize the widget to a suitable size.</db:para>
<db:programlisting language="cpp">void Screenshot::resizeEvent(QResizeEvent * /* event */)
{
    QSize scaledSize = originalPixmap.size();
    scaledSize.scale(screenshotLabel-&gt;size(), Qt::KeepAspectRatio);
    if (!screenshotLabel-&gt;pixmap() || scaledSize != screenshotLabel-&gt;pixmap()-&gt;size())
        updateScreenshotLabel();
}
</db:programlisting>
<db:para>The <db:code>resizeEvent()</db:code> function is reimplemented to receive the resize events dispatched to the widget. The purpose is to scale the preview screenshot pixmap without deformation of its content, and also make sure that the application can be resized smoothly.</db:para>
<db:para>To achieve the first goal, we scale the screenshot pixmap using <db:link xlink:href="qt.xml#AspectRatioMode-enum">Qt::KeepAspectRatio</db:link>. We scale the pixmap to a rectangle as large as possible inside the current size of the screenshot preview label, preserving the aspect ratio. This means that if the user resizes the application window in only one direction, the preview screenshot keeps the same size.</db:para>
<db:para>To reach our second goal, we make sure that the preview screenshot only is repainted (using the private <db:code>updateScreenshotLabel()</db:code> function) when it actually changes its size.</db:para>
<db:programlisting language="cpp">void Screenshot::newScreenshot()
{
    if (hideThisWindowCheckBox-&gt;isChecked())
        hide();
    newScreenshotButton-&gt;setDisabled(true);

    QTimer::singleShot(delaySpinBox-&gt;value() * 1000, this, SLOT(shootScreen()));
}
</db:programlisting>
<db:para>The private <db:code>newScreenshot()</db:code> slot is called when the user requests a new screenshot; but the slot only prepares a new screenshot.</db:para>
<db:para>First we see if the <db:guilabel>Hide This Window</db:guilabel> option is checked, if it is we hide the <db:code>Screenshot</db:code> widget. Then we disable the <db:guilabel>New Screenshot</db:guilabel> button, to make sure the user only can request one screenshot at a time.</db:para>
<db:para>We create a timer using the <db:link xlink:href="qtimer.xml">QTimer</db:link> class which provides repetitive and single-shot timers. We set the timer to time out only once, using the static <db:link xlink:href="qtimer.xml#singleShot">QTimer::singleShot</db:link>() function. This function calls the private <db:code>shootScreen()</db:code> slot after the time interval specified by the <db:guilabel>Screenshot Delay</db:guilabel> option. It is <db:code>shootScreen()</db:code> that actually performs the screenshot.</db:para>
<db:programlisting language="cpp">void Screenshot::saveScreenshot()
{
    QString format = &quot;png&quot;;
    QString initialPath = QDir::currentPath() + tr(&quot;/untitled.&quot;) + format;

    QString fileName = QFileDialog::getSaveFileName(this, tr(&quot;Save As&quot;), initialPath,
                                                    tr(&quot;%1 Files (*.%2);;All Files (*)&quot;)
                                                    .arg(format.toUpper())
                                                    .arg(format));
    if (!fileName.isEmpty())
        originalPixmap.save(fileName, format.toLatin1().constData());
}
</db:programlisting>
<db:para>The <db:code>saveScreenshot()</db:code> slot is called when the user push the <db:guilabel>Save</db:guilabel> button, and it presents a file dialog using the QFileDialog class.</db:para>
<db:para>QFileDialog enables a user to traverse the file system in order to select one or many files or a directory. The easiest way to create a QFileDialog is to use the convenience static functions.</db:para>
<db:para>We define the default file format to be png, and we make the file dialog's initial path the path the application is run from. We create the file dialog using the static QFileDialog::getSaveFileName() function which returns a file name selected by the user. The file does not have to exist. If the file name is valid, we use the QPixmap::save() function to save the screenshot's original pixmap in that file.</db:para>
<db:programlisting language="cpp">void Screenshot::shootScreen()
{
    if (delaySpinBox-&gt;value() != 0)
        qApp-&gt;beep();
</db:programlisting>
<db:para>The <db:code>shootScreen()</db:code> slot is called to take the screenshot. If the user has chosen to delay the screenshot, we make the application beep when the screenshot is taken using the static QApplication::beep() function.</db:para>
<db:para>The QApplication class manages the GUI application's control flow and main settings. It contains the main event loop, where all events from the window system and other sources are processed and dispatched.</db:para>
<db:programlisting language="cpp">    QScreen *screen = QGuiApplication::primaryScreen();
    if (screen)
        originalPixmap = screen-&gt;grabWindow(0);
    updateScreenshotLabel();

    newScreenshotButton-&gt;setDisabled(false);
    if (hideThisWindowCheckBox-&gt;isChecked())
        show();
}
</db:programlisting>
<db:para>Using the static function QApplication::primaryScreen(), we obtain the QScreen object for the application's main screen.</db:para>
<db:para>We take the screenshot using the QScreen::grabWindow() function. The function grabs the contents of the window passed as an argument, makes a pixmap out of it and returns that pixmap. The window id can be obtained with QWidget::winId() or QWindow::winId(). Here, however, we just pass 0 as the window id, indicating that we want to grab the entire screen.</db:para>
<db:para>We update the screenshot preview label using the private <db:code>updateScreenshotLabel()</db:code> function. Then we enable the <db:guilabel>New Screenshot</db:guilabel> button, and finally we make the <db:code>Screenshot</db:code> widget visible if it was hidden during the screenshot.</db:para>
<db:programlisting language="cpp">void Screenshot::updateCheckBox()
{
    if (delaySpinBox-&gt;value() == 0) {
        hideThisWindowCheckBox-&gt;setDisabled(true);
        hideThisWindowCheckBox-&gt;setChecked(false);
    } else {
        hideThisWindowCheckBox-&gt;setDisabled(false);
    }
}
</db:programlisting>
<db:para>The <db:guilabel>Hide This Window</db:guilabel> option is enabled or disabled depending on the delay of the screenshot. If there is no delay, the application window cannot be hidden and the option's checkbox is disabled.</db:para>
<db:para>The <db:code>updateCheckBox()</db:code> slot is called whenever the user changes the delay using the <db:guilabel>Screenshot Delay</db:guilabel> option.</db:para>
<db:programlisting language="cpp">void Screenshot::createOptionsGroupBox()
{
    optionsGroupBox = new QGroupBox(tr(&quot;Options&quot;));

    delaySpinBox = new QSpinBox;
    delaySpinBox-&gt;setSuffix(tr(&quot; s&quot;));
    delaySpinBox-&gt;setMaximum(60);
    connect(delaySpinBox, SIGNAL(valueChanged(int)), this, SLOT(updateCheckBox()));

    delaySpinBoxLabel = new QLabel(tr(&quot;Screenshot Delay:&quot;));

    hideThisWindowCheckBox = new QCheckBox(tr(&quot;Hide This Window&quot;));

    optionsGroupBoxLayout = new QGridLayout;
    optionsGroupBoxLayout-&gt;addWidget(delaySpinBoxLabel, 0, 0);
    optionsGroupBoxLayout-&gt;addWidget(delaySpinBox, 0, 1);
    optionsGroupBoxLayout-&gt;addWidget(hideThisWindowCheckBox, 1, 0, 1, 2);
    optionsGroupBox-&gt;setLayout(optionsGroupBoxLayout);
}
</db:programlisting>
<db:para>The private <db:code>createOptionsGroupBox()</db:code> function is called from the constructor.</db:para>
<db:para>First we create a group box that will contain all of the options' widgets. Then we create a QSpinBox and a QLabel for the <db:guilabel>Screenshot Delay</db:guilabel> option, and connect the spinbox to the <db:code>updateCheckBox()</db:code> slot. Finally, we create a QCheckBox for the <db:guilabel>Hide This Window</db:guilabel> option, add all the options' widgets to a QGridLayout and install the layout on the group box.</db:para>
<db:para>Note that we don't have to specify any parents for the widgets when we create them. The reason is that when we add a widget to a layout and install the layout on another widget, the layout's widgets are automatically reparented to the widget the layout is installed on.</db:para>
<db:programlisting language="cpp">void Screenshot::createButtonsLayout()
{
    newScreenshotButton = createButton(tr(&quot;New Screenshot&quot;), this, SLOT(newScreenshot()));
    saveScreenshotButton = createButton(tr(&quot;Save Screenshot&quot;), this, SLOT(saveScreenshot()));
    quitScreenshotButton = createButton(tr(&quot;Quit&quot;), this, SLOT(close()));

    buttonsLayout = new QHBoxLayout;
    buttonsLayout-&gt;addStretch();
    buttonsLayout-&gt;addWidget(newScreenshotButton);
    buttonsLayout-&gt;addWidget(saveScreenshotButton);
    buttonsLayout-&gt;addWidget(quitScreenshotButton);
}
</db:programlisting>
<db:para>The private <db:code>createButtonsLayout()</db:code> function is called from the constructor. We create the application's buttons using the private <db:code>createButton()</db:code> function, and add them to a QHBoxLayout.</db:para>
<db:programlisting language="cpp">QPushButton *Screenshot::createButton(const QString &amp;text, QWidget *receiver,
                                      const char *member)
{
    QPushButton *button = new QPushButton(text);
    button-&gt;connect(button, SIGNAL(clicked()), receiver, member);
    return button;
}
</db:programlisting>
<db:para>The private <db:code>createButton()</db:code> function is called from the <db:code>createButtonsLayout()</db:code> function. It simply creates a QPushButton with the provided text, connects it to the provided receiver and slot, and returns a pointer to the button.</db:para>
<db:programlisting language="cpp">void Screenshot::updateScreenshotLabel()
{
    screenshotLabel-&gt;setPixmap(originalPixmap.scaled(screenshotLabel-&gt;size(),
                                                     Qt::KeepAspectRatio,
                                                     Qt::SmoothTransformation));
}
</db:programlisting>
<db:para>The private <db:code>updateScreenshotLabel()</db:code> function is called whenever the screenshot changes, or when a resize event changes the size of the screenshot preview label. It updates the screenshot preview's label using the QLabel::setPixmap() and QPixmap::scaled() functions.</db:para>
<db:para>QPixmap::scaled() returns a copy of the given pixmap scaled to a rectangle of the given size according to the given <db:link xlink:href="qt.xml#AspectRatioMode-enum">Qt::AspectRatioMode</db:link> and <db:link xlink:href="qt.xml#TransformationMode-enum">Qt::TransformationMode</db:link>.</db:para>
<db:para>We scale the original pixmap to fit the current screenshot label's size, preserving the aspect ratio and giving the resulting pixmap smoothed edges.</db:para>
</db:section>
<db:section>
<db:title>List of Files</db:title>
<db:para>Files:</db:para>
<db:section>
<db:title>List of Files</db:title>
<db:itemizedlist>
<db:listitem>
<db:para><db:link xlink:href="desktop/screenshot/main.cpp">desktop/screenshot/main.cpp</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="desktop/screenshot/screenshot.cpp">desktop/screenshot/screenshot.cpp</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="desktop/screenshot/screenshot.h">desktop/screenshot/screenshot.h</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="desktop/screenshot/screenshot.pro">desktop/screenshot/screenshot.pro</db:link></db:para></db:listitem>
</db:itemizedlist>
</db:section>
</db:section></db:article>
