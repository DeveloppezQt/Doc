<?xml version="1.0" encoding="UTF-8"?>
<db:article xmlns:db="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink" version="5.2" xml:lang="en">
<db:info>
<db:title>XML Streaming</db:title>
<db:productname>QtXml</db:productname>
<db:edition>Qt 5.14.2 Reference Documentation</db:edition>
<db:titleabbrev>Qt XML Reference Documentation</db:titleabbrev>
<db:extendedlink xlink:type="extended"><db:link xlink:to="xml-namespaces.xml" xlink:type="arc" xlink:arcrole="prev" xlink:title="An Introduction to Namespaces"/></db:extendedlink>
<db:extendedlink xlink:type="extended"><db:link xlink:to="xml-sax.xml" xlink:type="arc" xlink:arcrole="next" xlink:title="The SAX interface"/></db:extendedlink>
<db:abstract>
<db:para>Qt XML Reference Documentation.</db:para></db:abstract>
<db:pubdate>2024-12-27</db:pubdate>
<db:date>2024-12-27</db:date>
<db:authorgroup>
<db:author>
<db:orgname class="corporation">The Qt Company Ltd.</db:orgname>
</db:author>
</db:authorgroup>
</db:info>
<db:para><db:emphasis role="bold">&lt;Unknown command&gt;<db:code>contentspage</db:code></db:emphasis>XML Processing</db:para>
<db:para>Since version 4.3, Qt provides two new classes for reading and writing XML: QXmlStreamReader and QXmlStreamWriter.</db:para>
<db:para>The QXmlStreamReader and QXmlStreamWriter are two new classes provided in Qt 4.3 and later. A stream reader reports an XML document as a stream of tokens. This differs from SAX as SAX applications provide handlers to receive XML events from the parser whereas the QXmlStreamReader drives the loop, pulling tokens from the reader when they are needed. This pulling approach makes it possible to build recursive descent parsers, allowing XML parsing code to be split into different methods or classes.</db:para>
<db:para>QXmlStreamReader is a well-formed XML 1.0 parser that excludes external parsed entities. Hence, data provided by the stream reader adheres to the W3C's criteria for well-formed XML, as long as no error occurs. Otherwise, functions such as <db:link xlink:href="">atEnd()</db:link>, <db:link xlink:href="">error()</db:link> and <db:link xlink:href="">hasError()</db:link> can be used to check and view the errors.</db:para>
<db:para>An example of QXmlStreamReader implementation would be the <db:code>XbelReader</db:code> in <db:link xlink:href="qtxml-streambookmarks-example.xml">QXmlStream Bookmarks Example</db:link>, which wraps a QXmlStreamReader. The constructor takes <db:code role="parameter">treeWidget</db:code> as a parameter and the class has Xbel specific functions:</db:para>
<db:programlisting language="cpp">    XbelReader(QTreeWidget *treeWidget);
    ...
    void readXBEL();
    void readTitle(QTreeWidgetItem *item);
    void readSeparator(QTreeWidgetItem *item);
    void readFolder(QTreeWidgetItem *item);
    void readBookmark(QTreeWidgetItem *item);

    QTreeWidgetItem *createChildItem(QTreeWidgetItem *item);

    QXmlStreamReader xml;
    QTreeWidget *treeWidget;
    ...
</db:programlisting>
<db:para>The <db:code>read()</db:code> function accepts a QIODevice and sets it with <db:link xlink:href="">setDevice()</db:link>. The <db:link xlink:href="">raiseError()</db:link> function is used to display a custom error message, inidicating that the file's version is incorrect.</db:para>
<db:programlisting language="cpp">bool XbelReader::read(QIODevice *device)
{
    xml.setDevice(device);

    if (xml.readNextStartElement()) {
        if (xml.name() == QLatin1String(&quot;xbel&quot;)
            &amp;amp;&amp;amp; xml.attributes().value(versionAttribute()) == QLatin1String(&quot;1.0&quot;)) {
            readXBEL();
        } else {
            xml.raiseError(QObject::tr(&quot;The file is not an XBEL version 1.0 file.&quot;));
        }
    }

    return !xml.error();
}
</db:programlisting>
<db:para>The pendent to QXmlStreamReader is QXmlStreamWriter, which provides an XML writer with a simple streaming API. QXmlStreamWriter operates on a QIODevice and has specialized functions for all XML tokens or events you want to write, such as <db:link xlink:href="">writeDTD()</db:link>, <db:link xlink:href="">writeCharacters()</db:link>, <db:link xlink:href="">writeComment()</db:link> and so on.</db:para>
<db:para>To write XML document with QXmlStreamWriter, you start a document with the <db:link xlink:href="">writeStartDocument()</db:link> function and end it with <db:link xlink:href="">writeEndDocument()</db:link>, which implicitly closes all remaining open tags. Element tags are opened with <db:link xlink:href="">writeStartDocument()</db:link> and followed by <db:link xlink:href="">writeAttribute()</db:link> or <db:link xlink:href="">writeAttributes()</db:link>, element content, and then <db:link xlink:href="">writeEndDocument()</db:link>. Also, <db:link xlink:href="">writeEmptyElement()</db:link> can be used to write empty elements.</db:para>
<db:para>Element content comprises characters, entity references or nested elements. Content can be written with <db:link xlink:href="">writeCharacters()</db:link>, a function that also takes care of escaping all forbidden characters and character sequences, <db:link xlink:href="">writeEntityReference()</db:link>, or subsequent calls to <db:link xlink:href="">writeStartElement()</db:link>.</db:para>
<db:para>The <db:code>XbelWriter</db:code> class from <db:link xlink:href="qtxml-streambookmarks-example.xml">QXmlStream Bookmarks Example</db:link> wraps a QXmlStreamWriter. Its <db:code>writeFile()</db:code> function illustrates the core functions of QXmlStreamWriter mentioned above:</db:para>
<db:programlisting language="cpp">bool XbelWriter::writeFile(QIODevice *device)
{
    xml.setDevice(device);

    xml.writeStartDocument();
    xml.writeDTD(QStringLiteral(&quot;&amp;lt;!DOCTYPE xbel&amp;gt;&quot;));
    xml.writeStartElement(QStringLiteral(&quot;xbel&quot;));
    xml.writeAttribute(XbelReader::versionAttribute(), QStringLiteral(&quot;1.0&quot;));
    for (int i = 0; i &amp;lt; treeWidget-&amp;gt;topLevelItemCount(); ++i)
        writeItem(treeWidget-&amp;gt;topLevelItem(i));

    xml.writeEndDocument();
    return true;
}
</db:programlisting>
</db:article>
