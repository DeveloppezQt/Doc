<?xml version="1.0" encoding="UTF-8"?>
<db:article xmlns:db="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink" version="5.2" xml:lang="en">
<db:info>
<db:title>LegendMarkers Example</db:title>
<db:productname>QtCharts</db:productname>
<db:edition>Qt 5.8.0 Reference Documentation</db:edition>
<db:titleabbrev>Qt Charts Reference Documentation</db:titleabbrev>
<db:abstract>
<db:para>The example shows how to make use of legend markers.</db:para>
</db:abstract>
<db:pubdate>2025-06-29</db:pubdate>
<db:date>2025-06-29</db:date>
<db:authorgroup>
<db:author>
<db:orgname class="corporation">The Qt Company Ltd.</db:orgname>
</db:author>
</db:authorgroup>
</db:info>
<db:para>In this example we create an application which uses the QLegendMarker clicked signal to show/hide the corresponding series in a chart. The connect marker button connects a clicked signal from all markers in a legend to the handleMarkerClicked slot.</db:para>
<db:mediaobject>
<db:imageobject>
<db:imagedata fileref="images/examples_legendmarkers.png"/>
</db:imageobject>
</db:mediaobject>
<db:section xml:id="running-the-example">
<db:title>Running the Example</db:title>
<db:para>To run the example from <db:link xlink:href="http://doc.qt.io/qtcreator/index.html">Qt Creator</db:link>, open the <db:emphasis role="bold">Welcome</db:emphasis> mode and select the example from <db:emphasis role="bold">Examples</db:emphasis>. For more information, visit <db:link xlink:href="http://doc.qt.io/qtcreator/creator-build-example-application.html">Building and Running an Example</db:link>.</db:para>
</db:section>
<db:section xml:id="using-legend-markers">
<db:title>Using Legend Markers</db:title>
<db:para>Our application has buttons for adding or removing a series in a chart and a button to connect or disconnect the legend markers clicked signal to our handler. In the image above we have connected the markers and clicked on one of them.</db:para>
<db:programlisting language="cpp">    // Connect all markers to handler
    foreach (QLegendMarker* marker, m_chart-&gt;legend()-&gt;markers()) {
        // Disconnect possible existing connection to avoid multiple connections
        QObject::disconnect(marker, SIGNAL(clicked()), this, SLOT(handleMarkerClicked()));
        QObject::connect(marker, SIGNAL(clicked()), this, SLOT(handleMarkerClicked()));
    }
</db:programlisting>
<db:para>Here we connect the markers in the legend to our handler. To avoid connecting the same marker more than once, we first disconnect it.</db:para>
<db:programlisting language="cpp">    foreach (QLegendMarker* marker, m_chart-&gt;legend()-&gt;markers()) {
        QObject::disconnect(marker, SIGNAL(clicked()), this, SLOT(handleMarkerClicked()));
    }
</db:programlisting>
<db:para>Here we disconnect all markers from our handler.</db:para>
<db:programlisting language="cpp">    QLegendMarker* marker = qobject_cast&lt;QLegendMarker*&gt; (sender());
    Q_ASSERT(marker);
</db:programlisting>
<db:para>In our handler we first cast the sender of the event to the QLegendMarker.</db:para>
<db:programlisting language="cpp">    switch (marker-&gt;type())
</db:programlisting>
<db:para>Then we check the type of the marker. This is required if we want to access the detailed methods of the marker and cast it to the correct type. If all we need is the pointer to QAbstractSeries, the casting isn't necessary. In case of a pie or bar series, we may need the pointer to a related QPieSlice or QBarSet.</db:para>
<db:programlisting language="cpp">        // Toggle visibility of series
        marker-&gt;series()-&gt;setVisible(!marker-&gt;series()-&gt;isVisible());

        // Turn legend marker back to visible, since hiding series also hides the marker
        // and we don't want it to happen now.
        marker-&gt;setVisible(true);
</db:programlisting>
<db:para>We want to toggle the visibility of the series, when the marker is clicked. To do so, we get the pointer to a related series from the marker and toggle its visibility. Since the legend marker follows the visibility of the series by default, we also set the marked back to visible. If we don't do so, the marker will be invisible in the legend and we can't click on it anymore.</db:para>
<db:programlisting language="cpp">        // Dim the marker, if series is not visible
        qreal alpha = 1.0;

        if (!marker-&gt;series()-&gt;isVisible()) {
            alpha = 0.5;
        }

        QColor color;
        QBrush brush = marker-&gt;labelBrush();
        color = brush.color();
        color.setAlphaF(alpha);
        brush.setColor(color);
        marker-&gt;setLabelBrush(brush);

        brush = marker-&gt;brush();
        color = brush.color();
        color.setAlphaF(alpha);
        brush.setColor(color);
        marker-&gt;setBrush(brush);

        QPen pen = marker-&gt;pen();
        color = pen.color();
        color.setAlphaF(alpha);
        pen.setColor(color);
        marker-&gt;setPen(pen);

</db:programlisting>
<db:para>Instead of making the marker invisible when the series is hidden, we dim the color of the marker. Here we do it by modifying the color of the laberBrush.</db:para>
</db:section>
<db:section>
<db:title>List of Files</db:title>
<db:para>Files:</db:para>
<db:section>
<db:title>List of Files</db:title>
<db:itemizedlist>
<db:listitem>
<db:para><db:link xlink:href="legendmarkers/legendmarkers.pro">legendmarkers/legendmarkers.pro</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="legendmarkers/main.cpp">legendmarkers/main.cpp</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="legendmarkers/mainwidget.cpp">legendmarkers/mainwidget.cpp</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="legendmarkers/mainwidget.h">legendmarkers/mainwidget.h</db:link></db:para></db:listitem>
</db:itemizedlist>
</db:section>
</db:section></db:article>
