<?xml version="1.0" encoding="UTF-8"?>
<db:article xmlns:db="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink" version="5.2" xml:lang="en_US">
<db:info>
<db:title>Diagram Scene Example</db:title>
<db:productname>QtWidgets</db:productname>
<db:titleabbrev>Qt Widgets Reference Documentation</db:titleabbrev>
<db:abstract>
<db:para>Demonstrate how to use the Graphics View framework.</db:para>
</db:abstract>
<db:pubdate>2025-06-29</db:pubdate>
<db:date>2025-06-29</db:date>
<db:authorgroup>
<db:author>
<db:orgname class="corporation">The Qt Company Ltd.</db:orgname>
</db:author>
</db:authorgroup>
</db:info>
<db:mediaobject>
<db:imageobject>
<db:imagedata fileref="images/diagramscene.png"/>
</db:imageobject>
</db:mediaobject>
<db:para>The Diagram Scene example is an application in which you can create a flowchart diagram. It is possible to add flowchart shapes and text and connect the shapes by arrows as shown in the image above. The shapes, arrows, and text can be given different colors, and it is possible to change the font, style, and underline of the text.</db:para>
<db:para>The Qt graphics view framework is designed to manage and display custom 2D graphics items. The main classes of the framework are <db:link xlink:href="qgraphicsitem.xml">QGraphicsItem</db:link>, <db:link xlink:href="qgraphicsscene.xml">QGraphicsScene</db:link> and <db:link xlink:href="qgraphicsview.xml">QGraphicsView</db:link>. The graphics scene manages the items and provides a surface for them. <db:link xlink:href="qgraphicsview.xml">QGraphicsView</db:link> is a widget that is used to render a scene on the screen. See the <db:link xlink:href="graphicsview.xml">Graphics View Framework</db:link> for a more detailed description of the framework.</db:para>
<db:para>In this example we show how to create such custom graphics scenes and items by implementing classes that inherit <db:link xlink:href="qgraphicsscene.xml">QGraphicsScene</db:link> and <db:link xlink:href="qgraphicsitem.xml">QGraphicsItem</db:link>.</db:para>
<db:para>In particular we show how to:</db:para>
<db:itemizedlist>
<db:listitem>
<db:para>Create custom graphics items.</db:para>
</db:listitem>
<db:listitem>
<db:para>Handle mouse events and movement of items.</db:para>
</db:listitem>
<db:listitem>
<db:para>Implement a graphics scene that can manage our custom items.</db:para>
</db:listitem>
<db:listitem>
<db:para>Custom painting of items.</db:para>
</db:listitem>
<db:listitem>
<db:para>Create a movable and editable text item.</db:para>
</db:listitem>
</db:itemizedlist>
<db:para>The example consists of the following classes:</db:para>
<db:itemizedlist>
<db:listitem>
<db:para><db:code>MainWindow</db:code> creates the widgets and display them in a <db:link xlink:href="qmainwindow.xml">QMainWindow</db:link>. It also manages the interaction between the widgets and the graphics scene, view and items.</db:para>
</db:listitem>
<db:listitem>
<db:para><db:code>DiagramItem</db:code> inherits <db:link xlink:href="qgraphicspolygonitem.xml">QGraphicsPolygonItem</db:link> and represents a flowchart shape.</db:para>
</db:listitem>
<db:listitem>
<db:para><db:code>TextDiagramItem</db:code> inherits <db:link xlink:href="qgraphicstextitem.xml">QGraphicsTextItem</db:link> and represents text items in the diagram. The class adds support for moving the item with the mouse, which is not supported by <db:link xlink:href="qgraphicstextitem.xml">QGraphicsTextItem</db:link>.</db:para>
</db:listitem>
<db:listitem>
<db:para><db:code>Arrow</db:code> inherits <db:link xlink:href="qgraphicslineitem.xml">QGraphicsLineItem</db:link> and is an arrow that connect two DiagramItems.</db:para>
</db:listitem>
<db:listitem>
<db:para><db:code>DiagramScene</db:code> inherits QGraphicsDiagramScene and provides support for <db:code>DiagramItem</db:code>, <db:code>Arrow</db:code> and <db:code>DiagramTextItem</db:code> (In addition to the support already handled by <db:link xlink:href="qgraphicsscene.xml">QGraphicsScene</db:link>).</db:para>
</db:listitem>
</db:itemizedlist>
<db:section xml:id="mainwindow-class-definition">
<db:title>MainWindow Class Definition</db:title>
<db:programlisting language="cpp">class MainWindow : public QMainWindow
{
    Q_OBJECT

public:
   MainWindow();

private slots:
    void backgroundButtonGroupClicked(QAbstractButton *button);
    void buttonGroupClicked(int id);
    void deleteItem();
    void pointerGroupClicked(int id);
    void bringToFront();
    void sendToBack();
    void itemInserted(DiagramItem *item);
    void textInserted(QGraphicsTextItem *item);
    void currentFontChanged(const QFont &amp;font);
    void fontSizeChanged(const QString &amp;size);
    void sceneScaleChanged(const QString &amp;scale);
    void textColorChanged();
    void itemColorChanged();
    void lineColorChanged();
    void textButtonTriggered();
    void fillButtonTriggered();
    void lineButtonTriggered();
    void handleFontChange();
    void itemSelected(QGraphicsItem *item);
    void about();

private:
    void createToolBox();
    void createActions();
    void createMenus();
    void createToolbars();
    QWidget *createBackgroundCellWidget(const QString &amp;text,
                                        const QString &amp;image);
    QWidget *createCellWidget(const QString &amp;text,
                              DiagramItem::DiagramType type);
    QMenu *createColorMenu(const char *slot, QColor defaultColor);
    QIcon createColorToolButtonIcon(const QString &amp;image, QColor color);
    QIcon createColorIcon(QColor color);

    DiagramScene *scene;
    QGraphicsView *view;

    QAction *exitAction;
    QAction *addAction;
    QAction *deleteAction;

    QAction *toFrontAction;
    QAction *sendBackAction;
    QAction *aboutAction;

    QMenu *fileMenu;
    QMenu *itemMenu;
    QMenu *aboutMenu;

    QToolBar *textToolBar;
    QToolBar *editToolBar;
    QToolBar *colorToolBar;
    QToolBar *pointerToolbar;

    QComboBox *sceneScaleCombo;
    QComboBox *itemColorCombo;
    QComboBox *textColorCombo;
    QComboBox *fontSizeCombo;
    QFontComboBox *fontCombo;

    QToolBox *toolBox;
    QButtonGroup *buttonGroup;
    QButtonGroup *pointerTypeGroup;
    QButtonGroup *backgroundButtonGroup;
    QToolButton *fontColorToolButton;
    QToolButton *fillColorToolButton;
    QToolButton *lineColorToolButton;
    QAction *boldAction;
    QAction *underlineAction;
    QAction *italicAction;
    QAction *textAction;
    QAction *fillAction;
    QAction *lineAction;
};
</db:programlisting>
<db:para>The <db:code>MainWindow</db:code> class creates and lays out the widgets in a <db:link xlink:href="qmainwindow.xml">QMainWindow</db:link>. The class forwards input from the widgets to the DiagramScene. It also updates its widgets when the diagram scene's text item changes, or a diagram item or a diagram text item is inserted into the scene.</db:para>
<db:para>The class also deletes items from the scene and handles the z-ordering, which decides the order in which items are drawn when they overlap each other.</db:para>
</db:section>
<db:section xml:id="mainwindow-class-implementation">
<db:title>MainWindow Class Implementation</db:title>
<db:para>We start with a look at the constructor:</db:para>
<db:programlisting language="cpp">MainWindow::MainWindow()
{
    createActions();
    createToolBox();
    createMenus();

    scene = new DiagramScene(itemMenu, this);
    scene-&gt;setSceneRect(QRectF(0, 0, 5000, 5000));
    connect(scene, SIGNAL(itemInserted(DiagramItem*)),
            this, SLOT(itemInserted(DiagramItem*)));
    connect(scene, SIGNAL(textInserted(QGraphicsTextItem*)),
            this, SLOT(textInserted(QGraphicsTextItem*)));
    connect(scene, SIGNAL(itemSelected(QGraphicsItem*)),
            this, SLOT(itemSelected(QGraphicsItem*)));
    createToolbars();

    QHBoxLayout *layout = new QHBoxLayout;
    layout-&gt;addWidget(toolBox);
    view = new QGraphicsView(scene);
    layout-&gt;addWidget(view);

    QWidget *widget = new QWidget;
    widget-&gt;setLayout(layout);

    setCentralWidget(widget);
    setWindowTitle(tr(&quot;Diagramscene&quot;));
    setUnifiedTitleAndToolBarOnMac(true);
}
</db:programlisting>
<db:para>In the constructor we call methods to create the widgets and layouts of the example before we create the diagram scene. The toolbars must be created after the scene as they connect to its signals. We then lay the widgets out in the window.</db:para>
<db:para>We connect to the <db:code>itemInserted()</db:code> and <db:code>textInserted()</db:code> slots of the diagram scenes as we want to uncheck the buttons in the tool box when an item is inserted. When an item is selected in the scene we receive the <db:code>itemSelected()</db:code> signal. We use this to update the widgets that display font properties if the item selected is a <db:code>DiagramTextItem</db:code>.</db:para>
<db:para>The <db:code>createToolBox()</db:code> function creates and lays out the widgets of the <db:code>toolBox</db:code> <db:link xlink:href="qtoolbox.xml">QToolBox</db:link>. We will not examine it with a high level of detail as it does not deal with graphics framework specific functionality. Here is its implementation:</db:para>
<db:programlisting language="cpp">void MainWindow::createToolBox()
{
    buttonGroup = new QButtonGroup(this);
    buttonGroup-&gt;setExclusive(false);
    connect(buttonGroup, SIGNAL(buttonClicked(int)),
            this, SLOT(buttonGroupClicked(int)));
    QGridLayout *layout = new QGridLayout;
    layout-&gt;addWidget(createCellWidget(tr(&quot;Conditional&quot;), DiagramItem::Conditional), 0, 0);
    layout-&gt;addWidget(createCellWidget(tr(&quot;Process&quot;), DiagramItem::Step),0, 1);
    layout-&gt;addWidget(createCellWidget(tr(&quot;Input/Output&quot;), DiagramItem::Io), 1, 0);
</db:programlisting>
<db:para>This part of the function sets up the tabbed widget item that contains the flowchart shapes. An exclusive <db:link xlink:href="qbuttongroup.xml">QButtonGroup</db:link> always keeps one button checked; we want the group to allow all buttons to be unchecked. We still use a button group since we can associate user data, which we use to store the diagram type, with each button. The <db:code>createCellWidget()</db:code> function sets up the buttons in the tabbed widget item and is examined later.</db:para>
<db:para>The buttons of the background tabbed widget item is set up in the same way, so we skip to the creation of the tool box:</db:para>
<db:programlisting language="cpp">    toolBox = new QToolBox;
    toolBox-&gt;setSizePolicy(QSizePolicy(QSizePolicy::Maximum, QSizePolicy::Ignored));
    toolBox-&gt;setMinimumWidth(itemWidget-&gt;sizeHint().width());
    toolBox-&gt;addItem(itemWidget, tr(&quot;Basic Flowchart Shapes&quot;));
    toolBox-&gt;addItem(backgroundWidget, tr(&quot;Backgrounds&quot;));
}
</db:programlisting>
<db:para>We set the preferred size of the toolbox as its maximum. This way, more space is given to the graphics view.</db:para>
<db:para>Here is the <db:code>createActions()</db:code> function:</db:para>
<db:programlisting language="cpp">void MainWindow::createActions()
{
    toFrontAction = new QAction(QIcon(&quot;:/images/bringtofront.png&quot;),
                                tr(&quot;Bring to &amp;Front&quot;), this);
    toFrontAction-&gt;setShortcut(tr(&quot;Ctrl+F&quot;));
    toFrontAction-&gt;setStatusTip(tr(&quot;Bring item to front&quot;));
    connect(toFrontAction, SIGNAL(triggered()), this, SLOT(bringToFront()));
</db:programlisting>
<db:para>We show an example of the creation of an action. The functionality the actions trigger is discussed in the slots we connect the actions to. You can see the <db:link xlink:href="qtwidgets-mainwindows-application-example.xml">application example</db:link> if you need a high-level introduction to actions.</db:para>
<db:para>The is the <db:code>createMenus()</db:code> function:</db:para>
<db:programlisting language="cpp">void MainWindow::createMenus()
{
    fileMenu = menuBar()-&gt;addMenu(tr(&quot;&amp;File&quot;));
    fileMenu-&gt;addAction(exitAction);

    itemMenu = menuBar()-&gt;addMenu(tr(&quot;&amp;Item&quot;));
    itemMenu-&gt;addAction(deleteAction);
    itemMenu-&gt;addSeparator();
    itemMenu-&gt;addAction(toFrontAction);
    itemMenu-&gt;addAction(sendBackAction);

    aboutMenu = menuBar()-&gt;addMenu(tr(&quot;&amp;Help&quot;));
    aboutMenu-&gt;addAction(aboutAction);
}
</db:programlisting>
<db:para>We create the three menus' of the example.</db:para>
<db:para>The <db:code>createToolbars()</db:code> function sets up the examples tool bars. The three <db:link xlink:href="qtoolbutton.xml">QToolButton</db:link>s in the <db:code>colorToolBar</db:code>, the <db:code>fontColorToolButton</db:code>, <db:code>fillColorToolButton</db:code>, and <db:code>lineColorToolButton</db:code>, are interesting as we create icons for them by drawing on a <db:link xlink:href="qpixmap.xml">QPixmap</db:link> with a <db:link xlink:href="qpainter.xml">QPainter</db:link>. We show how the <db:code>fillColorToolButton</db:code> is created. This button lets the user select a color for the diagram items.</db:para>
<db:programlisting language="cpp">void MainWindow::createToolbars()
{
    ...
    fillColorToolButton = new QToolButton;
    fillColorToolButton-&gt;setPopupMode(QToolButton::MenuButtonPopup);
    fillColorToolButton-&gt;setMenu(createColorMenu(SLOT(itemColorChanged()), Qt::white));
    fillAction = fillColorToolButton-&gt;menu()-&gt;defaultAction();
    fillColorToolButton-&gt;setIcon(createColorToolButtonIcon(
                                     &quot;:/images/floodfill.png&quot;, Qt::white));
    connect(fillColorToolButton, SIGNAL(clicked()),
            this, SLOT(fillButtonTriggered()));
</db:programlisting>
<db:para>We set the menu of the tool button with <db:link xlink:href="qtoolbutton.xml#setMenu">setMenu</db:link>(). We need the <db:code>fillAction</db:code> <db:link xlink:href="qaction.xml">QAction</db:link> object to always be pointing to the selected action of the menu. The menu is created with the <db:code>createColorMenu()</db:code> function and, as we shall see later, contains one menu item for each color that the items can have. When the user presses the button, which trigger the <db:link xlink:href="qabstractbutton.xml#clicked">clicked</db:link>() signal, we can set the color of the selected item to the color of <db:code>fillAction</db:code>. It is with <db:code>createColorToolButtonIcon()</db:code> we create the icon for the button.</db:para>
<db:programlisting language="cpp">    ...
}
</db:programlisting>
<db:para>Here is the <db:code>createBackgroundCellWidget()</db:code> function:</db:para>
<db:programlisting language="cpp">QWidget *MainWindow::createBackgroundCellWidget(const QString &amp;text, const QString &amp;image)
{
    QToolButton *button = new QToolButton;
    button-&gt;setText(text);
    button-&gt;setIcon(QIcon(image));
    button-&gt;setIconSize(QSize(50, 50));
    button-&gt;setCheckable(true);
    backgroundButtonGroup-&gt;addButton(button);

    QGridLayout *layout = new QGridLayout;
    layout-&gt;addWidget(button, 0, 0, Qt::AlignHCenter);
    layout-&gt;addWidget(new QLabel(text), 1, 0, Qt::AlignCenter);

    QWidget *widget = new QWidget;
    widget-&gt;setLayout(layout);

    return widget;
}
</db:programlisting>
<db:para>This function creates <db:link xlink:href="qwidget.xml">QWidget</db:link>s containing a tool button and a label. The widgets created with this function are used for the background tabbed widget item in the tool box.</db:para>
<db:para>Here is the <db:code>createCellWidget()</db:code> function:</db:para>
<db:programlisting language="cpp">QWidget *MainWindow::createCellWidget(const QString &amp;text, DiagramItem::DiagramType type)
{

    DiagramItem item(type, itemMenu);
    QIcon icon(item.image());

    QToolButton *button = new QToolButton;
    button-&gt;setIcon(icon);
    button-&gt;setIconSize(QSize(50, 50));
    button-&gt;setCheckable(true);
    buttonGroup-&gt;addButton(button, int(type));

    QGridLayout *layout = new QGridLayout;
    layout-&gt;addWidget(button, 0, 0, Qt::AlignHCenter);
    layout-&gt;addWidget(new QLabel(text), 1, 0, Qt::AlignCenter);

    QWidget *widget = new QWidget;
    widget-&gt;setLayout(layout);

    return widget;
}
</db:programlisting>
<db:para>This function returns a <db:link xlink:href="qwidget.xml">QWidget</db:link> containing a <db:link xlink:href="qtoolbutton.xml">QToolButton</db:link> with an image of one of the <db:code>DiagramItems</db:code>, i.e., flowchart shapes. The image is created by the <db:code>DiagramItem</db:code> through the <db:code>image()</db:code> function. The <db:link xlink:href="qbuttongroup.xml">QButtonGroup</db:link> class lets us attach an id (int) with each button; we store the diagram's type, i.e., the DiagramItem::DiagramType enum. We use the stored diagram type when we create new diagram items for the scene. The widgets created with this function is used in the tool box.</db:para>
<db:para>Here is the <db:code>createColorMenu()</db:code> function:</db:para>
<db:programlisting language="cpp">QMenu *MainWindow::createColorMenu(const char *slot, QColor defaultColor)
{
    QList&lt;QColor&gt; colors;
    colors &lt;&lt; Qt::black &lt;&lt; Qt::white &lt;&lt; Qt::red &lt;&lt; Qt::blue &lt;&lt; Qt::yellow;
    QStringList names;
    names &lt;&lt; tr(&quot;black&quot;) &lt;&lt; tr(&quot;white&quot;) &lt;&lt; tr(&quot;red&quot;) &lt;&lt; tr(&quot;blue&quot;)
          &lt;&lt; tr(&quot;yellow&quot;);

    QMenu *colorMenu = new QMenu(this);
    for (int i = 0; i &lt; colors.count(); ++i) {
        QAction *action = new QAction(names.at(i), this);
        action-&gt;setData(colors.at(i));
        action-&gt;setIcon(createColorIcon(colors.at(i)));
        connect(action, SIGNAL(triggered()), this, slot);
        colorMenu-&gt;addAction(action);
        if (colors.at(i) == defaultColor)
            colorMenu-&gt;setDefaultAction(action);
    }
    return colorMenu;
}
</db:programlisting>
<db:para>This function creates a color menu that is used as the drop-down menu for the tool buttons in the <db:code>colorToolBar</db:code>. We create an action for each color that we add to the menu. We fetch the actions data when we set the color of items, lines, and text.</db:para>
<db:para>Here is the <db:code>createColorToolButtonIcon()</db:code> function:</db:para>
<db:programlisting language="cpp">QIcon MainWindow::createColorToolButtonIcon(const QString &amp;imageFile, QColor color)
{
    QPixmap pixmap(50, 80);
    pixmap.fill(Qt::transparent);
    QPainter painter(&amp;pixmap);
    QPixmap image(imageFile);
    // Draw icon centred horizontally on button.
    QRect target(4, 0, 42, 43);
    QRect source(0, 0, 42, 43);
    painter.fillRect(QRect(0, 60, 50, 80), color);
    painter.drawPixmap(target, image, source);

    return QIcon(pixmap);
}
</db:programlisting>
<db:para>This function is used to create the <db:link xlink:href="qicon.xml">QIcon</db:link> of the <db:code>fillColorToolButton</db:code>, <db:code>fontColorToolButton</db:code>, and <db:code>lineColorToolButton</db:code>. The <db:code role="parameter">imageFile</db:code> string is either the text, flood-fill, or line symbol that is used for the buttons. Beneath the image we draw a filled rectangle using <db:code role="parameter">color</db:code>.</db:para>
<db:para>Here is the <db:code>createColorIcon()</db:code> function:</db:para>
<db:programlisting language="cpp">QIcon MainWindow::createColorIcon(QColor color)
{
    QPixmap pixmap(20, 20);
    QPainter painter(&amp;pixmap);
    painter.setPen(Qt::NoPen);
    painter.fillRect(QRect(0, 0, 20, 20), color);

    return QIcon(pixmap);
}
</db:programlisting>
<db:para>This function creates an icon with a filled rectangle in the color of <db:code role="parameter">color</db:code>. It is used for creating icons for the color menus in the <db:code>fillColorToolButton</db:code>, <db:code>fontColorToolButton</db:code>, and <db:code>lineColorToolButton</db:code>.</db:para>
<db:para>Here is the <db:code>backgroundButtonGroupClicked()</db:code> slot:</db:para>
<db:programlisting language="cpp">void MainWindow::backgroundButtonGroupClicked(QAbstractButton *button)
{
    QList&lt;QAbstractButton *&gt; buttons = backgroundButtonGroup-&gt;buttons();
    foreach (QAbstractButton *myButton, buttons) {
        if (myButton != button)
            button-&gt;setChecked(false);
    }
    QString text = button-&gt;text();
    if (text == tr(&quot;Blue Grid&quot;))
        scene-&gt;setBackgroundBrush(QPixmap(&quot;:/images/background1.png&quot;));
    else if (text == tr(&quot;White Grid&quot;))
        scene-&gt;setBackgroundBrush(QPixmap(&quot;:/images/background2.png&quot;));
    else if (text == tr(&quot;Gray Grid&quot;))
        scene-&gt;setBackgroundBrush(QPixmap(&quot;:/images/background3.png&quot;));
    else
        scene-&gt;setBackgroundBrush(QPixmap(&quot;:/images/background4.png&quot;));

    scene-&gt;update();
    view-&gt;update();
}
</db:programlisting>
<db:para>In this function we set the <db:link xlink:href="qbrush.xml">QBrush</db:link> that is used to draw the background of the diagramscene. The background can be a grid of squares of blue, gray, or white tiles, or no grid at all. We have <db:link xlink:href="qpixmap.xml">QPixmap</db:link>s of the tiles from png files that we create the brush with.</db:para>
<db:para>When one of the buttons in the background tabbed widget item is clicked we change the brush; we find out which button it is by checking its text.</db:para>
<db:para>Here is the implementation of <db:code>buttonGroupClicked()</db:code>:</db:para>
<db:programlisting language="cpp">void MainWindow::buttonGroupClicked(int id)
{
    QList&lt;QAbstractButton *&gt; buttons = buttonGroup-&gt;buttons();
    foreach (QAbstractButton *button, buttons) {
        if (buttonGroup-&gt;button(id) != button)
            button-&gt;setChecked(false);
    }
    if (id == InsertTextButton) {
        scene-&gt;setMode(DiagramScene::InsertText);
    } else {
        scene-&gt;setItemType(DiagramItem::DiagramType(id));
        scene-&gt;setMode(DiagramScene::InsertItem);
    }
}
</db:programlisting>
<db:para>This slot is called when a button in <db:code>buttonGroup</db:code> is checked. When a button is checked the user can click on the graphics view and a <db:code>DiagramItem</db:code> of the selected type will be inserted into the <db:code>DiagramScene</db:code>. We must loop through the buttons in the group to uncheck other buttons as only one button is allowed to be checked at a time.</db:para>
<db:para><db:code>QButtonGroup</db:code> assigns an id to each button. We have set the id of each button to the diagram type, as given by DiagramItem::DiagramType that will be inserted into the scene when it is clicked. We can then use the button id when we set the diagram type with <db:code>setItemType()</db:code>. In the case of text we assigned an id that has a value that is not in the DiagramType enum.</db:para>
<db:para>Here is the implementation of <db:code>deleteItem()</db:code>:</db:para>
<db:programlisting language="cpp">void MainWindow::deleteItem()
{
    foreach (QGraphicsItem *item, scene-&gt;selectedItems()) {
        if (item-&gt;type() == Arrow::Type) {
            scene-&gt;removeItem(item);
            Arrow *arrow = qgraphicsitem_cast&lt;Arrow *&gt;(item);
            arrow-&gt;startItem()-&gt;removeArrow(arrow);
            arrow-&gt;endItem()-&gt;removeArrow(arrow);
            delete item;
        }
    }

    foreach (QGraphicsItem *item, scene-&gt;selectedItems()) {
         if (item-&gt;type() == DiagramItem::Type)
             qgraphicsitem_cast&lt;DiagramItem *&gt;(item)-&gt;removeArrows();
         scene-&gt;removeItem(item);
         delete item;
     }
}
</db:programlisting>
<db:para>This slot deletes the selected item, if any, from the scene. It deletes the arrows first in order to avoid to delete them twice. If the item to be deleted is a <db:code>DiagramItem</db:code>, we also need to delete arrows connected to it; we don't want arrows in the scene that aren't connected to items in both ends.</db:para>
<db:para>This is the implementation of pointerGroupClicked():</db:para>
<db:programlisting language="cpp">void MainWindow::pointerGroupClicked(int)
{
    scene-&gt;setMode(DiagramScene::Mode(pointerTypeGroup-&gt;checkedId()));
}
</db:programlisting>
<db:para>The <db:code>pointerTypeGroup</db:code> decides whether the scene is in ItemMove or InsertLine mode. This button group is exclusive, i.e., only one button is checked at any time. As with the <db:code>buttonGroup</db:code> above we have assigned an id to the buttons that matches values of the DiagramScene::Mode enum, so that we can use the id to set the correct mode.</db:para>
<db:para>Here is the <db:code>bringToFront()</db:code> slot:</db:para>
<db:programlisting language="cpp">void MainWindow::bringToFront()
{
    if (scene-&gt;selectedItems().isEmpty())
        return;

    QGraphicsItem *selectedItem = scene-&gt;selectedItems().first();
    QList&lt;QGraphicsItem *&gt; overlapItems = selectedItem-&gt;collidingItems();

    qreal zValue = 0;
    foreach (QGraphicsItem *item, overlapItems) {
        if (item-&gt;zValue() &gt;= zValue &amp;&amp; item-&gt;type() == DiagramItem::Type)
            zValue = item-&gt;zValue() + 0.1;
    }
    selectedItem-&gt;setZValue(zValue);
}
</db:programlisting>
<db:para>Several items may collide, i.e., overlap, with each other in the scene. This slot is called when the user requests that an item should be placed on top of the items it collides with. <db:link xlink:href="qgraphicsitem.xml">QGrapicsItems</db:link> have a z-value that decides the order in which items are stacked in the scene; you can think of it as the z-axis in a 3D coordinate system. When items collide the items with higher z-values will be drawn on top of items with lower values. When we bring an item to the front we can loop through the items it collides with and set a z-value that is higher than all of them.</db:para>
<db:para>Here is the <db:code>sendToBack()</db:code> slot:</db:para>
<db:programlisting language="cpp">void MainWindow::sendToBack()
{
    if (scene-&gt;selectedItems().isEmpty())
        return;

    QGraphicsItem *selectedItem = scene-&gt;selectedItems().first();
    QList&lt;QGraphicsItem *&gt; overlapItems = selectedItem-&gt;collidingItems();

    qreal zValue = 0;
    foreach (QGraphicsItem *item, overlapItems) {
        if (item-&gt;zValue() &lt;= zValue &amp;&amp; item-&gt;type() == DiagramItem::Type)
            zValue = item-&gt;zValue() - 0.1;
    }
    selectedItem-&gt;setZValue(zValue);
}
</db:programlisting>
<db:para>This slot works in the same way as <db:code>bringToFront()</db:code> described above, but sets a z-value that is lower than items the item that should be send to the back collides with.</db:para>
<db:para>This is the implementation of <db:code>itemInserted()</db:code>:</db:para>
<db:programlisting language="cpp">void MainWindow::itemInserted(DiagramItem *item)
{
    pointerTypeGroup-&gt;button(int(DiagramScene::MoveItem))-&gt;setChecked(true);
    scene-&gt;setMode(DiagramScene::Mode(pointerTypeGroup-&gt;checkedId()));
    buttonGroup-&gt;button(int(item-&gt;diagramType()))-&gt;setChecked(false);
}
</db:programlisting>
<db:para>This slot is called from the <db:code>DiagramScene</db:code> when an item has been added to the scene. We set the mode of the scene back to the mode before the item was inserted, which is ItemMove or InsertText depending on which button is checked in the <db:code>pointerTypeGroup</db:code>. We must also uncheck the button in the in the <db:code>buttonGroup</db:code>.</db:para>
<db:para>Here is the implementation of <db:code>textInserted()</db:code>:</db:para>
<db:programlisting language="cpp">void MainWindow::textInserted(QGraphicsTextItem *)
{
    buttonGroup-&gt;button(InsertTextButton)-&gt;setChecked(false);
    scene-&gt;setMode(DiagramScene::Mode(pointerTypeGroup-&gt;checkedId()));
}
</db:programlisting>
<db:para>We simply set the mode of the scene back to the mode it had before the text was inserted.</db:para>
<db:para>Here is the <db:code>currentFontChanged()</db:code> slot:</db:para>
<db:programlisting language="cpp">void MainWindow::currentFontChanged(const QFont &amp;)
{
    handleFontChange();
}
</db:programlisting>
<db:para>When the user requests a font change, by using one of the widgets in the <db:code>fontToolBar</db:code>, we create a new <db:link xlink:href="qfont.xml">QFont</db:link> object and set its properties to match the state of the widgets. This is done in <db:code>handleFontChange()</db:code>, so we simply call that slot.</db:para>
<db:para>Here is the <db:code>fontSizeChanged()</db:code> slot:</db:para>
<db:programlisting language="cpp">void MainWindow::fontSizeChanged(const QString &amp;)
{
    handleFontChange();
}
</db:programlisting>
<db:para>When the user requests a font change, by using one of the widgets in the <db:code>fontToolBar</db:code>, we create a new <db:link xlink:href="qfont.xml">QFont</db:link> object and set its properties to match the state of the widgets. This is done in <db:code>handleFontChange()</db:code>, so we simply call that slot.</db:para>
<db:para>Here is the implementation of <db:code>sceneScaleChanged()</db:code>:</db:para>
<db:programlisting language="cpp">void MainWindow::sceneScaleChanged(const QString &amp;scale)
{
    double newScale = scale.left(scale.indexOf(tr(&quot;%&quot;))).toDouble() / 100.0;
    QMatrix oldMatrix = view-&gt;matrix();
    view-&gt;resetMatrix();
    view-&gt;translate(oldMatrix.dx(), oldMatrix.dy());
    view-&gt;scale(newScale, newScale);
}
</db:programlisting>
<db:para>The user can increase or decrease the scale, with the <db:code>sceneScaleCombo</db:code>, the scene is drawn in. It is not the scene itself that changes its scale, but only the view.</db:para>
<db:para>Here is the <db:code>textColorChanged()</db:code> slot:</db:para>
<db:programlisting language="cpp">void MainWindow::textColorChanged()
{
    textAction = qobject_cast&lt;QAction *&gt;(sender());
    fontColorToolButton-&gt;setIcon(createColorToolButtonIcon(
                                     &quot;:/images/textpointer.png&quot;,
                                     qvariant_cast&lt;QColor&gt;(textAction-&gt;data())));
    textButtonTriggered();
}
</db:programlisting>
<db:para>This slot is called when an item in the drop-down menu of the <db:code>fontColorToolButton</db:code> is pressed. We need to change the icon on the button to the color of the selected <db:link xlink:href="qaction.xml">QAction</db:link>. We keep a pointer to the selected action in <db:code>textAction</db:code>. It is in <db:code>textButtonTriggered()</db:code> we change the text color to the color of <db:code>textAction</db:code>, so we call that slot.</db:para>
<db:para>Here is the <db:code>itemColorChanged()</db:code> implementation:</db:para>
<db:programlisting language="cpp">void MainWindow::itemColorChanged()
{
    fillAction = qobject_cast&lt;QAction *&gt;(sender());
    fillColorToolButton-&gt;setIcon(createColorToolButtonIcon(
                                     &quot;:/images/floodfill.png&quot;,
                                     qvariant_cast&lt;QColor&gt;(fillAction-&gt;data())));
    fillButtonTriggered();
}
</db:programlisting>
<db:para>This slot handles requests for changing the color of <db:code>DiagramItems</db:code> in the same manner as <db:code>textColorChanged()</db:code> does for <db:code>DiagramTextItems</db:code>.</db:para>
<db:para>Here is the implementation of <db:code>lineColorChanged()</db:code>:</db:para>
<db:programlisting language="cpp">void MainWindow::lineColorChanged()
{
    lineAction = qobject_cast&lt;QAction *&gt;(sender());
    lineColorToolButton-&gt;setIcon(createColorToolButtonIcon(
                                     &quot;:/images/linecolor.png&quot;,
                                     qvariant_cast&lt;QColor&gt;(lineAction-&gt;data())));
    lineButtonTriggered();
}
</db:programlisting>
<db:para>This slot handles requests for changing the color of <db:code>Arrows</db:code> in the same manner that <db:code>textColorChanged()</db:code> does it for <db:code>DiagramTextItems</db:code>.</db:para>
<db:para>Here is the <db:code>textButtonTriggered()</db:code> slot:</db:para>
<db:programlisting language="cpp">void MainWindow::textButtonTriggered()
{
    scene-&gt;setTextColor(qvariant_cast&lt;QColor&gt;(textAction-&gt;data()));
}
</db:programlisting>
<db:para><db:code>textAction</db:code> points to the <db:link xlink:href="qaction.xml">QAction</db:link> of the currently selected menu item in the <db:code>fontColorToolButton</db:code>'s color drop-down menu. We have set the data of the action to the <db:link xlink:href="qcolor.xml">QColor</db:link> the action represents, so we can simply fetch this when we set the color of text with <db:code>setTextColor()</db:code>.</db:para>
<db:para>Here is the <db:code>fillButtonTriggered()</db:code> slot:</db:para>
<db:programlisting language="cpp">void MainWindow::fillButtonTriggered()
{
    scene-&gt;setItemColor(qvariant_cast&lt;QColor&gt;(fillAction-&gt;data()));
}
</db:programlisting>
<db:para><db:code>fillAction</db:code> points to the selected menu item in the drop-down menu of <db:code>fillColorToolButton()</db:code>. We can therefore use the data of this action when we set the item color with <db:code>setItemColor()</db:code>.</db:para>
<db:para>Here is the <db:code>lineButtonTriggered()</db:code> slot:</db:para>
<db:programlisting language="cpp">void MainWindow::lineButtonTriggered()
{
    scene-&gt;setLineColor(qvariant_cast&lt;QColor&gt;(lineAction-&gt;data()));
}
</db:programlisting>
<db:para><db:code>lineAction</db:code> point to the selected item in the drop-down menu of <db:code>lineColorToolButton</db:code>. We use its data when we set the arrow color with <db:code>setLineColor()</db:code>.</db:para>
<db:para>Here is the <db:code>handleFontChange()</db:code> function:</db:para>
<db:programlisting language="cpp">void MainWindow::handleFontChange()
{
    QFont font = fontCombo-&gt;currentFont();
    font.setPointSize(fontSizeCombo-&gt;currentText().toInt());
    font.setWeight(boldAction-&gt;isChecked() ? QFont::Bold : QFont::Normal);
    font.setItalic(italicAction-&gt;isChecked());
    font.setUnderline(underlineAction-&gt;isChecked());

    scene-&gt;setFont(font);
}
</db:programlisting>
<db:para><db:code>handleFontChange()</db:code> is called when any of the widgets that show font properties changes. We create a new <db:link xlink:href="qfont.xml">QFont</db:link> object and set its properties based on the widgets. We then call the <db:code>setFont()</db:code> function of <db:code>DiagramScene</db:code>; it is the scene that set the font of the <db:code>DiagramTextItems</db:code> it manages.</db:para>
<db:para>Here is the <db:code>itemSelected()</db:code> slot:</db:para>
<db:programlisting language="cpp">void MainWindow::itemSelected(QGraphicsItem *item)
{
    DiagramTextItem *textItem =
    qgraphicsitem_cast&lt;DiagramTextItem *&gt;(item);

    QFont font = textItem-&gt;font();
    fontCombo-&gt;setCurrentFont(font);
    fontSizeCombo-&gt;setEditText(QString().setNum(font.pointSize()));
    boldAction-&gt;setChecked(font.weight() == QFont::Bold);
    italicAction-&gt;setChecked(font.italic());
    underlineAction-&gt;setChecked(font.underline());
}
</db:programlisting>
<db:para>This slot is called when an item in the <db:code>DiagramScene</db:code> is selected. In the case of this example it is only text items that emit signals when they are selected, so we do not need to check what kind of graphics <db:code role="parameter">item</db:code> is.</db:para>
<db:para>We set the state of the widgets to match the properties of the font of the selected text item.</db:para>
<db:para>This is the <db:code>about()</db:code> slot:</db:para>
<db:programlisting language="cpp">void MainWindow::about()
{
    QMessageBox::about(this, tr(&quot;About Diagram Scene&quot;),
                       tr(&quot;The &lt;b&gt;Diagram Scene&lt;/b&gt; example shows &quot;
                          &quot;use of the graphics framework.&quot;));
}
</db:programlisting>
<db:para>This slot displays an about box for the example when the user selects the about menu item from the help menu.</db:para>
</db:section>
<db:section xml:id="diagramscene-class-definition">
<db:title>DiagramScene Class Definition</db:title>
<db:para>The <db:code>DiagramScene</db:code> class inherits <db:link xlink:href="qgraphicsscene.xml">QGraphicsScene</db:link> and adds functionality to handle <db:code>DiagramItems</db:code>, <db:code>Arrows</db:code>, and <db:code>DiagramTextItems</db:code> in addition to the items handled by its super class.</db:para>
<db:programlisting language="cpp">class DiagramScene : public QGraphicsScene
{
    Q_OBJECT

public:
    enum Mode { InsertItem, InsertLine, InsertText, MoveItem };

    explicit DiagramScene(QMenu *itemMenu, QObject *parent = 0);
    QFont font() const { return myFont; }
    QColor textColor() const { return myTextColor; }
    QColor itemColor() const { return myItemColor; }
    QColor lineColor() const { return myLineColor; }
    void setLineColor(const QColor &amp;color);
    void setTextColor(const QColor &amp;color);
    void setItemColor(const QColor &amp;color);
    void setFont(const QFont &amp;font);

public slots:
    void setMode(Mode mode);
    void setItemType(DiagramItem::DiagramType type);
    void editorLostFocus(DiagramTextItem *item);

signals:
    void itemInserted(DiagramItem *item);
    void textInserted(QGraphicsTextItem *item);
    void itemSelected(QGraphicsItem *item);

protected:
    void mousePressEvent(QGraphicsSceneMouseEvent *mouseEvent);
    void mouseMoveEvent(QGraphicsSceneMouseEvent *mouseEvent);
    void mouseReleaseEvent(QGraphicsSceneMouseEvent *mouseEvent);

private:
    bool isItemChange(int type);

    DiagramItem::DiagramType myItemType;
    QMenu *myItemMenu;
    Mode myMode;
    bool leftButtonDown;
    QPointF startPoint;
    QGraphicsLineItem *line;
    QFont myFont;
    DiagramTextItem *textItem;
    QColor myTextColor;
    QColor myItemColor;
    QColor myLineColor;
};
</db:programlisting>
<db:para>In the <db:code>DiagramScene</db:code> a mouse click can give three different actions: the item under the mouse can be moved, an item may be inserted, or an arrow may be connected between to diagram items. Which action a mouse click has depends on the mode, given by the Mode enum, the scene is in. The mode is set with the <db:code>setMode()</db:code> function.</db:para>
<db:para>The scene also sets the color of its items and the font of its text items. The colors and font used by the scene can be set with the <db:code>setLineColor()</db:code>, <db:code>setTextColor()</db:code>, <db:code>setItemColor()</db:code> and <db:code>setFont()</db:code> functions. The type of <db:code>DiagramItem</db:code>, given by the DiagramItem::DiagramType function, to be created when an item is inserted is set with the <db:code>setItemType()</db:code> slot.</db:para>
<db:para>The <db:code>MainWindow</db:code> and <db:code>DiagramScene</db:code> share responsibility for the examples functionality. <db:code>MainWindow</db:code> handles the following tasks: the deletion of items, text, and arrows; moving diagram items to the back and front; and setting the scale of the scene.</db:para>
</db:section>
<db:section xml:id="diagramscene-class-implementation">
<db:title>DiagramScene Class Implementation</db:title>
<db:para>We start with the constructor:</db:para>
<db:programlisting language="cpp">DiagramScene::DiagramScene(QMenu *itemMenu, QObject *parent)
    : QGraphicsScene(parent)
{
    myItemMenu = itemMenu;
    myMode = MoveItem;
    myItemType = DiagramItem::Step;
    line = 0;
    textItem = 0;
    myItemColor = Qt::white;
    myTextColor = Qt::black;
    myLineColor = Qt::black;
}
</db:programlisting>
<db:para>The scene uses <db:code>myItemMenu</db:code> to set the context menu when it creates <db:code>DiagramItems</db:code>. We set the default mode to <db:code>DiagramScene::MoveItem</db:code> as this gives the default behavior of <db:link xlink:href="qgraphicsscene.xml">QGraphicsScene</db:link>.</db:para>
<db:para>Here is the <db:code>setLineColor()</db:code> function:</db:para>
<db:programlisting language="cpp">void DiagramScene::setLineColor(const QColor &amp;color)
{
    myLineColor = color;
    if (isItemChange(Arrow::Type)) {
        Arrow *item = qgraphicsitem_cast&lt;Arrow *&gt;(selectedItems().first());
        item-&gt;setColor(myLineColor);
        update();
    }
}
</db:programlisting>
<db:para>The <db:code>isItemChange</db:code> function returns true if an <db:code>Arrow</db:code> item is selected in the scene in which case we want to change its color. When the <db:code>DiagramScene</db:code> creates and adds new arrows to the scene it will also use the new <db:code role="parameter">color</db:code>.</db:para>
<db:para>Here is the <db:code>setTextColor()</db:code> function:</db:para>
<db:programlisting language="cpp">void DiagramScene::setTextColor(const QColor &amp;color)
{
    myTextColor = color;
    if (isItemChange(DiagramTextItem::Type)) {
        DiagramTextItem *item = qgraphicsitem_cast&lt;DiagramTextItem *&gt;(selectedItems().first());
        item-&gt;setDefaultTextColor(myTextColor);
    }
}
</db:programlisting>
<db:para>This function sets the color of <db:code>DiagramTextItems</db:code> equal to the way <db:code>setLineColor()</db:code> sets the color of <db:code>Arrows</db:code>.</db:para>
<db:para>Here is the <db:code>setItemColor()</db:code> function:</db:para>
<db:programlisting language="cpp">void DiagramScene::setItemColor(const QColor &amp;color)
{
    myItemColor = color;
    if (isItemChange(DiagramItem::Type)) {
        DiagramItem *item = qgraphicsitem_cast&lt;DiagramItem *&gt;(selectedItems().first());
        item-&gt;setBrush(myItemColor);
    }
}
</db:programlisting>
<db:para>This function sets the color the scene will use when creating <db:code>DiagramItems</db:code>. It also changes the color of a selected <db:code>DiagramItem</db:code>.</db:para>
<db:para>This is the implementation of <db:code>setFont()</db:code>:</db:para>
<db:programlisting language="cpp">void DiagramScene::setFont(const QFont &amp;font)
{
    myFont = font;

    if (isItemChange(DiagramTextItem::Type)) {
        QGraphicsTextItem *item = qgraphicsitem_cast&lt;DiagramTextItem *&gt;(selectedItems().first());
        //At this point the selection can change so the first selected item might not be a DiagramTextItem
        if (item)
            item-&gt;setFont(myFont);
    }
}
</db:programlisting>
<db:para>Set the font to use for new and selected, if a text item is selected, <db:code>DiagramTextItems</db:code>.</db:para>
<db:para>This is the implementation of <db:code>editorLostFocus()</db:code> slot:</db:para>
<db:programlisting language="cpp">void DiagramScene::editorLostFocus(DiagramTextItem *item)
{
    QTextCursor cursor = item-&gt;textCursor();
    cursor.clearSelection();
    item-&gt;setTextCursor(cursor);

    if (item-&gt;toPlainText().isEmpty()) {
        removeItem(item);
        item-&gt;deleteLater();
    }
}
</db:programlisting>
<db:para><db:code>DiagramTextItems</db:code> emit a signal when they loose focus, which is connected to this slot. We remove the item if it has no text. If not, we would leak memory and confuse the user as the items will be edited when pressed on by the mouse.</db:para>
<db:para>The <db:code>mousePressEvent()</db:code> function handles mouse press event's different depending on which mode the <db:code>DiagramScene</db:code> is in. We examine its implementation for each mode:</db:para>
<db:programlisting language="cpp">void DiagramScene::mousePressEvent(QGraphicsSceneMouseEvent *mouseEvent)
{
    if (mouseEvent-&gt;button() != Qt::LeftButton)
        return;

    DiagramItem *item;
    switch (myMode) {
        case InsertItem:
            item = new DiagramItem(myItemType, myItemMenu);
            item-&gt;setBrush(myItemColor);
            addItem(item);
            item-&gt;setPos(mouseEvent-&gt;scenePos());
            emit itemInserted(item);
            break;
</db:programlisting>
<db:para>We simply create a new <db:code>DiagramItem</db:code> and add it to the scene at the position the mouse was pressed. Note that the origin of its local coordinate system will be under the mouse pointer position.</db:para>
<db:programlisting language="cpp">        case InsertLine:
            line = new QGraphicsLineItem(QLineF(mouseEvent-&gt;scenePos(),
                                        mouseEvent-&gt;scenePos()));
            line-&gt;setPen(QPen(myLineColor, 2));
            addItem(line);
            break;
</db:programlisting>
<db:para>The user adds <db:code>Arrows</db:code> to the scene by stretching a line between the items the arrow should connect. The start of the line is fixed in the place the user clicked the mouse and the end follows the mouse pointer as long as the button is held down. When the user releases the mouse button an <db:code>Arrow</db:code> will be added to the scene if there is a <db:code>DiagramItem</db:code> under the start and end of the line. We will see how this is implemented later; here we simply add the line.</db:para>
<db:programlisting language="cpp">        case InsertText:
            textItem = new DiagramTextItem();
            textItem-&gt;setFont(myFont);
            textItem-&gt;setTextInteractionFlags(Qt::TextEditorInteraction);
            textItem-&gt;setZValue(1000.0);
            connect(textItem, SIGNAL(lostFocus(DiagramTextItem*)),
                    this, SLOT(editorLostFocus(DiagramTextItem*)));
            connect(textItem, SIGNAL(selectedChange(QGraphicsItem*)),
                    this, SIGNAL(itemSelected(QGraphicsItem*)));
            addItem(textItem);
            textItem-&gt;setDefaultTextColor(myTextColor);
            textItem-&gt;setPos(mouseEvent-&gt;scenePos());
            emit textInserted(textItem);
</db:programlisting>
<db:para>The <db:code>DiagramTextItem</db:code> is editable when the <db:link xlink:href="qt.xml#TextInteractionFlag-enum">Qt::TextEditorInteraction</db:link> flag is set, else it is movable by the mouse. We always want the text to be drawn on top of the other items in the scene, so we set the value to a number higher than other items in the scene.</db:para>
<db:programlisting language="cpp">    default:
        ;
    }
    QGraphicsScene::mousePressEvent(mouseEvent);
}
</db:programlisting>
<db:para>We are in MoveItem mode if we get to the default switch; we can then call the <db:link xlink:href="qgraphicsscene.xml">QGraphicsScene</db:link> implementation, which handles movement of items with the mouse. We make this call even if we are in another mode making it possible to add an item and then keep the mouse button pressed down and start moving the item. In the case of text items, this is not possible as they do not propagate mouse events when they are editable.</db:para>
<db:para>This is the <db:code>mouseMoveEvent()</db:code> function:</db:para>
<db:programlisting language="cpp">void DiagramScene::mouseMoveEvent(QGraphicsSceneMouseEvent *mouseEvent)
{
    if (myMode == InsertLine &amp;&amp; line != 0) {
        QLineF newLine(line-&gt;line().p1(), mouseEvent-&gt;scenePos());
        line-&gt;setLine(newLine);
    } else if (myMode == MoveItem) {
        QGraphicsScene::mouseMoveEvent(mouseEvent);
    }
}
</db:programlisting>
<db:para>We must draw the line if we are in InsertMode and the mouse button is pressed down (the line is not 0). As discussed in <db:code>mousePressEvent()</db:code> the line is drawn from the position the mouse was pressed to the current position of the mouse.</db:para>
<db:para>If we are in MoveItem mode, we call the <db:link xlink:href="qgraphicsscene.xml">QGraphicsScene</db:link> implementation, which handles movement of items.</db:para>
<db:para>In the <db:code>mouseReleaseEvent()</db:code> function we need to check if an arrow should be added to the scene:</db:para>
<db:programlisting language="cpp">void DiagramScene::mouseReleaseEvent(QGraphicsSceneMouseEvent *mouseEvent)
{
    if (line != 0 &amp;&amp; myMode == InsertLine) {
        QList&lt;QGraphicsItem *&gt; startItems = items(line-&gt;line().p1());
        if (startItems.count() &amp;&amp; startItems.first() == line)
            startItems.removeFirst();
        QList&lt;QGraphicsItem *&gt; endItems = items(line-&gt;line().p2());
        if (endItems.count() &amp;&amp; endItems.first() == line)
            endItems.removeFirst();

        removeItem(line);
        delete line;
</db:programlisting>
<db:para>First we need to get the items (if any) under the line's start and end points. The line itself is the first item at these points, so we remove it from the lists. As a precaution, we check if the lists are empty, but this should never happen.</db:para>
<db:programlisting language="cpp">        if (startItems.count() &gt; 0 &amp;&amp; endItems.count() &gt; 0 &amp;&amp;
            startItems.first()-&gt;type() == DiagramItem::Type &amp;&amp;
            endItems.first()-&gt;type() == DiagramItem::Type &amp;&amp;
            startItems.first() != endItems.first()) {
            DiagramItem *startItem = qgraphicsitem_cast&lt;DiagramItem *&gt;(startItems.first());
            DiagramItem *endItem = qgraphicsitem_cast&lt;DiagramItem *&gt;(endItems.first());
            Arrow *arrow = new Arrow(startItem, endItem);
            arrow-&gt;setColor(myLineColor);
            startItem-&gt;addArrow(arrow);
            endItem-&gt;addArrow(arrow);
            arrow-&gt;setZValue(-1000.0);
            addItem(arrow);
            arrow-&gt;updatePosition();
        }
    }
</db:programlisting>
<db:para>Now we check if there are two different <db:code>DiagramItems</db:code> under the lines start and end points. If there are we can create an <db:code>Arrow</db:code> with the two items. The arrow is then added to each item and finally the scene. The arrow must be updated to adjust its start and end points to the items. We set the z-value of the arrow to -1000.0 because we always want it to be drawn under the items.</db:para>
<db:programlisting language="cpp">    line = 0;
    QGraphicsScene::mouseReleaseEvent(mouseEvent);
}
</db:programlisting>
<db:para>Here is the <db:code>isItemChange()</db:code> function:</db:para>
<db:programlisting language="cpp">bool DiagramScene::isItemChange(int type)
{
    foreach (QGraphicsItem *item, selectedItems()) {
        if (item-&gt;type() == type)
            return true;
    }
    return false;
}
</db:programlisting>
<db:para>The scene has single selection, i.e., only one item can be selected at any given time. The foreach will then loop one time with the selected item or none if no item is selected. <db:code>isItemChange()</db:code> is used to check whether a selected item exists and also is of the specified diagram <db:code role="parameter">type</db:code>.</db:para>
</db:section>
<db:section xml:id="diagramitem-class-definition">
<db:title>DiagramItem Class Definition</db:title>
<db:programlisting language="cpp">class DiagramItem : public QGraphicsPolygonItem
{
public:
    enum { Type = UserType + 15 };
    enum DiagramType { Step, Conditional, StartEnd, Io };

    DiagramItem(DiagramType diagramType, QMenu *contextMenu, QGraphicsItem *parent = 0);

    void removeArrow(Arrow *arrow);
    void removeArrows();
    DiagramType diagramType() const { return myDiagramType; }
    QPolygonF polygon() const { return myPolygon; }
    void addArrow(Arrow *arrow);
    QPixmap image() const;
    int type() const { return Type;}

protected:
    void contextMenuEvent(QGraphicsSceneContextMenuEvent *event);
    QVariant itemChange(GraphicsItemChange change, const QVariant &amp;value);

private:
    DiagramType myDiagramType;
    QPolygonF myPolygon;
    QMenu *myContextMenu;
    QList&lt;Arrow *&gt; arrows;
};
</db:programlisting>
<db:para>The <db:code>DiagramItem</db:code> represents a flowchart shape in the <db:code>DiagramScene</db:code>. It inherits <db:link xlink:href="qgraphicspolygonitem.xml">QGraphicsPolygonItem</db:link> and has a polygon for each shape. The enum DiagramType has a value for each of the flowchart shapes.</db:para>
<db:para>The class has a list of the arrows that are connected to it. This is necessary because only the item knows when it is being moved (with the <db:code>itemChanged()</db:code> function) at which time the arrows must be updated. The item can also draw itself onto a <db:link xlink:href="qpixmap.xml">QPixmap</db:link> with the <db:code>image()</db:code> function. This is used for the tool buttons in <db:code>MainWindow</db:code>, see <db:code>createColorToolButtonIcon()</db:code> in <db:code>MainWindow</db:code>.</db:para>
<db:para>The Type enum is a unique identifier of the class. It is used by <db:code>qgraphicsitem_cast()</db:code>, which does dynamic casts of graphics items. The UserType constant is the minimum value a custom graphics item type can be.</db:para>
</db:section>
<db:section xml:id="diagramitem-class-implementation">
<db:title>DiagramItem Class Implementation</db:title>
<db:para>We start with a look at the constructor:</db:para>
<db:programlisting language="cpp">DiagramItem::DiagramItem(DiagramType diagramType, QMenu *contextMenu,
             QGraphicsItem *parent)
    : QGraphicsPolygonItem(parent)
{
    myDiagramType = diagramType;
    myContextMenu = contextMenu;

    QPainterPath path;
    switch (myDiagramType) {
        case StartEnd:
            path.moveTo(200, 50);
            path.arcTo(150, 0, 50, 50, 0, 90);
            path.arcTo(50, 0, 50, 50, 90, 90);
            path.arcTo(50, 50, 50, 50, 180, 90);
            path.arcTo(150, 50, 50, 50, 270, 90);
            path.lineTo(200, 25);
            myPolygon = path.toFillPolygon();
            break;
        case Conditional:
            myPolygon &lt;&lt; QPointF(-100, 0) &lt;&lt; QPointF(0, 100)
                      &lt;&lt; QPointF(100, 0) &lt;&lt; QPointF(0, -100)
                      &lt;&lt; QPointF(-100, 0);
            break;
        case Step:
            myPolygon &lt;&lt; QPointF(-100, -100) &lt;&lt; QPointF(100, -100)
                      &lt;&lt; QPointF(100, 100) &lt;&lt; QPointF(-100, 100)
                      &lt;&lt; QPointF(-100, -100);
            break;
        default:
            myPolygon &lt;&lt; QPointF(-120, -80) &lt;&lt; QPointF(-70, 80)
                      &lt;&lt; QPointF(120, 80) &lt;&lt; QPointF(70, -80)
                      &lt;&lt; QPointF(-120, -80);
            break;
    }
    setPolygon(myPolygon);
    setFlag(QGraphicsItem::ItemIsMovable, true);
    setFlag(QGraphicsItem::ItemIsSelectable, true);
    setFlag(QGraphicsItem::ItemSendsGeometryChanges, true);
}
</db:programlisting>
<db:para>In the constructor we create the items polygon according to <db:code role="parameter">diagramType</db:code>. <db:link xlink:href="qgraphicsitem.xml">QGraphicsItem</db:link>s are not movable or selectable by default, so we must set these properties.</db:para>
<db:para>Here is the <db:code>removeArrow()</db:code> function:</db:para>
<db:programlisting language="cpp">void DiagramItem::removeArrow(Arrow *arrow)
{
    int index = arrows.indexOf(arrow);

    if (index != -1)
        arrows.removeAt(index);
}
</db:programlisting>
<db:para><db:code>removeArrow()</db:code> is used to remove <db:code>Arrow</db:code> items when they or <db:code>DiagramItems</db:code> they are connected to are removed from the scene.</db:para>
<db:para>Here is the <db:code>removeArrows()</db:code> function:</db:para>
<db:programlisting language="cpp">void DiagramItem::removeArrows()
{
    foreach (Arrow *arrow, arrows) {
        arrow-&gt;startItem()-&gt;removeArrow(arrow);
        arrow-&gt;endItem()-&gt;removeArrow(arrow);
        scene()-&gt;removeItem(arrow);
        delete arrow;
    }
}
</db:programlisting>
<db:para>This function is called when the item is removed from the scene and removes all arrows that are connected to this item. The arrow must be removed from the <db:code>arrows</db:code> list of both its start and end item.</db:para>
<db:para>Here is the <db:code>addArrow()</db:code> function:</db:para>
<db:programlisting language="cpp">void DiagramItem::addArrow(Arrow *arrow)
{
    arrows.append(arrow);
}
</db:programlisting>
<db:para>This function simply adds the <db:code role="parameter">arrow</db:code> to the items <db:code>arrows</db:code> list.</db:para>
<db:para>Here is the <db:code>image()</db:code> function:</db:para>
<db:programlisting language="cpp">QPixmap DiagramItem::image() const
{
    QPixmap pixmap(250, 250);
    pixmap.fill(Qt::transparent);
    QPainter painter(&amp;pixmap);
    painter.setPen(QPen(Qt::black, 8));
    painter.translate(125, 125);
    painter.drawPolyline(myPolygon);

    return pixmap;
}
</db:programlisting>
<db:para>This function draws the polygon of the item onto a <db:link xlink:href="qpixmap.xml">QPixmap</db:link>. In this example we use this to create icons for the tool buttons in the tool box.</db:para>
<db:para>Here is the <db:code>contextMenuEvent()</db:code> function:</db:para>
<db:programlisting language="cpp">void DiagramItem::contextMenuEvent(QGraphicsSceneContextMenuEvent *event)
{
    scene()-&gt;clearSelection();
    setSelected(true);
    myContextMenu-&gt;exec(event-&gt;screenPos());
}
</db:programlisting>
<db:para>We show the context menu. As right mouse clicks, which shows the menu, don't select items by default we set the item selected with <db:link xlink:href="qgraphicsitem.xml#setSelected">setSelected</db:link>(). This is necessary since an item must be selected to change its elevation with the <db:code>bringToFront</db:code> and <db:code>sendToBack</db:code> actions.</db:para>
<db:para>This is the implementation of <db:code>itemChange()</db:code>:</db:para>
<db:programlisting language="cpp">QVariant DiagramItem::itemChange(GraphicsItemChange change, const QVariant &amp;value)
{
    if (change == QGraphicsItem::ItemPositionChange) {
        foreach (Arrow *arrow, arrows) {
            arrow-&gt;updatePosition();
        }
    }

    return value;
}
</db:programlisting>
<db:para>If the item has moved, we need to update the positions of the arrows connected to it. The implementation of <db:link xlink:href="qgraphicsitem.xml">QGraphicsItem</db:link> does nothing, so we just return <db:code role="parameter">value</db:code>.</db:para>
</db:section>
<db:section xml:id="diagramtextitem-class-definition">
<db:title>DiagramTextItem Class Definition</db:title>
<db:para>The <db:code>TextDiagramItem</db:code> class inherits <db:link xlink:href="qgraphicstextitem.xml">QGraphicsTextItem</db:link> and adds the possibility to move editable text items. Editable QGraphicsTextItems are designed to be fixed in place and editing starts when the user single clicks on the item. With <db:code>DiagramTextItem</db:code> the editing starts with a double click leaving single click available to interact with and move it.</db:para>
<db:programlisting language="cpp">class DiagramTextItem : public QGraphicsTextItem
{
    Q_OBJECT

public:
    enum { Type = UserType + 3 };

    DiagramTextItem(QGraphicsItem *parent = 0);

    int type() const { return Type; }

signals:
    void lostFocus(DiagramTextItem *item);
    void selectedChange(QGraphicsItem *item);

protected:
    QVariant itemChange(GraphicsItemChange change, const QVariant &amp;value);
    void focusOutEvent(QFocusEvent *event);
    void mouseDoubleClickEvent(QGraphicsSceneMouseEvent *event);
};
</db:programlisting>
<db:para>We use <db:code>itemChange()</db:code> and <db:code>focusOutEvent()</db:code> to notify the <db:code>DiagramScene</db:code> when the text item loses focus and gets selected.</db:para>
<db:para>We reimplement the functions that handle mouse events to make it possible to alter the mouse behavior of <db:link xlink:href="qgraphicstextitem.xml">QGraphicsTextItem</db:link>.</db:para>
</db:section>
<db:section xml:id="diagramtextitem-implementation">
<db:title>DiagramTextItem Implementation</db:title>
<db:para>We start with the constructor:</db:para>
<db:programlisting language="cpp">DiagramTextItem::DiagramTextItem(QGraphicsItem *parent)
    : QGraphicsTextItem(parent)
{
    setFlag(QGraphicsItem::ItemIsMovable);
    setFlag(QGraphicsItem::ItemIsSelectable);
}
</db:programlisting>
<db:para>We simply set the item movable and selectable, as these flags are off by default.</db:para>
<db:para>Here is the <db:code>itemChange()</db:code> function:</db:para>
<db:programlisting language="cpp">QVariant DiagramTextItem::itemChange(GraphicsItemChange change,
                     const QVariant &amp;value)
{
    if (change == QGraphicsItem::ItemSelectedHasChanged)
        emit selectedChange(this);
    return value;
}
</db:programlisting>
<db:para>When the item is selected we emit the selectedChanged signal. The <db:code>MainWindow</db:code> uses this signal to update the widgets that display font properties to the font of the selected text item.</db:para>
<db:para>Here is the <db:code>focusOutEvent()</db:code> function:</db:para>
<db:programlisting language="cpp">void DiagramTextItem::focusOutEvent(QFocusEvent *event)
{
    setTextInteractionFlags(Qt::NoTextInteraction);
    emit lostFocus(this);
    QGraphicsTextItem::focusOutEvent(event);
}
</db:programlisting>
<db:para><db:code>DiagramScene</db:code> uses the signal emitted when the text item looses focus to remove the item if it is empty, i.e., it contains no text.</db:para>
<db:para>This is the implementation of <db:code>mouseDoubleClickEvent()</db:code>:</db:para>
<db:programlisting language="cpp">void DiagramTextItem::mouseDoubleClickEvent(QGraphicsSceneMouseEvent *event)
{
    if (textInteractionFlags() == Qt::NoTextInteraction)
        setTextInteractionFlags(Qt::TextEditorInteraction);
    QGraphicsTextItem::mouseDoubleClickEvent(event);
}
</db:programlisting>
<db:para>When we receive a double click event, we make the item editable by calling <db:link xlink:href="qgraphicstextitem.xml#setTextInteractionFlags">QGraphicsTextItem::setTextInteractionFlags</db:link>(). We then forward the double-click to the item itself.</db:para>
</db:section>
<db:section xml:id="arrow-class-definition">
<db:title>Arrow Class Definition</db:title>
<db:para>The <db:code>Arrow</db:code> class is a graphics item that connects two <db:code>DiagramItems</db:code>. It draws an arrow head to one of the items. To achieve this the item needs to paint itself and also re implement methods used by the graphics scene to check for collisions and selections. The class inherits QGraphicsLine item, and draws the arrowhead and moves with the items it connects.</db:para>
<db:programlisting language="cpp">class Arrow : public QGraphicsLineItem
{
public:
    enum { Type = UserType + 4 };

    Arrow(DiagramItem *startItem, DiagramItem *endItem,
      QGraphicsItem *parent = 0);

    int type() const { return Type; }
    QRectF boundingRect() const;
    QPainterPath shape() const;
    void setColor(const QColor &amp;color) { myColor = color; }
    DiagramItem *startItem() const { return myStartItem; }
    DiagramItem *endItem() const { return myEndItem; }

    void updatePosition();

protected:
    void paint(QPainter *painter, const QStyleOptionGraphicsItem *option, QWidget *widget = 0);

private:
    DiagramItem *myStartItem;
    DiagramItem *myEndItem;
    QColor myColor;
    QPolygonF arrowHead;
};
</db:programlisting>
<db:para>The item's color can be set with <db:code>setColor()</db:code>.</db:para>
<db:para><db:code>boundingRect()</db:code> and <db:code>shape()</db:code> are reimplemented from <db:link xlink:href="qgraphicslineitem.xml">QGraphicsLineItem</db:link> and are used by the scene to check for collisions and selections.</db:para>
<db:para>Calling <db:code>updatePosition()</db:code> causes the arrow to recalculate its position and arrow head angle. <db:code>paint()</db:code> is reimplemented so that we can paint an arrow rather than just a line between items.</db:para>
<db:para><db:code>myStartItem</db:code> and <db:code>myEndItem</db:code> are the diagram items that the arrow connects. The arrow is drawn with its head to the end item. <db:code>arrowHead</db:code> is a polygon with three vertices's we use to draw the arrow head.</db:para>
</db:section>
<db:section xml:id="arrow-class-implementation">
<db:title>Arrow Class Implementation</db:title>
<db:para>The constructor of the <db:code>Arrow</db:code> class looks like this:</db:para>
<db:programlisting language="cpp">Arrow::Arrow(DiagramItem *startItem, DiagramItem *endItem, QGraphicsItem *parent)
    : QGraphicsLineItem(parent)
{
    myStartItem = startItem;
    myEndItem = endItem;
    setFlag(QGraphicsItem::ItemIsSelectable, true);
    myColor = Qt::black;
    setPen(QPen(myColor, 2, Qt::SolidLine, Qt::RoundCap, Qt::RoundJoin));
}
</db:programlisting>
<db:para>We set the start and end diagram items of the arrow. The arrow head will be drawn where the line intersects the end item.</db:para>
<db:para>Here is the <db:code>boundingRect()</db:code> function:</db:para>
<db:programlisting language="cpp">QRectF Arrow::boundingRect() const
{
    qreal extra = (pen().width() + 20) / 2.0;

    return QRectF(line().p1(), QSizeF(line().p2().x() - line().p1().x(),
                                      line().p2().y() - line().p1().y()))
        .normalized()
        .adjusted(-extra, -extra, extra, extra);
}
</db:programlisting>
<db:para>We need to reimplement this function because the arrow is larger than the bounding rectangle of the <db:link xlink:href="qgraphicslineitem.xml">QGraphicsLineItem</db:link>. The graphics scene uses the bounding rectangle to know which regions of the scene to update.</db:para>
<db:para>Here is the <db:code>shape()</db:code> function:</db:para>
<db:programlisting language="cpp">QPainterPath Arrow::shape() const
{
    QPainterPath path = QGraphicsLineItem::shape();
    path.addPolygon(arrowHead);
    return path;
}
</db:programlisting>
<db:para>The shape function returns a <db:link xlink:href="qpainterpath.xml">QPainterPath</db:link> that is the exact shape of the item. The <db:link xlink:href="qgraphicslineitem.xml#shape">QGraphicsLineItem::shape</db:link>() returns a path with a line drawn with the current pen, so we only need to add the arrow head. This function is used to check for collisions and selections with the mouse.</db:para>
<db:para>Here is the <db:code>updatePosition()</db:code> slot:</db:para>
<db:programlisting language="cpp">void Arrow::updatePosition()
{
    QLineF line(mapFromItem(myStartItem, 0, 0), mapFromItem(myEndItem, 0, 0));
    setLine(line);
}
</db:programlisting>
<db:para>This slot updates the arrow by setting the start and end points of its line to the center of the items it connects.</db:para>
<db:para>Here is the <db:code>paint()</db:code> function:</db:para>
<db:programlisting language="cpp">void Arrow::paint(QPainter *painter, const QStyleOptionGraphicsItem *,
          QWidget *)
{
    if (myStartItem-&gt;collidesWithItem(myEndItem))
        return;

    QPen myPen = pen();
    myPen.setColor(myColor);
    qreal arrowSize = 20;
    painter-&gt;setPen(myPen);
    painter-&gt;setBrush(myColor);
</db:programlisting>
<db:para>If the start and end items collide we do not draw the arrow; the algorithm we use to find the point the arrow should be drawn at may fail if the items collide.</db:para>
<db:para>We first set the pen and brush we will use for drawing the arrow.</db:para>
<db:programlisting language="cpp">    QLineF centerLine(myStartItem-&gt;pos(), myEndItem-&gt;pos());
    QPolygonF endPolygon = myEndItem-&gt;polygon();
    QPointF p1 = endPolygon.first() + myEndItem-&gt;pos();
    QPointF p2;
    QPointF intersectPoint;
    QLineF polyLine;
    for (int i = 1; i &lt; endPolygon.count(); ++i) {
    p2 = endPolygon.at(i) + myEndItem-&gt;pos();
    polyLine = QLineF(p1, p2);
    QLineF::IntersectType intersectType =
        polyLine.intersect(centerLine, &amp;intersectPoint);
    if (intersectType == QLineF::BoundedIntersection)
        break;
        p1 = p2;
    }

    setLine(QLineF(intersectPoint, myStartItem-&gt;pos()));
</db:programlisting>
<db:para>We then need to find the position at which to draw the arrowhead. The head should be drawn where the line and the end item intersects. This is done by taking the line between each point in the polygon and check if it intersects with the line of the arrow. Since the line start and end points are set to the center of the items the arrow line should intersect one and only one of the lines of the polygon. Note that the points in the polygon are relative to the local coordinate system of the item. We must therefore add the position of the end item to make the coordinates relative to the scene.</db:para>
<db:programlisting language="cpp">    double angle = ::acos(line().dx() / line().length());
    if (line().dy() &gt;= 0)
        angle = (Pi * 2) - angle;

        QPointF arrowP1 = line().p1() + QPointF(sin(angle + Pi / 3) * arrowSize,
                                        cos(angle + Pi / 3) * arrowSize);
        QPointF arrowP2 = line().p1() + QPointF(sin(angle + Pi - Pi / 3) * arrowSize,
                                        cos(angle + Pi - Pi / 3) * arrowSize);

        arrowHead.clear();
        arrowHead &lt;&lt; line().p1() &lt;&lt; arrowP1 &lt;&lt; arrowP2;
</db:programlisting>
<db:para>We calculate the angle between the x-axis and the line of the arrow. We need to turn the arrow head to this angle so that it follows the direction of the arrow. If the angle is negative we must turn the direction of the arrow.</db:para>
<db:para>We can then calculate the three points of the arrow head polygon. One of the points is the end of the line, which now is the intersection between the arrow line and the end polygon. Then we clear the <db:code>arrowHead</db:code> polygon from the previous calculated arrow head and set these new points.</db:para>
<db:programlisting language="cpp">        painter-&gt;drawLine(line());
        painter-&gt;drawPolygon(arrowHead);
        if (isSelected()) {
            painter-&gt;setPen(QPen(myColor, 1, Qt::DashLine));
        QLineF myLine = line();
        myLine.translate(0, 4.0);
        painter-&gt;drawLine(myLine);
        myLine.translate(0,-8.0);
        painter-&gt;drawLine(myLine);
    }
}
</db:programlisting>
<db:para>If the line is selected, we draw two dotted lines that are parallel with the line of the arrow. We do not use the default implementation, which uses <db:link xlink:href="qgraphicsitem.xml#boundingRect">boundingRect</db:link>() because the <db:link xlink:href="qrect.xml">QRect</db:link> bounding rectangle is considerably larger than the line.</db:para>
</db:section>
<db:section>
<db:title>List of Files</db:title>
<db:para>Files:</db:para>
<db:section>
<db:title>List of Files</db:title>
<db:itemizedlist>
<db:listitem>
<db:para><db:link xlink:href="graphicsview/diagramscene/arrow.cpp">graphicsview/diagramscene/arrow.cpp</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="graphicsview/diagramscene/arrow.h">graphicsview/diagramscene/arrow.h</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="graphicsview/diagramscene/diagramitem.cpp">graphicsview/diagramscene/diagramitem.cpp</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="graphicsview/diagramscene/diagramitem.h">graphicsview/diagramscene/diagramitem.h</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="graphicsview/diagramscene/diagramscene.cpp">graphicsview/diagramscene/diagramscene.cpp</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="graphicsview/diagramscene/diagramscene.h">graphicsview/diagramscene/diagramscene.h</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="graphicsview/diagramscene/diagramscene.pro">graphicsview/diagramscene/diagramscene.pro</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="graphicsview/diagramscene/diagramscene.qrc">graphicsview/diagramscene/diagramscene.qrc</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="graphicsview/diagramscene/diagramtextitem.cpp">graphicsview/diagramscene/diagramtextitem.cpp</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="graphicsview/diagramscene/diagramtextitem.h">graphicsview/diagramscene/diagramtextitem.h</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="graphicsview/diagramscene/main.cpp">graphicsview/diagramscene/main.cpp</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="graphicsview/diagramscene/mainwindow.cpp">graphicsview/diagramscene/mainwindow.cpp</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="graphicsview/diagramscene/mainwindow.h">graphicsview/diagramscene/mainwindow.h</db:link></db:para></db:listitem>
</db:itemizedlist>
</db:section>
<db:section>
<db:title>List of Files</db:title>
<db:para>Images:</db:para>
<db:section>
<db:title>List of Files</db:title>
<db:itemizedlist>
<db:listitem>
<db:para><db:link xlink:href="graphicsview/diagramscene/images/background1.png">graphicsview/diagramscene/images/background1.png</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="graphicsview/diagramscene/images/background2.png">graphicsview/diagramscene/images/background2.png</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="graphicsview/diagramscene/images/background3.png">graphicsview/diagramscene/images/background3.png</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="graphicsview/diagramscene/images/background4.png">graphicsview/diagramscene/images/background4.png</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="graphicsview/diagramscene/images/bold.png">graphicsview/diagramscene/images/bold.png</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="graphicsview/diagramscene/images/bringtofront.png">graphicsview/diagramscene/images/bringtofront.png</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="graphicsview/diagramscene/images/delete.png">graphicsview/diagramscene/images/delete.png</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="graphicsview/diagramscene/images/floodfill.png">graphicsview/diagramscene/images/floodfill.png</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="graphicsview/diagramscene/images/italic.png">graphicsview/diagramscene/images/italic.png</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="graphicsview/diagramscene/images/linecolor.png">graphicsview/diagramscene/images/linecolor.png</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="graphicsview/diagramscene/images/linepointer.png">graphicsview/diagramscene/images/linepointer.png</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="graphicsview/diagramscene/images/pointer.png">graphicsview/diagramscene/images/pointer.png</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="graphicsview/diagramscene/images/sendtoback.png">graphicsview/diagramscene/images/sendtoback.png</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="graphicsview/diagramscene/images/textpointer.png">graphicsview/diagramscene/images/textpointer.png</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="graphicsview/diagramscene/images/underline.png">graphicsview/diagramscene/images/underline.png</db:link></db:para></db:listitem>
</db:itemizedlist>
</db:section>
</db:section></db:section></db:article>
