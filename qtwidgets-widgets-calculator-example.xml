<?xml version="1.0" encoding="UTF-8"?>
<db:article xmlns:db="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink" version="5.2" xml:lang="en">
<db:info>
<db:title>Calculator Example</db:title>
<db:productname>QtWidgets</db:productname>
<db:edition>Qt 5.4.2 Reference Documentation</db:edition>
<db:titleabbrev>Qt Widgets Reference Documentation</db:titleabbrev>
<db:abstract>
<db:para>The example shows how to use signals and slots to implement the functionality of a calculator widget, and how to use QGridLayout to place child widgets in a grid.</db:para>
</db:abstract>
<db:pubdate>2025-06-29</db:pubdate>
<db:date>2025-06-29</db:date>
<db:authorgroup>
<db:author>
<db:orgname class="corporation">The Qt Company Ltd.</db:orgname>
</db:author>
</db:authorgroup>
</db:info>
<db:mediaobject>
<db:alt>Screenshot of the Calculator example</db:alt>
<db:imageobject>
<db:imagedata fileref="images/calculator-example.png"/>
</db:imageobject>
</db:mediaobject>
<db:para>The example consists of two classes:</db:para>
<db:itemizedlist>
<db:listitem>
<db:para><db:code>Calculator</db:code> is the calculator widget, with all the calculator functionality.</db:para>
</db:listitem>
<db:listitem>
<db:para><db:code>Button</db:code> is the widget used for each of the calculator button. It derives from QToolButton.</db:para>
</db:listitem>
</db:itemizedlist>
<db:para>We will start by reviewing <db:code>Calculator</db:code>, then we will take a look at <db:code>Button</db:code>.</db:para>
<db:section xml:id="calculator-class-definition">
<db:title>Calculator Class Definition</db:title>
<db:programlisting language="cpp">class Calculator : public QWidget
{
    Q_OBJECT

public:
    Calculator(QWidget *parent = 0);

private slots:
    void digitClicked();
    void unaryOperatorClicked();
    void additiveOperatorClicked();
    void multiplicativeOperatorClicked();
    void equalClicked();
    void pointClicked();
    void changeSignClicked();
    void backspaceClicked();
    void clear();
    void clearAll();
    void clearMemory();
    void readMemory();
    void setMemory();
    void addToMemory();
</db:programlisting>
<db:para>The <db:code>Calculator</db:code> class provides a simple calculator widget. It inherits from QDialog and has several private slots associated with the calculator's buttons. <db:link xlink:href="qobject.xml#eventFilter">QObject::eventFilter</db:link>() is reimplemented to handle mouse events on the calculator's display.</db:para>
<db:para>Buttons are grouped in categories according to their behavior. For example, all the digit buttons (labeled <db:guilabel>0</db:guilabel> to <db:guilabel>9</db:guilabel>) append a digit to the current operand. For these, we connect multiple buttons to the same slot (e.g., <db:code>digitClicked()</db:code>). The categories are digits, unary operators (<db:guilabel>Sqrt</db:guilabel>, <db:guilabel>x²</db:guilabel>, <db:guilabel>1/x</db:guilabel>), additive operators (<db:guilabel>+</db:guilabel>, <db:guilabel>-</db:guilabel>), and multiplicative operators (<db:guilabel>×</db:guilabel>, <db:guilabel>÷</db:guilabel>). The other buttons have their own slots.</db:para>
<db:programlisting language="cpp">private:
    Button *createButton(const QString &amp;text, const char *member);
    void abortOperation();
    bool calculate(double rightOperand, const QString &amp;pendingOperator);
</db:programlisting>
<db:para>The private <db:code>createButton()</db:code> function is used as part of the widget construction. <db:code>abortOperation()</db:code> is called whenever a division by zero occurs or when a square root operation is applied to a negative number. <db:code>calculate()</db:code> applies a binary operator (<db:guilabel>+</db:guilabel>, <db:guilabel>-</db:guilabel>, <db:guilabel>×</db:guilabel>, or <db:guilabel>÷</db:guilabel>).</db:para>
<db:programlisting language="cpp">    double sumInMemory;
    double sumSoFar;
    double factorSoFar;
    QString pendingAdditiveOperator;
    QString pendingMultiplicativeOperator;
    bool waitingForOperand;
</db:programlisting>
<db:para>These variables, together with the contents of the calculator display (a QLineEdit), encode the state of the calculator:</db:para>
<db:itemizedlist>
<db:listitem>
<db:para><db:code>sumInMemory</db:code> contains the value stored in the calculator's memory (using <db:guilabel>MS</db:guilabel>, <db:guilabel>M+</db:guilabel>, or <db:guilabel>MC</db:guilabel>).</db:para>
</db:listitem>
<db:listitem>
<db:para><db:code>sumSoFar</db:code> stores the value accumulated so far. When the user clicks <db:guilabel>=</db:guilabel>, <db:code>sumSoFar</db:code> is recomputed and shown on the display. <db:guilabel>Clear All</db:guilabel> resets <db:code>sumSoFar</db:code> to zero.</db:para>
</db:listitem>
<db:listitem>
<db:para><db:code>factorSoFar</db:code> stores a temporary value when doing multiplications and divisions.</db:para>
</db:listitem>
<db:listitem>
<db:para><db:code>pendingAdditiveOperator</db:code> stores the last additive operator clicked by the user.</db:para>
</db:listitem>
<db:listitem>
<db:para><db:code>pendingMultiplicativeOperator</db:code> stores the last multiplicative operator clicked by the user.</db:para>
</db:listitem>
<db:listitem>
<db:para><db:code>waitingForOperand</db:code> is <db:code>true</db:code> when the calculator is expecting the user to start typing an operand.</db:para>
</db:listitem>
</db:itemizedlist>
<db:para>Additive and multiplicative operators are treated differently because they have different precedences. For example, <db:guilabel>1 + 2 ÷ 3</db:guilabel> is interpreted as <db:guilabel>1 + (2 ÷ 3)</db:guilabel> because <db:guilabel>÷</db:guilabel> has higher precedence than <db:guilabel>+</db:guilabel>.</db:para>
<db:para>The table below shows the evolution of the calculator state as the user enters a mathematical expression.</db:para>
<db:informaltable style="generic">
<db:thead>
<db:tr>
<db:th>
<db:para>User Input</db:para>
</db:th>
<db:th>
<db:para>Display</db:para>
</db:th>
<db:th>
<db:para>Sum so Far</db:para>
</db:th>
<db:th>
<db:para>Add. Op.</db:para>
</db:th>
<db:th>
<db:para>Factor so Far</db:para>
</db:th>
<db:th>
<db:para>Mult. Op.</db:para>
</db:th>
<db:th>
<db:para>Waiting for Operand?</db:para>
</db:th>
</db:tr>
</db:thead>
<db:tr valign="top">
<db:td>
</db:td>
<db:td>
<db:para>0</db:para>
</db:td>
<db:td>
<db:para>0</db:para>
</db:td>
<db:td>
</db:td>
<db:td>
</db:td>
<db:td>
</db:td>
<db:td>
<db:para><db:code>true</db:code></db:para>
</db:td>
</db:tr>
<db:tr valign="top">
<db:td>
<db:para><db:guilabel>1</db:guilabel></db:para>
</db:td>
<db:td>
<db:para>1</db:para>
</db:td>
<db:td>
<db:para>0</db:para>
</db:td>
<db:td>
</db:td>
<db:td>
</db:td>
<db:td>
</db:td>
<db:td>
<db:para><db:code>false</db:code></db:para>
</db:td>
</db:tr>
<db:tr valign="top">
<db:td>
<db:para><db:guilabel>1 +</db:guilabel></db:para>
</db:td>
<db:td>
<db:para>1</db:para>
</db:td>
<db:td>
<db:para>1</db:para>
</db:td>
<db:td>
<db:para><db:guilabel>+</db:guilabel></db:para>
</db:td>
<db:td>
</db:td>
<db:td>
</db:td>
<db:td>
<db:para><db:code>true</db:code></db:para>
</db:td>
</db:tr>
<db:tr valign="top">
<db:td>
<db:para><db:guilabel>1 + 2</db:guilabel></db:para>
</db:td>
<db:td>
<db:para>2</db:para>
</db:td>
<db:td>
<db:para>1</db:para>
</db:td>
<db:td>
<db:para><db:guilabel>+</db:guilabel></db:para>
</db:td>
<db:td>
</db:td>
<db:td>
</db:td>
<db:td>
<db:para><db:code>false</db:code></db:para>
</db:td>
</db:tr>
<db:tr valign="top">
<db:td>
<db:para><db:guilabel>1 + 2 ÷</db:guilabel></db:para>
</db:td>
<db:td>
<db:para>2</db:para>
</db:td>
<db:td>
<db:para>1</db:para>
</db:td>
<db:td>
<db:para><db:guilabel>+</db:guilabel></db:para>
</db:td>
<db:td>
<db:para>2</db:para>
</db:td>
<db:td>
<db:para><db:guilabel>÷</db:guilabel></db:para>
</db:td>
<db:td>
<db:para><db:code>true</db:code></db:para>
</db:td>
</db:tr>
<db:tr valign="top">
<db:td>
<db:para><db:guilabel>1 + 2 ÷ 3</db:guilabel></db:para>
</db:td>
<db:td>
<db:para>3</db:para>
</db:td>
<db:td>
<db:para>1</db:para>
</db:td>
<db:td>
<db:para><db:guilabel>+</db:guilabel></db:para>
</db:td>
<db:td>
<db:para>2</db:para>
</db:td>
<db:td>
<db:para><db:guilabel>÷</db:guilabel></db:para>
</db:td>
<db:td>
<db:para><db:code>false</db:code></db:para>
</db:td>
</db:tr>
<db:tr valign="top">
<db:td>
<db:para><db:guilabel>1 + 2 ÷ 3 -</db:guilabel></db:para>
</db:td>
<db:td>
<db:para>1.66667</db:para>
</db:td>
<db:td>
<db:para>1.66667</db:para>
</db:td>
<db:td>
<db:para><db:guilabel>-</db:guilabel></db:para>
</db:td>
<db:td>
</db:td>
<db:td>
</db:td>
<db:td>
<db:para><db:code>true</db:code></db:para>
</db:td>
</db:tr>
<db:tr valign="top">
<db:td>
<db:para><db:guilabel>1 + 2 ÷ 3 - 4</db:guilabel></db:para>
</db:td>
<db:td>
<db:para>4</db:para>
</db:td>
<db:td>
<db:para>1.66667</db:para>
</db:td>
<db:td>
<db:para><db:guilabel>-</db:guilabel></db:para>
</db:td>
<db:td>
</db:td>
<db:td>
</db:td>
<db:td>
<db:para><db:code>false</db:code></db:para>
</db:td>
</db:tr>
<db:tr valign="top">
<db:td>
<db:para><db:guilabel>1 + 2 ÷ 3 - 4 =</db:guilabel></db:para>
</db:td>
<db:td>
<db:para>-2.33333</db:para>
</db:td>
<db:td>
<db:para>0</db:para>
</db:td>
<db:td>
</db:td>
<db:td>
</db:td>
<db:td>
</db:td>
<db:td>
<db:para><db:code>true</db:code></db:para>
</db:td>
</db:tr>
</db:informaltable>
<db:para>Unary operators, such as <db:guilabel>Sqrt</db:guilabel>, require no special handling; they can be applied immediately since the operand is already known when the operator button is clicked.</db:para>
<db:programlisting language="cpp">    QLineEdit *display;

    enum { NumDigitButtons = 10 };
    Button *digitButtons[NumDigitButtons];
};
</db:programlisting>
<db:para>Finally, we declare the variables associated with the display and the buttons used to display numerals.</db:para>
</db:section>
<db:section xml:id="calculator-class-implementation">
<db:title>Calculator Class Implementation</db:title>
<db:programlisting language="cpp">Calculator::Calculator(QWidget *parent)
    : QWidget(parent)
{
    sumInMemory = 0.0;
    sumSoFar = 0.0;
    factorSoFar = 0.0;
    waitingForOperand = true;
</db:programlisting>
<db:para>In the constructor, we initialize the calculator's state. The <db:code>pendingAdditiveOperator</db:code> and <db:code>pendingMultiplicativeOperator</db:code> variables don't need to be initialized explicitly, because the <db:link xlink:href="qstring.xml">QString</db:link> constructor initializes them to empty strings.</db:para>
<db:programlisting language="cpp">    display = new QLineEdit(&quot;0&quot;);
    display-&gt;setReadOnly(true);
    display-&gt;setAlignment(Qt::AlignRight);
    display-&gt;setMaxLength(15);

    QFont font = display-&gt;font();
    font.setPointSize(font.pointSize() + 8);
    display-&gt;setFont(font);
</db:programlisting>
<db:para>We create the QLineEdit representing the calculator's display and set up some of its properties. In particular, we set it to be read-only.</db:para>
<db:para>We also enlarge <db:code>display</db:code>'s font by 8 points.</db:para>
<db:programlisting language="cpp">    for (int i = 0; i &lt; NumDigitButtons; ++i) {
        digitButtons[i] = createButton(QString::number(i), SLOT(digitClicked()));
    }

    Button *pointButton = createButton(tr(&quot;.&quot;), SLOT(pointClicked()));
    Button *changeSignButton = createButton(tr(&quot;\302\261&quot;), SLOT(changeSignClicked()));

    Button *backspaceButton = createButton(tr(&quot;Backspace&quot;), SLOT(backspaceClicked()));
    Button *clearButton = createButton(tr(&quot;Clear&quot;), SLOT(clear()));
    Button *clearAllButton = createButton(tr(&quot;Clear All&quot;), SLOT(clearAll()));

    Button *clearMemoryButton = createButton(tr(&quot;MC&quot;), SLOT(clearMemory()));
    Button *readMemoryButton = createButton(tr(&quot;MR&quot;), SLOT(readMemory()));
    Button *setMemoryButton = createButton(tr(&quot;MS&quot;), SLOT(setMemory()));
    Button *addToMemoryButton = createButton(tr(&quot;M+&quot;), SLOT(addToMemory()));

    Button *divisionButton = createButton(tr(&quot;\303\267&quot;), SLOT(multiplicativeOperatorClicked()));
    Button *timesButton = createButton(tr(&quot;\303\227&quot;), SLOT(multiplicativeOperatorClicked()));
    Button *minusButton = createButton(tr(&quot;-&quot;), SLOT(additiveOperatorClicked()));
    Button *plusButton = createButton(tr(&quot;+&quot;), SLOT(additiveOperatorClicked()));

    Button *squareRootButton = createButton(tr(&quot;Sqrt&quot;), SLOT(unaryOperatorClicked()));
    Button *powerButton = createButton(tr(&quot;x\302\262&quot;), SLOT(unaryOperatorClicked()));
    Button *reciprocalButton = createButton(tr(&quot;1/x&quot;), SLOT(unaryOperatorClicked()));
    Button *equalButton = createButton(tr(&quot;=&quot;), SLOT(equalClicked()));
</db:programlisting>
<db:para>For each button, we call the private <db:code>createButton()</db:code> function with the proper text label and a slot to connect to the button.</db:para>
<db:programlisting language="cpp">    QGridLayout *mainLayout = new QGridLayout;
    mainLayout-&gt;setSizeConstraint(QLayout::SetFixedSize);
    mainLayout-&gt;addWidget(display, 0, 0, 1, 6);
    mainLayout-&gt;addWidget(backspaceButton, 1, 0, 1, 2);
    mainLayout-&gt;addWidget(clearButton, 1, 2, 1, 2);
    mainLayout-&gt;addWidget(clearAllButton, 1, 4, 1, 2);

    mainLayout-&gt;addWidget(clearMemoryButton, 2, 0);
    mainLayout-&gt;addWidget(readMemoryButton, 3, 0);
    mainLayout-&gt;addWidget(setMemoryButton, 4, 0);
    mainLayout-&gt;addWidget(addToMemoryButton, 5, 0);

    for (int i = 1; i &lt; NumDigitButtons; ++i) {
        int row = ((9 - i) / 3) + 2;
        int column = ((i - 1) % 3) + 1;
        mainLayout-&gt;addWidget(digitButtons[i], row, column);
    }

    mainLayout-&gt;addWidget(digitButtons[0], 5, 1);
    mainLayout-&gt;addWidget(pointButton, 5, 2);
    mainLayout-&gt;addWidget(changeSignButton, 5, 3);

    mainLayout-&gt;addWidget(divisionButton, 2, 4);
    mainLayout-&gt;addWidget(timesButton, 3, 4);
    mainLayout-&gt;addWidget(minusButton, 4, 4);
    mainLayout-&gt;addWidget(plusButton, 5, 4);

    mainLayout-&gt;addWidget(squareRootButton, 2, 5);
    mainLayout-&gt;addWidget(powerButton, 3, 5);
    mainLayout-&gt;addWidget(reciprocalButton, 4, 5);
    mainLayout-&gt;addWidget(equalButton, 5, 5);
    setLayout(mainLayout);

    setWindowTitle(tr(&quot;Calculator&quot;));
}
</db:programlisting>
<db:para>The layout is handled by a single QGridLayout. The QLayout::setSizeConstraint() call ensures that the <db:code>Calculator</db:code> widget is always shown as its optimal size (its <db:link xlink:href="">size hint</db:link>), preventing the user from resizing the calculator. The size hint is determined by the size and <db:link xlink:href="">size policy</db:link> of the child widgets.</db:para>
<db:para>Most child widgets occupy only one cell in the grid layout. For these, we only need to pass a row and a column to QGridLayout::addWidget(). The <db:code>display</db:code>, <db:code>backspaceButton</db:code>, <db:code>clearButton</db:code>, and <db:code>clearAllButton</db:code> widgets occupy more than one column; for these we must also pass a row span and a column span.</db:para>
<db:programlisting language="cpp">void Calculator::digitClicked()
{
    Button *clickedButton = qobject_cast&lt;Button *&gt;(sender());
    int digitValue = clickedButton-&gt;text().toInt();
    if (display-&gt;text() == &quot;0&quot; &amp;&amp; digitValue == 0.0)
        return;

    if (waitingForOperand) {
        display-&gt;clear();
        waitingForOperand = false;
    }
    display-&gt;setText(display-&gt;text() + QString::number(digitValue));
}
</db:programlisting>
<db:para>Pressing one of the calculator's digit buttons will emit the button's <db:link xlink:href="">clicked()</db:link> signal, which will trigger the <db:code>digitClicked()</db:code> slot.</db:para>
<db:para>First, we find out which button sent the signal using <db:link xlink:href="qobject.xml#sender">QObject::sender</db:link>(). This function returns the sender as a <db:link xlink:href="qobject.xml">QObject</db:link> pointer. Since we know that the sender is a <db:code>Button</db:code> object, we can safely cast the <db:link xlink:href="qobject.xml">QObject</db:link>. We could have used a C-style cast or a C++ <db:code>static_cast&lt;&gt;()</db:code>, but as a defensive programming technique we use a <db:link xlink:href="">qobject_cast()</db:link>. The advantage is that if the object has the wrong type, a null pointer is returned. Crashes due to null pointers are much easier to diagnose than crashes due to unsafe casts. Once we have the button, we extract the operator using QToolButton::text().</db:para>
<db:para>The slot needs to consider two situations in particular. If <db:code>display</db:code> contains &quot;0&quot; and the user clicks the <db:guilabel>0</db:guilabel> button, it would be silly to show &quot;00&quot;. And if the calculator is in a state where it is waiting for a new operand, the new digit is the first digit of that new operand; in that case, any result of a previous calculation must be cleared first.</db:para>
<db:para>At the end, we append the new digit to the value in the display.</db:para>
<db:programlisting language="cpp">void Calculator::unaryOperatorClicked()
{
    Button *clickedButton = qobject_cast&lt;Button *&gt;(sender());
    QString clickedOperator = clickedButton-&gt;text();
    double operand = display-&gt;text().toDouble();
    double result = 0.0;

    if (clickedOperator == tr(&quot;Sqrt&quot;)) {
        if (operand &lt; 0.0) {
            abortOperation();
            return;
        }
        result = sqrt(operand);
    } else if (clickedOperator == tr(&quot;x\302\262&quot;)) {
        result = pow(operand, 2.0);
    } else if (clickedOperator == tr(&quot;1/x&quot;)) {
        if (operand == 0.0) {
            abortOperation();
            return;
        }
        result = 1.0 / operand;
    }
    display-&gt;setText(QString::number(result));
    waitingForOperand = true;
}
</db:programlisting>
<db:para>The <db:code>unaryOperatorClicked()</db:code> slot is called whenever one of the unary operator buttons is clicked. Again a pointer to the clicked button is retrieved using <db:link xlink:href="qobject.xml#sender">QObject::sender</db:link>(). The operator is extracted from the button's text and stored in <db:code>clickedOperator</db:code>. The operand is obtained from <db:code>display</db:code>.</db:para>
<db:para>Then we perform the operation. If <db:guilabel>Sqrt</db:guilabel> is applied to a negative number or <db:guilabel>1/x</db:guilabel> to zero, we call <db:code>abortOperation()</db:code>. If everything goes well, we display the result of the operation in the line edit and we set <db:code>waitingForOperand</db:code> to <db:code>true</db:code>. This ensures that if the user types a new digit, the digit will be considered as a new operand, instead of being appended to the current value.</db:para>
<db:programlisting language="cpp">void Calculator::additiveOperatorClicked()
{
    Button *clickedButton = qobject_cast&lt;Button *&gt;(sender());
    QString clickedOperator = clickedButton-&gt;text();
    double operand = display-&gt;text().toDouble();

</db:programlisting>
<db:para>The <db:code>additiveOperatorClicked()</db:code> slot is called when the user clicks the <db:guilabel>+</db:guilabel> or <db:guilabel>-</db:guilabel> button.</db:para>
<db:para>Before we can actually do something about the clicked operator, we must handle any pending operations. We start with the multiplicative operators, since these have higher precedence than additive operators:</db:para>
<db:programlisting language="cpp">    if (!pendingMultiplicativeOperator.isEmpty()) {
        if (!calculate(operand, pendingMultiplicativeOperator)) {
            abortOperation();
            return;
        }
        display-&gt;setText(QString::number(factorSoFar));
        operand = factorSoFar;
        factorSoFar = 0.0;
        pendingMultiplicativeOperator.clear();
    }

</db:programlisting>
<db:para>If <db:guilabel>×</db:guilabel> or <db:guilabel>÷</db:guilabel> has been clicked earlier, without clicking <db:guilabel>=</db:guilabel> afterward, the current value in the display is the right operand of the <db:guilabel>×</db:guilabel> or <db:guilabel>÷</db:guilabel> operator and we can finally perform the operation and update the display.</db:para>
<db:programlisting language="cpp">    if (!pendingAdditiveOperator.isEmpty()) {
        if (!calculate(operand, pendingAdditiveOperator)) {
            abortOperation();
            return;
        }
        display-&gt;setText(QString::number(sumSoFar));
    } else {
        sumSoFar = operand;
    }

</db:programlisting>
<db:para>If <db:guilabel>+</db:guilabel> or <db:guilabel>-</db:guilabel> has been clicked earlier, <db:code>sumSoFar</db:code> is the left operand and the current value in the display is the right operand of the operator. If there is no pending additive operator, <db:code>sumSoFar</db:code> is simply set to be the text in the display.</db:para>
<db:programlisting language="cpp">    pendingAdditiveOperator = clickedOperator;
    waitingForOperand = true;
}
</db:programlisting>
<db:para>Finally, we can take care of the operator that was just clicked. Since we don't have the right-hand operand yet, we store the clicked operator in the <db:code>pendingAdditiveOperator</db:code> variable. We will apply the operation later, when we have a right operand, with <db:code>sumSoFar</db:code> as the left operand.</db:para>
<db:programlisting language="cpp">void Calculator::multiplicativeOperatorClicked()
{
    Button *clickedButton = qobject_cast&lt;Button *&gt;(sender());
    QString clickedOperator = clickedButton-&gt;text();
    double operand = display-&gt;text().toDouble();

    if (!pendingMultiplicativeOperator.isEmpty()) {
        if (!calculate(operand, pendingMultiplicativeOperator)) {
            abortOperation();
            return;
        }
        display-&gt;setText(QString::number(factorSoFar));
    } else {
        factorSoFar = operand;
    }

    pendingMultiplicativeOperator = clickedOperator;
    waitingForOperand = true;
}
</db:programlisting>
<db:para>The <db:code>multiplicativeOperatorClicked()</db:code> slot is similar to <db:code>additiveOperatorClicked()</db:code>. We don't need to worry about pending additive operators here, because multiplicative operators have precedence over additive operators.</db:para>
<db:programlisting language="cpp">void Calculator::equalClicked()
{
    double operand = display-&gt;text().toDouble();

    if (!pendingMultiplicativeOperator.isEmpty()) {
        if (!calculate(operand, pendingMultiplicativeOperator)) {
            abortOperation();
            return;
        }
        operand = factorSoFar;
        factorSoFar = 0.0;
        pendingMultiplicativeOperator.clear();
    }
    if (!pendingAdditiveOperator.isEmpty()) {
        if (!calculate(operand, pendingAdditiveOperator)) {
            abortOperation();
            return;
        }
        pendingAdditiveOperator.clear();
    } else {
        sumSoFar = operand;
    }

    display-&gt;setText(QString::number(sumSoFar));
    sumSoFar = 0.0;
    waitingForOperand = true;
}
</db:programlisting>
<db:para>Like in <db:code>additiveOperatorClicked()</db:code>, we start by handing any pending multiplicative and additive operators. Then we display <db:code>sumSoFar</db:code> and reset the variable to zero. Resetting the variable to zero is necessary to avoid counting the value twice.</db:para>
<db:programlisting language="cpp">void Calculator::pointClicked()
{
    if (waitingForOperand)
        display-&gt;setText(&quot;0&quot;);
    if (!display-&gt;text().contains(&quot;.&quot;))
        display-&gt;setText(display-&gt;text() + tr(&quot;.&quot;));
    waitingForOperand = false;
}
</db:programlisting>
<db:para>The <db:code>pointClicked()</db:code> slot adds a decimal point to the content in <db:code>display</db:code>.</db:para>
<db:programlisting language="cpp">void Calculator::changeSignClicked()
{
    QString text = display-&gt;text();
    double value = text.toDouble();

    if (value &gt; 0.0) {
        text.prepend(tr(&quot;-&quot;));
    } else if (value &lt; 0.0) {
        text.remove(0, 1);
    }
    display-&gt;setText(text);
}
</db:programlisting>
<db:para>The <db:code>changeSignClicked()</db:code> slot changes the sign of the value in <db:code>display</db:code>. If the current value is positive, we prepend a minus sign; if the current value is negative, we remove the first character from the value (the minus sign).</db:para>
<db:programlisting language="cpp">void Calculator::backspaceClicked()
{
    if (waitingForOperand)
        return;

    QString text = display-&gt;text();
    text.chop(1);
    if (text.isEmpty()) {
        text = &quot;0&quot;;
        waitingForOperand = true;
    }
    display-&gt;setText(text);
}
</db:programlisting>
<db:para>The <db:code>backspaceClicked()</db:code> removes the rightmost character in the display. If we get an empty string, we show &quot;0&quot; and set <db:code>waitingForOperand</db:code> to <db:code>true</db:code>.</db:para>
<db:programlisting language="cpp">void Calculator::clear()
{
    if (waitingForOperand)
        return;

    display-&gt;setText(&quot;0&quot;);
    waitingForOperand = true;
}
</db:programlisting>
<db:para>The <db:code>clear()</db:code> slot resets the current operand to zero. It is equivalent to clicking <db:guilabel>Backspace</db:guilabel> enough times to erase the entire operand.</db:para>
<db:programlisting language="cpp">void Calculator::clearAll()
{
    sumSoFar = 0.0;
    factorSoFar = 0.0;
    pendingAdditiveOperator.clear();
    pendingMultiplicativeOperator.clear();
    display-&gt;setText(&quot;0&quot;);
    waitingForOperand = true;
}
</db:programlisting>
<db:para>The <db:code>clearAll()</db:code> slot resets the calculator to its initial state.</db:para>
<db:programlisting language="cpp">void Calculator::clearMemory()
{
    sumInMemory = 0.0;
}

void Calculator::readMemory()
{
    display-&gt;setText(QString::number(sumInMemory));
    waitingForOperand = true;
}

void Calculator::setMemory()
{
    equalClicked();
    sumInMemory = display-&gt;text().toDouble();
}

void Calculator::addToMemory()
{
    equalClicked();
    sumInMemory += display-&gt;text().toDouble();
}
</db:programlisting>
<db:para>The <db:code>clearMemory()</db:code> slot erases the sum kept in memory, <db:code>readMemory()</db:code> displays the sum as an operand, <db:code>setMemory()</db:code> replace the sum in memory with the current sum, and <db:code>addToMemory()</db:code> adds the current value to the value in memory. For <db:code>setMemory()</db:code> and <db:code>addToMemory()</db:code>, we start by calling <db:code>equalClicked()</db:code> to update <db:code>sumSoFar</db:code> and the value in the display.</db:para>
<db:programlisting language="cpp">Button *Calculator::createButton(const QString &amp;text, const char *member)
{
    Button *button = new Button(text);
    connect(button, SIGNAL(clicked()), this, member);
    return button;
}
</db:programlisting>
<db:para>The private <db:code>createButton()</db:code> function is called from the constructor to create calculator buttons.</db:para>
<db:programlisting language="cpp">void Calculator::abortOperation()
{
    clearAll();
    display-&gt;setText(tr(&quot;####&quot;));
}
</db:programlisting>
<db:para>The private <db:code>abortOperation()</db:code> function is called whenever a calculation fails. It resets the calculator state and displays &quot;####&quot;.</db:para>
<db:programlisting language="cpp">bool Calculator::calculate(double rightOperand, const QString &amp;pendingOperator)
{
    if (pendingOperator == tr(&quot;+&quot;)) {
        sumSoFar += rightOperand;
    } else if (pendingOperator == tr(&quot;-&quot;)) {
        sumSoFar -= rightOperand;
    } else if (pendingOperator == tr(&quot;\303\227&quot;)) {
        factorSoFar *= rightOperand;
    } else if (pendingOperator == tr(&quot;\303\267&quot;)) {
        if (rightOperand == 0.0)
            return false;
        factorSoFar /= rightOperand;
    }
    return true;
}
</db:programlisting>
<db:para>The private <db:code>calculate()</db:code> function performs a binary operation. The right operand is given by <db:code>rightOperand</db:code>. For additive operators, the left operand is <db:code>sumSoFar</db:code>; for multiplicative operators, the left operand is <db:code>factorSoFar</db:code>. The function return <db:code>false</db:code> if a division by zero occurs.</db:para>
</db:section>
<db:section xml:id="button-class-definition">
<db:title>Button Class Definition</db:title>
<db:para>Let's now take a look at the <db:code>Button</db:code> class:</db:para>
<db:programlisting language="cpp">class Button : public QToolButton
{
    Q_OBJECT

public:
    explicit Button(const QString &amp;text, QWidget *parent = 0);

    QSize sizeHint() const Q_DECL_OVERRIDE;
};
</db:programlisting>
<db:para>The <db:code>Button</db:code> class has a convenience constructor that takes a text label and a parent widget, and it reimplements QWidget::sizeHint() to provide more space around the text than the amount QToolButton normally provides.</db:para>
</db:section>
<db:section xml:id="button-class-implementation">
<db:title>Button Class Implementation</db:title>
<db:programlisting language="cpp">Button::Button(const QString &amp;text, QWidget *parent)
    : QToolButton(parent)
{
    setSizePolicy(QSizePolicy::Expanding, QSizePolicy::Preferred);
    setText(text);
}
</db:programlisting>
<db:para>The buttons' appearance is determined by the layout of the calculator widget through the size and <db:link xlink:href="">size policy</db:link> of the layout's child widgets. The call to the <db:link xlink:href="">setSizePolicy()</db:link> function in the constructor ensures that the button will expand horizontally to fill all the available space; by default, QToolButtons don't expand to fill available space. Without this call, the different buttons in a same column would have different widths.</db:para>
<db:programlisting language="cpp">QSize Button::sizeHint() const
{
    QSize size = QToolButton::sizeHint();
    size.rheight() += 20;
    size.rwidth() = qMax(size.width(), size.height());
    return size;
}
</db:programlisting>
<db:para>In <db:link xlink:href="">sizeHint()</db:link>, we try to return a size that looks good for most buttons. We reuse the size hint of the base class (QToolButton) but modify it in the following ways:</db:para>
<db:itemizedlist>
<db:listitem>
<db:para>We add 20 to the <db:link xlink:href="qsize.xml#height">height</db:link> component of the size hint.</db:para>
</db:listitem>
<db:listitem>
<db:para>We make the <db:link xlink:href="qsize.xml#width">width</db:link> component of the size hint at least as much as the <db:link xlink:href="qsize.xml#width">height</db:link>.</db:para>
</db:listitem>
</db:itemizedlist>
<db:para>This ensures that with most fonts, the digit and operator buttons will be square, without truncating the text on the <db:guilabel>Backspace</db:guilabel>, <db:guilabel>Clear</db:guilabel>, and <db:guilabel>Clear All</db:guilabel> buttons.</db:para>
<db:para>The screenshot below shows how the <db:code>Calculator</db:code> widget would look like if we <db:emphasis>didn't</db:emphasis> set the horizontal size policy to QSizePolicy::Expanding in the constructor and if we didn't reimplement QWidget::sizeHint().</db:para>
<db:mediaobject>
<db:alt>The Calculator example with default size policies and size hints</db:alt>
<db:imageobject>
<db:imagedata fileref="images/calculator-ugly.png"/>
</db:imageobject>
</db:mediaobject>
</db:section>
<db:section>
<db:title>List of Files</db:title>
<db:para>Files:</db:para>
<db:section>
<db:title>List of Files</db:title>
<db:itemizedlist>
<db:listitem>
<db:para><db:link xlink:href="widgets/calculator/button.cpp">widgets/calculator/button.cpp</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="widgets/calculator/button.h">widgets/calculator/button.h</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="widgets/calculator/calculator.cpp">widgets/calculator/calculator.cpp</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="widgets/calculator/calculator.h">widgets/calculator/calculator.h</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="widgets/calculator/calculator.pro">widgets/calculator/calculator.pro</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="widgets/calculator/main.cpp">widgets/calculator/main.cpp</db:link></db:para></db:listitem>
</db:itemizedlist>
</db:section>
</db:section></db:article>
