<?xml version="1.0" encoding="UTF-8"?>
<db:article xmlns:db="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink" version="5.2" xml:lang="en">
<db:info>
<db:title>Platform and Compiler Notes - Android</db:title>
<db:productname>QtDoc</db:productname>
<db:edition>Qt 5.14.2 Reference Documentation</db:edition>
<db:titleabbrev>Qt Reference Documentation</db:titleabbrev>
<db:abstract>
<db:para>Qt Reference Documentation.</db:para></db:abstract>
<db:pubdate>2024-12-27</db:pubdate>
<db:date>2024-12-27</db:date>
<db:authorgroup>
<db:author>
<db:orgname class="corporation">The Qt Company Ltd.</db:orgname>
</db:author>
</db:authorgroup>
</db:info>
<db:para><db:emphasis role="bold">&lt;Unknown command&gt;<db:code>contentspage</db:code></db:emphasis>Platform and Compiler Notes</db:para>
<db:para>This page contains information particular to building Qt applications for and running them on the <db:link xlink:href="android.xml">Android</db:link> platform. Qt supports Android versions 4.1 (API level 16) or later.</db:para>
<db:section xml:id="android-development-in-qt-creator">
<db:title>Android Development in Qt Creator</db:title>
<db:para>The easiest way to develop with Qt for Android is to use <db:link xlink:href="https://doc.qt.io/qtcreator/creator-developing-android.html">Qt Creator</db:link>. When you apply a <db:emphasis role="bold">Qt for Android Kit</db:emphasis> to a Qt Creator project, it will create and maintain a set of files which are required to make your application run on Android.</db:para>
<db:para>The files added to your project are:</db:para>
<db:itemizedlist>
<db:listitem>
<db:para><db:emphasis>.java</db:emphasis> files will serve as the entry point into your application and automatically load Qt to execute the native code in your application</db:para>
</db:listitem>
<db:listitem>
<db:para><db:emphasis>AndroidManifest.xml</db:emphasis> which provides meta-information about your application</db:para>
</db:listitem>
<db:listitem>
<db:para>Other XML files detailing the dependencies of your application</db:para>
</db:listitem>
<db:listitem>
<db:para>Resource files</db:para>
</db:listitem>
<db:listitem>
<db:para>Depending on the deployment method selected in Qt Creator, additional files like libraries and QML files can be included in the project.</db:para>
</db:listitem>
</db:itemizedlist>
<db:para>Qt Creator adds these files in a subdirectory of your project called <db:emphasis role="bold">android</db:emphasis>. The contents of the <db:emphasis role="bold">android</db:emphasis> folder is used as basis for your app's distributable application package.</db:para>
</db:section>
<db:section xml:id="application-package">
<db:title>Application Package</db:title>
<db:para>On Android, apps are distributed to devices in packages called <db:emphasis>APK</db:emphasis>.</db:para>
<db:para>For distributing apps in Google Play, a different format called AAB is used instead.</db:para>
<db:para>Although Qt Creator supports building both these package formats for you, you could also build them manually when needed. To do so, ensure that the necessary packages and build files are in place. For more detailed information about how the packaging is done, see <db:link xlink:href="deployment-android.xml">Deploying an Application on Android</db:link>.</db:para>
</db:section>
<db:section xml:id="plugins-and-imports-special-considerations">
<db:title>Plugins and Imports Special Considerations</db:title>
<db:para>If an application uses plugins that depend on other modules, these modules must be listed in the application's dependencies. This is because Qt Creator does not know ahead of time which plugins your application will end up loading.</db:para>
<db:para>For example, if a plugin depends on <db:link xlink:href="qtmultimedia-index.xml">Qt Multimedia</db:link>, then the Qt Multimedia module must explicitly be made a dependency of the application, otherwise the plugin cannot be loaded. You can do this by adding it to the application's .pro file:</db:para>
<db:programlisting language="cpp">QT += multimedia
</db:programlisting>
<db:para>The automatic deployment tool detects any dependencies on QML modules that are imported from the application's code, but in special case (for instance when QML code is generated by the C++ code), this is not possible. If this generated code imports any special QML modules, they are not detected by the deployment tool and therefore will not be included in the application bundle.</db:para>
<db:para>In these cases, you must add &quot;dummy&quot; QML code importing the same modules as the generated code, so that the automated tool detects the dependencies.</db:para>
</db:section>
<db:section xml:id="text-special-considerations">
<db:title>Text Special Considerations</db:title>
<db:para>Because of a bug in some OpenGL drivers, the mechanism used by Qt to cache text glyphs does not work as expected on all Android devices, causing text to appear scrambled. To remedy this, a workaround is in place, which increases memory consumption and can also affect text rendering performance. Before Qt 5.3.2, the workaround was enabled only for a particular set of devices. It is now used by default on all devices.</db:para>
<db:para>You can disable the workaround by setting the QT_ANDROID_DISABLE_GLYPH_CACHE_WORKAROUND environment variable to 1. You should do so only after verifying that text appears correctly on all targeted devices.</db:para>
</db:section>
<db:section xml:id="opengl-special-considerations">
<db:title>OpenGL Special Considerations</db:title>
<db:para>Modern devices often support OpenGL ES 3.0 or 3.1 in addition to 2.0. To get a suitable OpenGL context, set the requested version via QSurfaceFormat::setVersion(). Note however that the header files are only available in recent API levels, for example to include gl31.h, you need to target API level 21. Keep in mind also that using OpenGL ES 3.x features will result in the application breaking on older devices that only support 2.0.</db:para>
</db:section>
<db:section xml:id="multimedia-special-considerations">
<db:title>Multimedia Special Considerations</db:title>
<db:para>The <db:link xlink:href="qtmultimediawidgets-index.xml">Qt Multimedia Widgets</db:link> module is not supported on Android, which means video display is only available using the <db:link xlink:href="qml-qtmultimedia-videooutput.xml">VideoOutput</db:link> and the <db:link xlink:href="qml-qtmultimedia-video.xml">Video</db:link> QML Type.</db:para>
</db:section>
<db:section xml:id="assets-file-system">
<db:title>Assets File System</db:title>
<db:para>Qt for Android provides a special, virtual file system which is based on the <db:emphasis>assets</db:emphasis> mechanism in Android. Files that are put under <db:emphasis>assets</db:emphasis> in the <db:emphasis>android</db:emphasis> folder created by Qt Creator, will be packaged as part of your application package. These can be accessed in Qt by prefixing the paths with assets:/. For instance, to access the image <db:emphasis>logo.png</db:emphasis> in the folder <db:emphasis>assets/images</db:emphasis>, you can use QPixmap(&quot;assets:/images/logo.png&quot;).</db:para>
<db:para>If using the assets mechanism is not required for your app, the recommended way of distributing resources with your Qt app is to use <db:link xlink:href="resources.xml">The Qt Resource System</db:link>, which is a cross-platform mechanism for distributing resources with your app.</db:para>
</db:section>
<db:section xml:id="supported-architectures">
<db:title>Supported Architectures</db:title>
<db:para>Qt for Android currently has binaries for armv7a, arm64-v8a, x86 and x86-64.</db:para>
<db:para>If you want to support several different ABIs in your application, the recommendation is to build an Application App Bundle (AAB) containing binaries for each of the ABIs. Based on this, Google Play generates optimized Application Packages (APK) for the device issuing the download request.</db:para>
<db:para>The Application App Bundle is generated by Qt Creator, if the corresponding checkbox for this is selected in the project settings. It can also be built from the command line by using the &quot;aab&quot; Makefile target.</db:para>
<db:programlisting language="cpp">% make aab
</db:programlisting>
</db:section>
<db:section xml:id="known-issues">
<db:title>Known Issues</db:title>
<db:para>Due to a bug on some devices, when you turn off predictive text with ImhNoPredictiveText, this property will be ignored and predictive text will still be enabled. To work around this, set the QT_ANDROID_ENABLE_WORKAROUND_TO_DISABLE_PREDICTIVE_TEXT environment variable to 1. However, one side effect is that this environment variable can cause a problem with other keyboards such as Gboard. If you use a language like Japanese, with Gboard, only a QWERTY keyboard is displayed. This environment variable is queried each time the keyboard is displayed, so it's possible to toggle the workaround on and off, as necessary.</db:para>
</db:section>
<db:section xml:id="supported-environment-variables">
<db:title>Supported Environment Variables</db:title>
<db:para>The following environment variables are available for building applications with Qt for Android.</db:para>
<db:informaltable style="generic">
<db:thead>
<db:tr>
<db:th>
<db:para>Variable</db:para>
</db:th>
<db:th>
<db:para>Description</db:para>
</db:th>
</db:tr>
</db:thead>
<db:tr valign="top">
<db:td>
<db:para>ANDROID_NDK_ROOT</db:para>
</db:td>
<db:td>
<db:para>Specifies where the Android NDK is located.</db:para>
</db:td>
</db:tr>
<db:tr valign="top">
<db:td>
<db:para>ANDROID_SDK_ROOT</db:para>
</db:td>
<db:td>
<db:para>Specifies where the Android SDK is located.</db:para>
</db:td>
</db:tr>
<db:tr valign="top">
<db:td>
<db:para>ANDROID_NDK_PLATFORM</db:para>
</db:td>
<db:td>
<db:para>Specifies the NDK API version; the default is android-21.</db:para>
</db:td>
</db:tr>
<db:tr valign="top">
<db:td>
<db:para>ANDROID_API_VERSION</db:para>
</db:td>
<db:td>
<db:para>Specifies the Java API version, which can differ from your NDK API version (ANDROID_NDK_PLATFORM). The default is the highest API version found on your system.</db:para>
</db:td>
</db:tr>
<db:tr valign="top">
<db:td>
<db:para>ANDROID_NDK_HOST</db:para>
</db:td>
<db:td>
<db:para>Specifies the host for which the toolchain was built. For example, linux-x86_64 for Linux, windows{} for Windows, and \c{darwin-x86_64} for macOS. \note This variable is detected automatically. Normally, you don't have to change it. \row \li ANDROID_BUILD_TOOLS_REVISION \li Specifies the version of the SDK build tools used as part of the deployment process. For example, 28.0.3. \note Currently in the \c{build.gradle} script file, a known working value is hardcoded; so you don't have to change it. \endtable</db:para>
</db:td>
</db:tr></db:informaltable></db:section></db:article>
