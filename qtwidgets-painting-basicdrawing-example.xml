<?xml version="1.0" encoding="UTF-8"?>
<db:article xmlns:db="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink" version="5.2" xml:lang="en">
<db:info>
<db:title>Basic Drawing Example</db:title>
<db:productname>QtWidgets</db:productname>
<db:edition>Qt 5.4.2 Reference Documentation</db:edition>
<db:titleabbrev>Qt Widgets Reference Documentation</db:titleabbrev>
<db:abstract>
<db:para>The Basic Drawing example shows how to display basic graphics primitives in a variety of styles using the QPainter class.</db:para>
</db:abstract>
<db:pubdate>2025-06-29</db:pubdate>
<db:date>2025-06-29</db:date>
<db:authorgroup>
<db:author>
<db:orgname class="corporation">The Qt Company Ltd.</db:orgname>
</db:author>
</db:authorgroup>
</db:info>
<db:para>QPainter performs low-level painting on widgets and other paint devices. The class can draw everything from simple lines to complex shapes like pies and chords. It can also draw aligned text and pixmaps. Normally, it draws in a &quot;natural&quot; coordinate system, but it can in addition do view and world transformation.</db:para>
<db:mediaobject>
<db:imageobject>
<db:imagedata fileref="images/basicdrawing-example.png"/>
</db:imageobject>
</db:mediaobject>
<db:para>The example provides a render area, displaying the currently active shape, and lets the user manipulate the rendered shape and its appearance using the QPainter parameters: The user can change the active shape (<db:guilabel>Shape</db:guilabel>), and modify the QPainter's pen (<db:guilabel>Pen Width</db:guilabel>, <db:guilabel>Pen Style</db:guilabel>, <db:guilabel>Pen Cap</db:guilabel>, <db:guilabel>Pen Join</db:guilabel>), brush (<db:guilabel>Brush Style</db:guilabel>) and render hints (<db:guilabel>Antialiasing</db:guilabel>). In addition the user can rotate a shape (<db:guilabel>Transformations</db:guilabel>); behind the scenes we use QPainter's ability to manipulate the coordinate system to perform the rotation.</db:para>
<db:para>The Basic Drawing example consists of two classes:</db:para>
<db:itemizedlist>
<db:listitem>
<db:para><db:code>RenderArea</db:code> is a custom widget that renders multiple copies of the currently active shape.</db:para>
</db:listitem>
<db:listitem>
<db:para><db:code>Window</db:code> is the application's main window displaying a <db:code>RenderArea</db:code> widget in addition to several parameter widgets.</db:para>
</db:listitem>
</db:itemizedlist>
<db:para>First we will review the <db:code>Window</db:code> class, then we will take a look at the <db:code>RenderArea</db:code> class.</db:para>
<db:section xml:id="window-class-definition">
<db:title>Window Class Definition</db:title>
<db:para>The Window class inherits QWidget, and is the application's main window displaying a <db:code>RenderArea</db:code> widget in addition to several parameter widgets.</db:para>
<db:programlisting language="cpp">class Window : public QWidget
{
    Q_OBJECT

public:
    Window();

private slots:
    void shapeChanged();
    void penChanged();
    void brushChanged();

private:
    RenderArea *renderArea;
    QLabel *shapeLabel;
    QLabel *penWidthLabel;
    QLabel *penStyleLabel;
    QLabel *penCapLabel;
    QLabel *penJoinLabel;
    QLabel *brushStyleLabel;
    QLabel *otherOptionsLabel;
    QComboBox *shapeComboBox;
    QSpinBox *penWidthSpinBox;
    QComboBox *penStyleComboBox;
    QComboBox *penCapComboBox;
    QComboBox *penJoinComboBox;
    QComboBox *brushStyleComboBox;
    QCheckBox *antialiasingCheckBox;
    QCheckBox *transformationsCheckBox;
};
</db:programlisting>
<db:para>We declare the various widgets, and three private slots updating the <db:code>RenderArea</db:code> widget: The <db:code>shapeChanged()</db:code> slot updates the <db:code>RenderArea</db:code> widget when the user changes the currently active shape. We call the <db:code>penChanged()</db:code> slot when either of the QPainter's pen parameters changes. And the <db:code>brushChanged()</db:code> slot updates the <db:code>RenderArea</db:code> widget when the user changes the painter's brush style.</db:para>
</db:section>
<db:section xml:id="window-class-implementation">
<db:title>Window Class Implementation</db:title>
<db:para>In the constructor we create and initialize the various widgets appearing in the main application window.</db:para>
<db:programlisting language="cpp">Window::Window()
{
    renderArea = new RenderArea;

    shapeComboBox = new QComboBox;
    shapeComboBox-&gt;addItem(tr(&quot;Polygon&quot;), RenderArea::Polygon);
    shapeComboBox-&gt;addItem(tr(&quot;Rectangle&quot;), RenderArea::Rect);
    shapeComboBox-&gt;addItem(tr(&quot;Rounded Rectangle&quot;), RenderArea::RoundedRect);
    shapeComboBox-&gt;addItem(tr(&quot;Ellipse&quot;), RenderArea::Ellipse);
    shapeComboBox-&gt;addItem(tr(&quot;Pie&quot;), RenderArea::Pie);
    shapeComboBox-&gt;addItem(tr(&quot;Chord&quot;), RenderArea::Chord);
    shapeComboBox-&gt;addItem(tr(&quot;Path&quot;), RenderArea::Path);
    shapeComboBox-&gt;addItem(tr(&quot;Line&quot;), RenderArea::Line);
    shapeComboBox-&gt;addItem(tr(&quot;Polyline&quot;), RenderArea::Polyline);
    shapeComboBox-&gt;addItem(tr(&quot;Arc&quot;), RenderArea::Arc);
    shapeComboBox-&gt;addItem(tr(&quot;Points&quot;), RenderArea::Points);
    shapeComboBox-&gt;addItem(tr(&quot;Text&quot;), RenderArea::Text);
    shapeComboBox-&gt;addItem(tr(&quot;Pixmap&quot;), RenderArea::Pixmap);

    shapeLabel = new QLabel(tr(&quot;&amp;Shape:&quot;));
    shapeLabel-&gt;setBuddy(shapeComboBox);
</db:programlisting>
<db:para>First we create the <db:code>RenderArea</db:code> widget that will render the currently active shape. Then we create the <db:guilabel>Shape</db:guilabel> combobox, and add the associated items (i.e. the different shapes a QPainter can draw).</db:para>
<db:programlisting language="cpp">    penWidthSpinBox = new QSpinBox;
    penWidthSpinBox-&gt;setRange(0, 20);
    penWidthSpinBox-&gt;setSpecialValueText(tr(&quot;0 (cosmetic pen)&quot;));

    penWidthLabel = new QLabel(tr(&quot;Pen &amp;Width:&quot;));
    penWidthLabel-&gt;setBuddy(penWidthSpinBox);
</db:programlisting>
<db:para>QPainter's pen is a QPen object; the QPen class defines how a painter should draw lines and outlines of shapes. A pen has several properties: Width, style, cap and join.</db:para>
<db:para>A pen's width can be <db:emphasis>zero</db:emphasis> or greater, but the most common width is zero. Note that this doesn't mean 0 pixels, but implies that the shape is drawn as smoothly as possible although perhaps not mathematically correct.</db:para>
<db:para>We create a QSpinBox for the <db:guilabel>Pen Width</db:guilabel> parameter.</db:para>
<db:programlisting language="cpp">    penStyleComboBox = new QComboBox;
    penStyleComboBox-&gt;addItem(tr(&quot;Solid&quot;), static_cast&lt;int&gt;(Qt::SolidLine));
    penStyleComboBox-&gt;addItem(tr(&quot;Dash&quot;), static_cast&lt;int&gt;(Qt::DashLine));
    penStyleComboBox-&gt;addItem(tr(&quot;Dot&quot;), static_cast&lt;int&gt;(Qt::DotLine));
    penStyleComboBox-&gt;addItem(tr(&quot;Dash Dot&quot;), static_cast&lt;int&gt;(Qt::DashDotLine));
    penStyleComboBox-&gt;addItem(tr(&quot;Dash Dot Dot&quot;), static_cast&lt;int&gt;(Qt::DashDotDotLine));
    penStyleComboBox-&gt;addItem(tr(&quot;None&quot;), static_cast&lt;int&gt;(Qt::NoPen));

    penStyleLabel = new QLabel(tr(&quot;&amp;Pen Style:&quot;));
    penStyleLabel-&gt;setBuddy(penStyleComboBox);

    penCapComboBox = new QComboBox;
    penCapComboBox-&gt;addItem(tr(&quot;Flat&quot;), Qt::FlatCap);
    penCapComboBox-&gt;addItem(tr(&quot;Square&quot;), Qt::SquareCap);
    penCapComboBox-&gt;addItem(tr(&quot;Round&quot;), Qt::RoundCap);

    penCapLabel = new QLabel(tr(&quot;Pen &amp;Cap:&quot;));
    penCapLabel-&gt;setBuddy(penCapComboBox);

    penJoinComboBox = new QComboBox;
    penJoinComboBox-&gt;addItem(tr(&quot;Miter&quot;), Qt::MiterJoin);
    penJoinComboBox-&gt;addItem(tr(&quot;Bevel&quot;), Qt::BevelJoin);
    penJoinComboBox-&gt;addItem(tr(&quot;Round&quot;), Qt::RoundJoin);

    penJoinLabel = new QLabel(tr(&quot;Pen &amp;Join:&quot;));
    penJoinLabel-&gt;setBuddy(penJoinComboBox);
</db:programlisting>
<db:para>The pen style defines the line type. The default style is solid (<db:link xlink:href="qt.xml#PenStyle-enum">Qt::SolidLine</db:link>). Setting the style to none (<db:link xlink:href="qt.xml#PenStyle-enum">Qt::NoPen</db:link>) tells the painter to not draw lines or outlines. The pen cap defines how the end points of lines are drawn. And the pen join defines how two lines join when multiple connected lines are drawn. The cap and join only apply to lines with a width of 1 pixel or greater.</db:para>
<db:para>We create QComboBoxes for each of the <db:guilabel>Pen Style</db:guilabel>, <db:guilabel>Pen Cap</db:guilabel> and <db:guilabel>Pen Join</db:guilabel> parameters, and adds the associated items (i.e the values of the <db:link xlink:href="qt.xml#PenStyle-enum">Qt::PenStyle</db:link>, <db:link xlink:href="qt.xml#PenCapStyle-enum">Qt::PenCapStyle</db:link> and <db:link xlink:href="qt.xml#PenJoinStyle-enum">Qt::PenJoinStyle</db:link> enums respectively).</db:para>
<db:programlisting language="cpp">    brushStyleComboBox = new QComboBox;
    brushStyleComboBox-&gt;addItem(tr(&quot;Linear Gradient&quot;),
            static_cast&lt;int&gt;(Qt::LinearGradientPattern));
    brushStyleComboBox-&gt;addItem(tr(&quot;Radial Gradient&quot;),
            static_cast&lt;int&gt;(Qt::RadialGradientPattern));
    brushStyleComboBox-&gt;addItem(tr(&quot;Conical Gradient&quot;),
            static_cast&lt;int&gt;(Qt::ConicalGradientPattern));
    brushStyleComboBox-&gt;addItem(tr(&quot;Texture&quot;), static_cast&lt;int&gt;(Qt::TexturePattern));
    brushStyleComboBox-&gt;addItem(tr(&quot;Solid&quot;), static_cast&lt;int&gt;(Qt::SolidPattern));
    brushStyleComboBox-&gt;addItem(tr(&quot;Horizontal&quot;), static_cast&lt;int&gt;(Qt::HorPattern));
    brushStyleComboBox-&gt;addItem(tr(&quot;Vertical&quot;), static_cast&lt;int&gt;(Qt::VerPattern));
    brushStyleComboBox-&gt;addItem(tr(&quot;Cross&quot;), static_cast&lt;int&gt;(Qt::CrossPattern));
    brushStyleComboBox-&gt;addItem(tr(&quot;Backward Diagonal&quot;), static_cast&lt;int&gt;(Qt::BDiagPattern));
    brushStyleComboBox-&gt;addItem(tr(&quot;Forward Diagonal&quot;), static_cast&lt;int&gt;(Qt::FDiagPattern));
    brushStyleComboBox-&gt;addItem(tr(&quot;Diagonal Cross&quot;), static_cast&lt;int&gt;(Qt::DiagCrossPattern));
    brushStyleComboBox-&gt;addItem(tr(&quot;Dense 1&quot;), static_cast&lt;int&gt;(Qt::Dense1Pattern));
    brushStyleComboBox-&gt;addItem(tr(&quot;Dense 2&quot;), static_cast&lt;int&gt;(Qt::Dense2Pattern));
    brushStyleComboBox-&gt;addItem(tr(&quot;Dense 3&quot;), static_cast&lt;int&gt;(Qt::Dense3Pattern));
    brushStyleComboBox-&gt;addItem(tr(&quot;Dense 4&quot;), static_cast&lt;int&gt;(Qt::Dense4Pattern));
    brushStyleComboBox-&gt;addItem(tr(&quot;Dense 5&quot;), static_cast&lt;int&gt;(Qt::Dense5Pattern));
    brushStyleComboBox-&gt;addItem(tr(&quot;Dense 6&quot;), static_cast&lt;int&gt;(Qt::Dense6Pattern));
    brushStyleComboBox-&gt;addItem(tr(&quot;Dense 7&quot;), static_cast&lt;int&gt;(Qt::Dense7Pattern));
    brushStyleComboBox-&gt;addItem(tr(&quot;None&quot;), static_cast&lt;int&gt;(Qt::NoBrush));

    brushStyleLabel = new QLabel(tr(&quot;&amp;Brush:&quot;));
    brushStyleLabel-&gt;setBuddy(brushStyleComboBox);
</db:programlisting>
<db:para>The QBrush class defines the fill pattern of shapes drawn by a QPainter. The default brush style is <db:link xlink:href="qt.xml#BrushStyle-enum">Qt::NoBrush</db:link>. This style tells the painter to not fill shapes. The standard style for filling is <db:link xlink:href="qt.xml#BrushStyle-enum">Qt::SolidPattern</db:link>.</db:para>
<db:para>We create a QComboBox for the <db:guilabel>Brush Style</db:guilabel> parameter, and add the associated items (i.e. the values of the <db:link xlink:href="qt.xml#BrushStyle-enum">Qt::BrushStyle</db:link> enum).</db:para>
<db:programlisting language="cpp">    otherOptionsLabel = new QLabel(tr(&quot;Options:&quot;));
    antialiasingCheckBox = new QCheckBox(tr(&quot;&amp;Antialiasing&quot;));
</db:programlisting>
<db:para>Antialiasing is a feature that &quot;smoothes&quot; the pixels to create more even and less jagged lines, and can be applied using QPainter's render hints. QPainter::RenderHints are used to specify flags to QPainter that may or may not be respected by any given engine.</db:para>
<db:para>We simply create a QCheckBox for the <db:guilabel>Antialiasing</db:guilabel> option.</db:para>
<db:programlisting language="cpp">    transformationsCheckBox = new QCheckBox(tr(&quot;&amp;Transformations&quot;));
</db:programlisting>
<db:para>The <db:guilabel>Transformations</db:guilabel> option implies a manipulation of the coordinate system that will appear as if the rendered shape is rotated in three dimensions.</db:para>
<db:para>We use the QPainter::translate(), QPainter::rotate() and QPainter::scale() functions to implement this feature represented in the main application window by a simple QCheckBox.</db:para>
<db:programlisting language="cpp">    connect(shapeComboBox, SIGNAL(activated(int)),
            this, SLOT(shapeChanged()));
    connect(penWidthSpinBox, SIGNAL(valueChanged(int)),
            this, SLOT(penChanged()));
    connect(penStyleComboBox, SIGNAL(activated(int)),
            this, SLOT(penChanged()));
    connect(penCapComboBox, SIGNAL(activated(int)),
            this, SLOT(penChanged()));
    connect(penJoinComboBox, SIGNAL(activated(int)),
            this, SLOT(penChanged()));
    connect(brushStyleComboBox, SIGNAL(activated(int)),
            this, SLOT(brushChanged()));
    connect(antialiasingCheckBox, SIGNAL(toggled(bool)),
            renderArea, SLOT(setAntialiased(bool)));
    connect(transformationsCheckBox, SIGNAL(toggled(bool)),
            renderArea, SLOT(setTransformed(bool)));
</db:programlisting>
<db:para>Then we connect the parameter widgets with their associated slots using the static <db:link xlink:href="qobject.xml#connect">QObject::connect</db:link>() function, ensuring that the <db:code>RenderArea</db:code> widget is updated whenever the user changes the shape, or any of the other parameters.</db:para>
<db:programlisting language="cpp">    QGridLayout *mainLayout = new QGridLayout;
    mainLayout-&gt;setColumnStretch(0, 1);
    mainLayout-&gt;setColumnStretch(3, 1);
    mainLayout-&gt;addWidget(renderArea, 0, 0, 1, 4);
    mainLayout-&gt;addWidget(shapeLabel, 2, 0, Qt::AlignRight);
    mainLayout-&gt;addWidget(shapeComboBox, 2, 1);
    mainLayout-&gt;addWidget(penWidthLabel, 3, 0, Qt::AlignRight);
    mainLayout-&gt;addWidget(penWidthSpinBox, 3, 1);
    mainLayout-&gt;addWidget(penStyleLabel, 4, 0, Qt::AlignRight);
    mainLayout-&gt;addWidget(penStyleComboBox, 4, 1);
    mainLayout-&gt;addWidget(penCapLabel, 3, 2, Qt::AlignRight);
    mainLayout-&gt;addWidget(penCapComboBox, 3, 3);
    mainLayout-&gt;addWidget(penJoinLabel, 2, 2, Qt::AlignRight);
    mainLayout-&gt;addWidget(penJoinComboBox, 2, 3);
    mainLayout-&gt;addWidget(brushStyleLabel, 4, 2, Qt::AlignRight);
    mainLayout-&gt;addWidget(brushStyleComboBox, 4, 3);
    mainLayout-&gt;addWidget(otherOptionsLabel, 5, 0, Qt::AlignRight);
    mainLayout-&gt;addWidget(antialiasingCheckBox, 5, 1, 1, 1, Qt::AlignRight);
    mainLayout-&gt;addWidget(transformationsCheckBox, 5, 2, 1, 2, Qt::AlignRight);
    setLayout(mainLayout);

    shapeChanged();
    penChanged();
    brushChanged();
    antialiasingCheckBox-&gt;setChecked(true);

    setWindowTitle(tr(&quot;Basic Drawing&quot;));
}
</db:programlisting>
<db:para>Finally, we add the various widgets to a layout, and call the <db:code>shapeChanged()</db:code>, <db:code>penChanged()</db:code>, and <db:code>brushChanged()</db:code> slots to initialize the application. We also turn on antialiasing.</db:para>
<db:programlisting language="cpp">void Window::shapeChanged()
{
    RenderArea::Shape shape = RenderArea::Shape(shapeComboBox-&gt;itemData(
            shapeComboBox-&gt;currentIndex(), IdRole).toInt());
    renderArea-&gt;setShape(shape);
}
</db:programlisting>
<db:para>The <db:code>shapeChanged()</db:code> slot is called whenever the user changes the currently active shape.</db:para>
<db:para>First we retrieve the shape the user has chosen using the QComboBox::itemData() function. This function returns the data for the given role in the given index in the combobox. We use QComboBox::currentIndex() to retrieve the index of the shape, and the role is defined by the <db:link xlink:href="qt.xml#ItemDataRole-enum">Qt::ItemDataRole</db:link> enum; <db:code>IdRole</db:code> is an alias for <db:link xlink:href="qt.xml#ItemDataRole-enum">Qt::UserRole</db:link>.</db:para>
<db:para>Note that <db:link xlink:href="qt.xml#ItemDataRole-enum">Qt::UserRole</db:link> is only the first role that can be used for application-specific purposes. If you need to store different data in the same index, you can use different roles by simply incrementing the value of <db:link xlink:href="qt.xml#ItemDataRole-enum">Qt::UserRole</db:link>, for example: '<db:link xlink:href="qt.xml#ItemDataRole-enum">Qt::UserRole</db:link> + 1' and '<db:link xlink:href="qt.xml#ItemDataRole-enum">Qt::UserRole</db:link> + 2'. However, it is a good programming practice to give each role their own name: 'myFirstRole = <db:link xlink:href="qt.xml#ItemDataRole-enum">Qt::UserRole</db:link> + 1' and 'mySecondRole = <db:link xlink:href="qt.xml#ItemDataRole-enum">Qt::UserRole</db:link> + 2'. Even though we only need a single role in this particular example, we add the following line of code to the beginning of the <db:code>window.cpp</db:code> file.</db:para>
<db:programlisting language="cpp">const int IdRole = Qt::UserRole;
</db:programlisting>
<db:para>The QComboBox::itemData() function returns the data as a <db:link xlink:href="qvariant.xml">QVariant</db:link>, so we need to cast the data to <db:code>RenderArea::Shape</db:code>. If there is no data for the given role, the function returns QVariant::Invalid.</db:para>
<db:para>In the end we call the <db:code>RenderArea::setShape()</db:code> slot to update the <db:code>RenderArea</db:code> widget.</db:para>
<db:programlisting language="cpp">void Window::penChanged()
{
    int width = penWidthSpinBox-&gt;value();
    Qt::PenStyle style = Qt::PenStyle(penStyleComboBox-&gt;itemData(
            penStyleComboBox-&gt;currentIndex(), IdRole).toInt());
    Qt::PenCapStyle cap = Qt::PenCapStyle(penCapComboBox-&gt;itemData(
            penCapComboBox-&gt;currentIndex(), IdRole).toInt());
    Qt::PenJoinStyle join = Qt::PenJoinStyle(penJoinComboBox-&gt;itemData(
            penJoinComboBox-&gt;currentIndex(), IdRole).toInt());

    renderArea-&gt;setPen(QPen(Qt::blue, width, style, cap, join));
}
</db:programlisting>
<db:para>We call the <db:code>penChanged()</db:code> slot whenever the user changes any of the pen parameters. Again we use the QComboBox::itemData() function to retrieve the parameters, and then we call the <db:code>RenderArea::setPen()</db:code> slot to update the <db:code>RenderArea</db:code> widget.</db:para>
<db:programlisting language="cpp">void Window::brushChanged()
{
    Qt::BrushStyle style = Qt::BrushStyle(brushStyleComboBox-&gt;itemData(
</db:programlisting>
<db:para>The brushChanged() slot is called whenever the user changes the brush parameter which we retrieve using the QComboBox::itemData() function as before.</db:para>
<db:programlisting language="cpp">    if (style == Qt::LinearGradientPattern) {
        QLinearGradient linearGradient(0, 0, 100, 100);
        linearGradient.setColorAt(0.0, Qt::white);
        linearGradient.setColorAt(0.2, Qt::green);
        linearGradient.setColorAt(1.0, Qt::black);
        renderArea-&gt;setBrush(linearGradient);
</db:programlisting>
<db:para>If the brush parameter is a gradient fill, special actions are required.</db:para>
<db:para>The QGradient class is used in combination with QBrush to specify gradient fills. Qt currently supports three types of gradient fills: linear, radial and conical. Each of these is represented by a subclass of QGradient: QLinearGradient, QRadialGradient and QConicalGradient.</db:para>
<db:para>So if the brush style is <db:link xlink:href="qt.xml#BrushStyle-enum">Qt::LinearGradientPattern</db:link>, we first create a QLinearGradient object with interpolation area between the coordinates passed as arguments to the constructor. The positions are specified using logical coordinates. Then we set the gradient's colors using the QGradient::setColorAt() function. The colors is defined using stop points which are composed by a position (between 0 and 1) and a QColor. The set of stop points describes how the gradient area should be filled. A gradient can have an arbitrary number of stop points.</db:para>
<db:para>In the end we call <db:code>RenderArea::setBrush()</db:code> slot to update the <db:code>RenderArea</db:code> widget's brush with the QLinearGradient object.</db:para>
<db:programlisting language="cpp">    } else if (style == Qt::RadialGradientPattern) {
        QRadialGradient radialGradient(50, 50, 50, 70, 70);
        radialGradient.setColorAt(0.0, Qt::white);
        radialGradient.setColorAt(0.2, Qt::green);
        radialGradient.setColorAt(1.0, Qt::black);
        renderArea-&gt;setBrush(radialGradient);
    } else if (style == Qt::ConicalGradientPattern) {
        QConicalGradient conicalGradient(50, 50, 150);
        conicalGradient.setColorAt(0.0, Qt::white);
        conicalGradient.setColorAt(0.2, Qt::green);
        conicalGradient.setColorAt(1.0, Qt::black);
        renderArea-&gt;setBrush(conicalGradient);
</db:programlisting>
<db:para>A similar pattern of actions, as the one used for QLinearGradient, is used in the cases of <db:link xlink:href="qt.xml#BrushStyle-enum">Qt::RadialGradientPattern</db:link> and <db:link xlink:href="qt.xml#BrushStyle-enum">Qt::ConicalGradientPattern</db:link>.</db:para>
<db:para>The only difference is the arguments passed to the constructor: Regarding the QRadialGradient constructor the first argument is the center, and the second the radial gradient's radius. The third argument is optional, but can be used to define the focal point of the gradient inside the circle (the default focal point is the circle center). Regarding the QConicalGradient constructor, the first argument specifies the center of the conical, and the second specifies the start angle of the interpolation.</db:para>
<db:programlisting language="cpp">    } else if (style == Qt::TexturePattern) {
        renderArea-&gt;setBrush(QBrush(QPixmap(&quot;:/images/brick.png&quot;)));
</db:programlisting>
<db:para>If the brush style is <db:link xlink:href="qt.xml#BrushStyle-enum">Qt::TexturePattern</db:link> we create a QBrush from a QPixmap. Then we call <db:code>RenderArea::setBrush()</db:code> slot to update the <db:code>RenderArea</db:code> widget with the newly created brush.</db:para>
<db:programlisting language="cpp">    } else {
        renderArea-&gt;setBrush(QBrush(Qt::green, style));
    }
}
</db:programlisting>
<db:para>Otherwise we simply create a brush with the given style and a green color, and then call <db:code>RenderArea::setBrush()</db:code> slot to update the <db:code>RenderArea</db:code> widget with the newly created brush.</db:para>
</db:section>
<db:section xml:id="renderarea-class-definition">
<db:title>RenderArea Class Definition</db:title>
<db:para>The <db:code>RenderArea</db:code> class inherits QWidget, and renders multiple copies of the currently active shape using a QPainter.</db:para>
<db:programlisting language="cpp">class RenderArea : public QWidget
{
    Q_OBJECT

public:
    enum Shape { Line, Points, Polyline, Polygon, Rect, RoundedRect, Ellipse, Arc,
                 Chord, Pie, Path, Text, Pixmap };

    RenderArea(QWidget *parent = 0);

    QSize minimumSizeHint() const Q_DECL_OVERRIDE;
    QSize sizeHint() const Q_DECL_OVERRIDE;

public slots:
    void setShape(Shape shape);
    void setPen(const QPen &amp;pen);
    void setBrush(const QBrush &amp;brush);
    void setAntialiased(bool antialiased);
    void setTransformed(bool transformed);

protected:
    void paintEvent(QPaintEvent *event) Q_DECL_OVERRIDE;

private:
    Shape shape;
    QPen pen;
    QBrush brush;
    bool antialiased;
    bool transformed;
    QPixmap pixmap;
};
</db:programlisting>
<db:para>First we define a public <db:code>Shape</db:code> enum to hold the different shapes that can be rendered by the widget (i.e the shapes that can be rendered by a QPainter). Then we reimplement the constructor as well as two of QWidget's public functions: <db:link xlink:href="">minimumSizeHint()</db:link> and <db:link xlink:href="">sizeHint()</db:link>.</db:para>
<db:para>We also reimplement the QWidget::paintEvent() function to be able to draw the currently active shape according to the specified parameters.</db:para>
<db:para>We declare several private slots: The <db:code>setShape()</db:code> slot changes the <db:code>RenderArea</db:code>'s shape, the <db:code>setPen()</db:code> and <db:code>setBrush()</db:code> slots modify the widget's pen and brush, and the <db:code>setAntialiased()</db:code> and <db:code>setTransformed()</db:code> slots modify the widget's respective properties.</db:para>
</db:section>
<db:section xml:id="renderarea-class-implementation">
<db:title>RenderArea Class Implementation</db:title>
<db:para>In the constructor we initialize some of the widget's variables.</db:para>
<db:programlisting language="cpp">RenderArea::RenderArea(QWidget *parent)
    : QWidget(parent)
{
    shape = Polygon;
    antialiased = false;
    transformed = false;
    pixmap.load(&quot;:/images/qt-logo.png&quot;);

    setBackgroundRole(QPalette::Base);
    setAutoFillBackground(true);
}
</db:programlisting>
<db:para>We set its shape to be a <db:guilabel>Polygon</db:guilabel>, its antialiased property to be false and we load an image into the widget's pixmap variable. In the end we set the widget's background role, defining the brush from the widget's palette that will be used to render the background. QPalette::Base is typically white.</db:para>
<db:programlisting language="cpp">QSize RenderArea::sizeHint() const
{
    return QSize(400, 200);
}
</db:programlisting>
<db:para>The <db:code>RenderArea</db:code> inherits QWidget's sizeHint property holding the recommended size for the widget. If the value of this property is an invalid size, no size is recommended.</db:para>
<db:para>The default implementation of the QWidget::sizeHint() function returns an invalid size if there is no layout for the widget, and returns the layout's preferred size otherwise.</db:para>
<db:para>Our reimplementation of the function returns a <db:link xlink:href="qsize.xml">QSize</db:link> with a 400 pixels width and a 200 pixels height.</db:para>
<db:programlisting language="cpp">QSize RenderArea::minimumSizeHint() const
{
    return QSize(100, 100);
}
</db:programlisting>
<db:para><db:code>RenderArea</db:code> also inherits QWidget's minimumSizeHint property holding the recommended minimum size for the widget. Again, if the value of this property is an invalid size, no size is recommended.</db:para>
<db:para>The default implementation of QWidget::minimumSizeHint() returns an invalid size if there is no layout for the widget, and returns the layout's minimum size otherwise.</db:para>
<db:para>Our reimplementation of the function returns a <db:link xlink:href="qsize.xml">QSize</db:link> with a 100 pixels width and a 100 pixels height.</db:para>
<db:programlisting language="cpp">void RenderArea::setShape(Shape shape)
{
    this-&gt;shape = shape;
    update();
}

void RenderArea::setPen(const QPen &amp;pen)
{
    this-&gt;pen = pen;
    update();
}

void RenderArea::setBrush(const QBrush &amp;brush)
{
    this-&gt;brush = brush;
    update();
}
</db:programlisting>
<db:para>The public <db:code>setShape()</db:code>, <db:code>setPen()</db:code> and <db:code>setBrush()</db:code> slots are called whenever we want to modify a <db:code>RenderArea</db:code> widget's shape, pen or brush. We set the shape, pen or brush according to the slot parameter, and call QWidget::update() to make the changes visible in the <db:code>RenderArea</db:code> widget.</db:para>
<db:para>The QWidget::update() slot does not cause an immediate repaint; instead it schedules a paint event for processing when Qt returns to the main event loop.</db:para>
<db:programlisting language="cpp">void RenderArea::setAntialiased(bool antialiased)
{
    this-&gt;antialiased = antialiased;
    update();
}

void RenderArea::setTransformed(bool transformed)
{
    this-&gt;transformed = transformed;
    update();
}
</db:programlisting>
<db:para>With the <db:code>setAntialiased()</db:code> and <db:code>setTransformed()</db:code> slots we change the state of the properties according to the slot parameter, and call the QWidget::update() slot to make the changes visible in the <db:code>RenderArea</db:code> widget.</db:para>
<db:programlisting language="cpp">void RenderArea::paintEvent(QPaintEvent * /* event */)
{
    static const QPoint points[4] = {
        QPoint(10, 80),
        QPoint(20, 10),
        QPoint(80, 30),
        QPoint(90, 70)
    };

    QRect rect(10, 20, 80, 60);

    QPainterPath path;
    path.moveTo(20, 80);
    path.lineTo(20, 30);
    path.cubicTo(80, 0, 50, 50, 80, 80);

    int startAngle = 20 * 16;
    int arcLength = 120 * 16;
</db:programlisting>
<db:para>Then we reimplement the QWidget::paintEvent() function. The first thing we do is to create the graphical objects we will need to draw the various shapes.</db:para>
<db:para>We create a vector of four <db:link xlink:href="qpoint.xml">QPoint</db:link>s. We use this vector to render the <db:guilabel>Points</db:guilabel>, <db:guilabel>Polyline</db:guilabel> and <db:guilabel>Polygon</db:guilabel> shapes. Then we create a <db:link xlink:href="qrect.xml">QRect</db:link>, defining a rectangle in the plane, which we use as the bounding rectangle for all the shapes excluding the <db:guilabel>Path</db:guilabel> and the <db:guilabel>Pixmap</db:guilabel>.</db:para>
<db:para>We also create a QPainterPath. The QPainterPath class provides a container for painting operations, enabling graphical shapes to be constructed and reused. A painter path is an object composed of a number of graphical building blocks, such as rectangles, ellipses, lines, and curves. For more information about the QPainterPath class, see the <db:link xlink:href="qtwidgets-painting-painterpaths-example.xml">Painter Paths</db:link> example. In this example, we create a painter path composed of one straight line and a Bezier curve.</db:para>
<db:para>In addition we define a start angle and an arc length that we will use when drawing the <db:guilabel>Arc</db:guilabel>, <db:guilabel>Chord</db:guilabel> and <db:guilabel>Pie</db:guilabel> shapes.</db:para>
<db:programlisting language="cpp">    QPainter painter(this);
    painter.setPen(pen);
    painter.setBrush(brush);
    if (antialiased)
        painter.setRenderHint(QPainter::Antialiasing, true);
</db:programlisting>
<db:para>We create a QPainter for the <db:code>RenderArea</db:code> widget, and set the painters pen and brush according to the <db:code>RenderArea</db:code>'s pen and brush. If the <db:guilabel>Antialiasing</db:guilabel> parameter option is checked, we also set the painter's render hints. QPainter::Antialiasing indicates that the engine should antialias edges of primitives if possible.</db:para>
<db:programlisting language="cpp">    for (int x = 0; x &lt; width(); x += 100) {
        for (int y = 0; y &lt; height(); y += 100) {
            painter.save();
            painter.translate(x, y);
</db:programlisting>
<db:para>Finally, we render the multiple copies of the <db:code>RenderArea</db:code>'s shape. The number of copies is depending on the size of the <db:code>RenderArea</db:code> widget, and we calculate their positions using two <db:code>for</db:code> loops and the widgets height and width.</db:para>
<db:para>For each copy we first save the current painter state (pushes the state onto a stack). Then we translate the coordinate system, using the QPainter::translate() function, to the position determined by the variables of the <db:code>for</db:code> loops. If we omit this translation of the coordinate system all the copies of the shape will be rendered on top of each other in the top left cormer of the <db:code>RenderArea</db:code> widget.</db:para>
<db:programlisting language="cpp">            if (transformed) {
                painter.translate(50, 50);
                painter.rotate(60.0);
                painter.scale(0.6, 0.9);
                painter.translate(-50, -50);
            }
</db:programlisting>
<db:para>If the <db:guilabel>Transformations</db:guilabel> parameter option is checked, we do an additional translation of the coordinate system before we rotate the coordinate system 60 degrees clockwise using the QPainter::rotate() function and scale it down in size using the QPainter::scale() function. In the end we translate the coordinate system back to where it was before we rotated and scaled it.</db:para>
<db:para>Now, when rendering the shape, it will appear as if it was rotated in three dimensions.</db:para>
<db:programlisting language="cpp">            switch (shape) {
            case Line:
                painter.drawLine(rect.bottomLeft(), rect.topRight());
                break;
            case Points:
                painter.drawPoints(points, 4);
                break;
            case Polyline:
                painter.drawPolyline(points, 4);
                break;
            case Polygon:
                painter.drawPolygon(points, 4);
                break;
            case Rect:
                painter.drawRect(rect);
                break;
            case RoundedRect:
                painter.drawRoundedRect(rect, 25, 25, Qt::RelativeSize);
                break;
            case Ellipse:
                painter.drawEllipse(rect);
                break;
            case Arc:
                painter.drawArc(rect, startAngle, arcLength);
                break;
            case Chord:
                painter.drawChord(rect, startAngle, arcLength);
                break;
            case Pie:
                painter.drawPie(rect, startAngle, arcLength);
                break;
            case Path:
                painter.drawPath(path);
                break;
            case Text:
                painter.drawText(rect, Qt::AlignCenter, tr(&quot;Qt by\nDigia&quot;));
                break;
            case Pixmap:
                painter.drawPixmap(10, 10, pixmap);
            }
</db:programlisting>
<db:para>Next, we identify the <db:code>RenderArea</db:code>'s shape, and render it using the associated QPainter drawing function:</db:para>
<db:itemizedlist>
<db:listitem>
<db:para>QPainter::drawLine(),</db:para>
</db:listitem>
<db:listitem>
<db:para>QPainter::drawPoints(),</db:para>
</db:listitem>
<db:listitem>
<db:para>QPainter::drawPolyline(),</db:para>
</db:listitem>
<db:listitem>
<db:para>QPainter::drawPolygon(),</db:para>
</db:listitem>
<db:listitem>
<db:para>QPainter::drawRect(),</db:para>
</db:listitem>
<db:listitem>
<db:para>QPainter::drawRoundedRect(),</db:para>
</db:listitem>
<db:listitem>
<db:para>QPainter::drawEllipse(),</db:para>
</db:listitem>
<db:listitem>
<db:para>QPainter::drawArc(),</db:para>
</db:listitem>
<db:listitem>
<db:para>QPainter::drawChord(),</db:para>
</db:listitem>
<db:listitem>
<db:para>QPainter::drawPie(),</db:para>
</db:listitem>
<db:listitem>
<db:para>QPainter::drawPath(),</db:para>
</db:listitem>
<db:listitem>
<db:para>QPainter::drawText() or</db:para>
</db:listitem>
<db:listitem>
<db:para>QPainter::drawPixmap()</db:para>
</db:listitem>
</db:itemizedlist>
<db:para>Before we started rendering, we saved the current painter state (pushes the state onto a stack). The rationale for this is that we calculate each shape copy's position relative to the same point in the coordinate system. When translating the coordinate system, we lose the knowledge of this point unless we save the current painter state <db:emphasis>before</db:emphasis> we start the translating process.</db:para>
<db:programlisting language="cpp">            painter.restore();
        }
    }

    painter.setRenderHint(QPainter::Antialiasing, false);
    painter.setPen(palette().dark().color());
    painter.setBrush(Qt::NoBrush);
    painter.drawRect(QRect(0, 0, width() - 1, height() - 1));
}
</db:programlisting>
<db:para>Then, when we are finished rendering a copy of the shape we can restore the original painter state, with its associated coordinate system, using the QPainter::restore() function. In this way we ensure that the next shape copy will be rendered in the correct position.</db:para>
<db:para>We could translate the coordinate system back using QPainter::translate() instead of saving the painter state. But since we in addition to translating the coordinate system (when the <db:guilabel>Transformation</db:guilabel> parameter option is checked) both rotate and scale the coordinate system, the easiest solution is to save the current painter state.</db:para>
</db:section>
<db:section>
<db:title>List of Files</db:title>
<db:para>Files:</db:para>
<db:section>
<db:title>List of Files</db:title>
<db:itemizedlist>
<db:listitem>
<db:para><db:link xlink:href="painting/basicdrawing/basicdrawing.pro">painting/basicdrawing/basicdrawing.pro</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="painting/basicdrawing/basicdrawing.qrc">painting/basicdrawing/basicdrawing.qrc</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="painting/basicdrawing/main.cpp">painting/basicdrawing/main.cpp</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="painting/basicdrawing/renderarea.cpp">painting/basicdrawing/renderarea.cpp</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="painting/basicdrawing/renderarea.h">painting/basicdrawing/renderarea.h</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="painting/basicdrawing/window.cpp">painting/basicdrawing/window.cpp</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="painting/basicdrawing/window.h">painting/basicdrawing/window.h</db:link></db:para></db:listitem>
</db:itemizedlist>
</db:section>
<db:section>
<db:title>List of Files</db:title>
<db:para>Images:</db:para>
<db:section>
<db:title>List of Files</db:title>
<db:itemizedlist>
<db:listitem>
<db:para><db:link xlink:href="painting/basicdrawing/images/brick.png">painting/basicdrawing/images/brick.png</db:link></db:para></db:listitem>
<db:listitem>
<db:para><db:link xlink:href="painting/basicdrawing/images/qt-logo.png">painting/basicdrawing/images/qt-logo.png</db:link></db:para></db:listitem>
</db:itemizedlist>
</db:section>
</db:section></db:section></db:article>
